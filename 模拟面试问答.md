# IntelliLens-MCP — 模拟面试官深度追问

---

## 第一轮：架构设计

### Q1: "你为什么要选 LlamaIndex 而不是 LangChain？三句话说清楚。"

**答**：
1. **MetadataFilters 原生支持**：LlamaIndex 的 `MetadataFilters` 可以直接下推到向量存储层（Milvus 标量过滤），LangChain 需要手动拼装 filter 表达式，多租户场景下 LlamaIndex 的抽象更干净。
2. **官方可观测性集成**：`CallbackManager` + Langfuse 官方 handler，一行 `Settings.callback_manager = CallbackManager([handler])` 就完成了全链路追踪，不需要自己写拦截器。
3. **内置评估器**：`FaithfulnessEvaluator` 直接可用，配合 `aevaluate()` 异步调用，LangChain 的评估体系需要额外集成。

**追问**: "那 LlamaIndex 的缺点是什么？"

**答**：文档和社区比 LangChain 小，遇到边缘问题排查更费劲。另外 `IngestionPipeline` 的 `transformations` 抽象在复杂多步变换时不如 LangChain 的 LCEL 灵活。如果后续要做复杂的多步 Agent 编排，可能会混用两个框架。

---

### Q2: "你的五道防线，哪一道最难实现？为什么？"

**答**：最难的是**第一道——多租户 RBAC 在存储引擎层的硬隔离**。

不是因为代码复杂，而是因为**安全假设必须零容忍**。应用层的 `if tenant_id == ...` 过滤有个致命问题：如果某个新入职的工程师在写新 retriever 时忘了加过滤条件，数据就泄露了。

所以我把过滤推到 Milvus 的 `MetadataFilters`，在存储引擎查询阶段就完成隔离——即使上层代码有 bug，Milvus 也不会返回其他租户的数据。这是一种**纵深防御**思路：不在最后一层设防，而是在最底层设防。

**追问**: "那如果攻击者直接连 Milvus 呢？"

**答**：生产环境 Milvus 应该开启认证（token），并且放在 VPC 内网，不暴露公网端口。应用服务器通过内网 IP + Milvus token 连接。另外可以在 Milvus 前面加一层 Proxy 做审计日志。

---

### Q3: "检索流程中为什么需要向量+关键词两路融合？只用向量不够吗？"

**答**：这是为了应对**不同类型查询的召回需求**：

- **语义类查询**（"如何提升用户留存率"）：向量检索擅长，能抓到语义相关的概念性文档
- **精确类查询**（"合同编号 CN-2024-0883 的终止日期"）：向量检索可能漏掉或排到很后面，关键词匹配（BM25）对数字、编号、专业术语的召回更准确

两路融合用 `RELATIVE_SCORE` 模式归一化得分后合并排序，兼顾了语义覆盖和精确命中。

**追问**: "关键词检索引擎为什么自己写而不用 Elasticsearch？"

**答**：当前实现是内存 TF 词频匹配，适合 POC 阶段。文档里已标注生产应替换为 ES。之所以没直接集成 ES 是考虑到部署复杂度——这个项目定位是展示架构设计能力，每个模块都可以独立替换。关键词检索的 `KeywordRetriever` 实现了与 `BaseRetriever` 兼容的接口，换成 ES 只需换实现类，调用方代码零改动。

---

## 第二轮：分布式与高可用

### Q4: "Redis 限流用了 Lua 脚本，为什么不能只用 Redis 基础命令？"

**答**：因为 **TOCTOU 竞态**（Time-of-Check Time-of-Use）。

假设不用 Lua，分为三步：
```python
await redis.zremrangebyscore(key, 0, now - window)  # 清理
count = await redis.zcard(key)                        # 计数
if count < max:
    await redis.zadd(key, now, member)                # 新增
```

在 "计数" 和 "新增" 之间，另一个并发请求也通过了 `count < max` 的判断——两个请求都被放行，但实际计数可能已超限。在 4 worker × 1000 QPS 场景下，这种竞态会导致实际限流阈值失效。

Lua 脚本在 Redis 服务端单线程执行，三条命令不可分割，从根本上消除了竞态窗口。

**追问**: "为什么选择了 ZSET 而不是 String + INCR 的固定窗口？"

**答**：固定窗口（如每分钟计数器 + EXPIRE 60s）有个**边界突发**问题：
- 假设限制 60次/分钟
- 用户在 12:00:59 发了 60 次
- 在 12:01:00 计数器归零，又能发 60 次
- 实际 2 秒内发出了 120 次请求

滑动窗口用 ZSET 记录每个请求的精确时间戳，没有固定边界，任何 60 秒窗口内都不会超过阈值。

---

### Q5: "Redis 挂了你的限流怎么办？你会让服务中断吗？"

**答**：不会。我设计的策略是 **fail-open + 告警**：

```python
try:
    result = await redis.evalsha(...)
except (ConnectionError, TimeoutError):
    logger.error("Redis 不可用，fail-open 放行")
    return True  # 放行
```

选 fail-open 而不是 fail-closed 的原因是：限流是**保护性功能**，不是**核心业务路径**。因为限流故障导致所有合法请求被拒，这是把保护机制变成了单点故障。

**追问**: "那恶意用户趁着 Redis 挂了刷爆你的 API 怎么办？"

**答**：两个补充手段：
1. 在 Uvicorn 前面加一层 Nginx/Envoy，配置 `limit_req_zone` 作为 IP 级的兜底限流——这是进程级别的，不依赖 Redis
2. 可以在应用层加一个本地内存计数器作为二级限流（代价是进程间不一致，但至少能挡住单进程的极端情况）

---

### Q6: "如果你的主模型和备用模型都来自同一个 API Provider（比如都是 OpenAI），Provider 整体宕机了怎么办？"

**答**：当前设计的主备模型假设是不同的模型型号（gpt-4o → gpt-4o-mini），如果 Provider 整体宕机确实无法切换。

生产级的改进方案：
1. **多 Provider 配置**：`PRIMARY_MODEL` 走 OpenAI，`FALLBACK_MODEL` 走 Azure OpenAI 或 Anthropic Claude，API Base URL 分别配置
2. **本地模型兜底**：部署 vLLM 或 Ollama 托管一个开源模型（如 Qwen-2.5），作为第三层降级
3. **缓存热点问答**：高频问题走 Redis 缓存直接返回，减少实时 LLM 调用

当前代码的 `_get_fallback_llm()` 可以配置不同的 `api_base`，只需在 Settings 中增加 `FALLBACK_BASE_URL` 字段即可支持多 Provider。

---

## 第三轮：安全模型

### Q7: "JWT Token 被窃取了怎么办？你做了哪些防护？"

**答**：坦诚说，当前实现是基础 JWT 验证，没有做 Token 撤销和刷新机制。生产需要加：

1. **短期 Token + Refresh Token**：Access Token 有效期 15 分钟，Refresh Token 7 天，泄露窗口有限
2. **Token 黑名单**（Redis）：用户登出或异常行为时，将 `jti` 加入黑名单，中间件每次校验时查 Redis
3. **IP 绑定**（可选）：JWT Payload 中包含签发时的客户端 IP，验证时比对，防止 Token 跨网络使用
4. **限流本身就提供了一定保护**：同一个 tenant_id 一分钟内最多 60 次请求，即使 Token 泄露，攻击者也无法大规模滥用

---

### Q8: "你的 RBAC 三个角色（admin/editor/viewer），怎么和实际的企业权限系统对接？"

**答**：当前 `RBACGuard` 是依赖注入方式：

```python
@app.get("/admin-only")
async def admin_route(ctx: dict = Depends(RBACGuard("admin"))):
    ...
```

对接企业权限系统（如 LDAP/AD/Okta）有两种方式：

1. **JWT Payload 映射**：企业 SSO 签发的 JWT 中通常包含 `groups` 或 `realm_access.roles` 字段，在 `extract_tenant_context` 中映射到 admin/editor/viewer
2. **外部权限服务调用**：中间件拿到 `user_id` 后调用企业权限 API 查询角色，缓存结果到 Redis（TTL 5 分钟）

当前设计的关键是 `ROLE_HIERARCHY` 字典可以直接扩展——增加 `super_admin: 4` 或更细粒度的 `auditor: 0.5` 都不会破坏现有逻辑。

---

### Q9: "如果租户 A 的管理员恶意构造请求，尝试访问租户 B 的数据，你有哪些防护层？"

**答**：四层纵深防御：

| 层级 | 机制 | 如果被绕过 |
|---|---|---|
| 1. JWT 认证 | Token 中的 tenant_id 不可篡改（签名校验） | 需要窃取租户 B 的有效 Token |
| 2. RBACGuard | 路由级角色校验 | 同租户内不同角色的权限隔离 |
| 3. MetadataFilters | Milvus 存储引擎层过滤 | 即使应用层代码有 bug，Milvus 也不会返回跨租户数据 |
| 4. 审计追踪（Langfuse） | 每次查询记录 tenant_id + user_id + 检索结果 | 事后审计能发现异常查询模式 |

核心思想：不要把安全寄托在任何单一层上。最关键的第三层——即便有人跳过了前两层，存储引擎的硬过滤是最后一道无法绕过的防线。

---

## 第四轮：性能与扩展性

### Q10: "你的混合检索中，向量检索和关键词检索是串行还是并行？"

**答**：当前实现是串行：先向量检索拿到候选集，再在候选集上做关键词匹配和融合。

这在候选集不大（top_k=10）时没问题，但如果要做大规模召回（top_k=1000），优化方案是：

```python
# 并行执行 + asyncio.gather
vector_task = vector_retriever.aretrieve(query_bundle)
keyword_task = keyword_retriever.aretrieve(query_bundle)
vector_nodes, keyword_nodes = await asyncio.gather(vector_task, keyword_task)
```

然后融合两路独立的结果集，而不是关键词仅在向量结果上重排。这样两路可以独立扩展。

---

### Q11: "大模型调用慢怎么办？用户要等 3-5 秒。"

**答**：几个方向：

1. **流式输出（SSE）**：当前 `acomplete()` 是等待完整响应。改用 `astream_chat()` + StreamingResponse，用户 200ms 就能看到第一个 token
2. **缓存常见问题**：对高频查询（如"公司年假政策"），将最终回答缓存到 Redis，相同 query → 直接返回，跳过 LLM
3. **缩短上下文**：Rerank 只保留 Top-3（当前 Top-5），减少 Prompt Token 数
4. **异步预测**：对于可以预判的后续问题（如列表中的下一个），提前预热检索结果

当前`query_engine.py` 的 Prompt 是中英双语，可以去掉英文部分减少 Token。

---

### Q12: "Milvus 集合有多大？索引怎么选的？"

**答**：当前代码未涉及索引选型（Collection 假定已创建）。对于一个典型的企业知识库场景（百万级文档，千万级 Chunk）：

- **索引类型**：IVF_SQ8（内存敏感）或 HNSW（速度优先）
- **标量索引**：tenant_id 和 doc_id 建字典索引（Dict Index），过滤查询走索引不扫全表
- **分区策略**：按 tenant_id 做 Partition Key，大租户物理隔离，小租户共享分区

这些配置应该在 Migration 脚本中定义，不在应用代码中。

---

## 第五轮：质量与运维

### Q13: "你的裁判模型评估结果谁来看？发现了坏例然后呢？"

**答**：当前实现是**自动沉淀**——坏例写入 `bad_cases.jsonl`，但还没有自动处理链路。完整的闭环应该是：

```
bad_cases.jsonl
    → 定时任务（Celery/Cron）聚合分析
    → 按租户 + 错误类型分类
    → 高优 Bad Case 推送 Slack/钉钉通知 → 人工审核
    → 审核后的修正答案 → 写回知识库 / 微调模型 / 更新 Prompt
```

`bad_cases.jsonl` 的 JSONL 格式设计就是为了方便后续流式处理——每行一个独立 JSON，可以 tail -f 实时消费，也可以批量导入 Pandas 做分析。

---

### Q14: "FaithfulnessEvaluator 本身也可能产生幻觉（裁判误判），你怎么处理？"

**答**：确实存在这个问题。裁判模型的评估结果不能 100% 相信：

1. **阈值不是硬边界**：score=0.79 和 score=0.81 在统计上可能没有显著差异，所以 `bad_cases.jsonl` 中同时记录了 `eval_score` 和 `eval_feedback`，人工审核时可以二次判断
2. **多裁判交叉验证**（生产优化）：对同一回答用多个裁判模型打分（GPT-4o + Claude），取最低分或投票
3. **定期人工抽检**：每周从 `bad_cases.jsonl` 中随机抽 20 条人工复审，计算裁判模型的准确率和召回率

当前设计中 temperature=0.0 可以减少裁判模型自身的随机性，但不能消除系统性偏见。

---

### Q15: "这个系统如果要上生产，还缺什么？"

**答**：

| 缺失项 | 优先级 | 说明 |
|---|---|---|
| Collection Schema Migration | P0 | 首次部署需要创建 Milvus Collection + 索引 |
| Token 刷新/撤销机制 | P0 | JWT 目前无状态，无法主动失效 |
| Nginx/Envoy 反向代理 | P1 | TLS 终止、静态文件、连接限流 |
| 健康检查就绪探针 | P1 | K8s readiness probe 需要检查 Milvus + Redis 连接 |
| 配置热更新 | P1 | 当前修改配置需要重启服务 |
| 日志聚合 | P1 | Loguru 写文件 → Fluentd/Fluent Bit → Elasticsearch |
| 指标暴露 | P2 | Prometheus metrics（请求延迟、检索延迟、限流触发次数） |
| 集成测试 | P2 | 当前只有单元架构，缺少 E2E 测试 |
| 多 Provider 降级 | P2 | 支持 OpenAI + Azure + Anthropic 互备 |
| Redis Cluster/Sentinel | P2 | 限流层高可用 |

---

## 第六轮：独立 coding 能力

### Q16: "如果让你加一个功能——用户对回答不满意可以点'踩'，然后系统用这个反馈来优化检索策略。你打算怎么设计？"

**答**：三步设计：

**第一步：反馈收集（轻量）**
```python
# POST /v1/feedback
class FeedbackRequest(BaseModel):
    query_id: str      # 哪次查询
    rating: int        # 1-5 打分，或 thumbs_up / thumbs_down
    comment: str       # 用户自由反馈（可选）
```
数据写入 `data/feedback.jsonl`，同时上报 Langfuse 作为 `score` trace。

**第二步：检索策略自适应（离线）**
- 定期分析低分 query 的共性（query 长度？领域术语？缺少的关键词？）
- 如果特定领域的低分率高 → 调整 `SemanticSplitterNodeParser` 的分块大小
- 如果关键词类查询低分率高 → 增加 `KeywordRetriever` 的融合权重（`FUSION_MODES.RECIPROCAL_RANK`）

**第三步：Prompt 优化（半自动）**
- 坏例聚类 → 发现重复的错误模式（如"回答引用了过期的合同模板"）
- 在 Prompt 中加入反例：`"如果上下文中包含日期早于 2023 年的合同，请明确标注'该信息可能已过期'"`
- A/B 测试新旧 Prompt

关键设计原则：用户反馈用于**优化检索策略和 Prompt**，而不是直接喂给模型微调——避免某条错误反馈污染模型。

---

### Q17: "AsyncEvaluator 的 tenacity 重试，如果 3 次都失败了，评估就丢了。有没有更好的方案？"

**答**：当前设计是"尽力而为"（best-effort），评估丢失意味着坏例没被记录。更可靠的方案：

**方案 A：死信队列（DLQ）**
```python
try:
    result = await evaluate_with_retry(...)
except MaxRetryError:
    await redis.lpush("eval_dlq", json.dumps(eval_request))
    # 独立 worker 定期重试 DLQ 中的任务
```

**方案 B：数据库持久化 + 定时任务**
```python
# 评估任务先写入 DB（状态=pending）
await db.insert("eval_tasks", {status: "pending", ...})
# 定时 Celery 任务扫描 pending 任务并执行
# 失败标记为 failed，超过 N 次标记为 abandoned
```

**方案 C（当前最佳平衡）**：BackgroundTasks + Redis DLQ
- 主流程：BackgroundTasks → evaluate → 失败 → 写入 DLQ
- 后台 Cron 每分钟扫描 DLQ 重试（最多 3 次）
- 3 次后告警 + 人工介入

当前代码选"尽力而为"是为了保持模块简洁，面试中我会说明：这是一个有意识的权衡——评估是离线质量监控，不是在线事务，丢失少量评估样本不影响业务正确性。

---

### Q18: "Retriever 中你用 monkey-patch 替换了 vector_store.query 方法。这个做法有什么问题？如果两个并发请求同时进来会怎样？"

**答**：这是一个很好的发现。当前代码：

```python
self.vector_store.query = filtered_query  # 全局修改！
try:
    nodes = await vector_retriever.aretrieve(...)
finally:
    self.vector_store.query = original_query
```

问题确实存在：**两个并发请求的 tenant_id 不同，后一个请求会覆盖前一个的 filter**。在 `finally` 恢复前，另一个请求可能用到了错误的 tenant filter。

**正确的修复方案**（我标注为待优化）：

```python
async def retrieve(self, query: str, tenant_id: str, top_k: int = 10):
    # 方案1: 如果 MilvusVectorStore 支持 filter 参数
    retriever = self.vector_store.as_retriever(
        similarity_top_k=top_k,
        vector_store_kwargs={"filters": tenant_filter}
    )
    
    # 方案2: 直接调用 vector_store.aquery() 传入 filter
    query = VectorStoreQuery(
        query_str=query, 
        similarity_top_k=top_k,
        filters=tenant_filter
    )
    result = await self.vector_store.aquery(query)
```

这是当前代码中最大的已知技术债，在"修复项目.md"和面试中应当主动提及——展示对并发安全的理解。

---

## 总结：面试官可能的评价维度

| 维度 | 表现 |
|---|---|
| 架构能力 | 五道防线设计有层次，知道每一层的不可替代性 |
| 安全意识 | 理解纵深防御，存储层过滤 > 应用层过滤 |
| 分布式理解 | Redis Lua 原子操作，ZSET vs 固定窗口的区别 |
| 工程务实 | 承认 monkey-patch 是临时方案，知道如何正确修复 |
| 面试表达 | 能一句话说清选型理由，不堆砌术语 |
