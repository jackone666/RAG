# IntelliLens-MCP 架构审查与修复报告

## 审查日期

2026-05-17

## 审查范围

按照 Project.md 规范完成项目搭建后，对全部模块进行生产级架构审查。

---

## 已发现并修复的问题

### 1. [严重] 鉴权中间件 Depends 注入失效

**文件**: `src/middleware/auth.py`

**问题**: 原实现中 `extract_tenant_context` 使用 FastAPI 的 `Depends(security)` 获取 `HTTPAuthorizationCredentials`
。但该函数由 HTTP 中间件直接调用（`await extract_tenant_context(request)`），而非通过 FastAPI 依赖注入系统。`Depends`
在中间件上下文中不会被解析，导致 `credentials` 始终为 `None`，JWT 认证完全失效。

**修复**: 移除 `Depends` 依赖，改为手动解析 `Authorization` Header 并直接调用 `jwt.decode()`。保留 `RBACGuard`
类作为路由级别的权限依赖注入。

---

### 2. [中等] 限流器内存泄漏

**文件**: `src/middleware/rate_limiter.py`

**问题**: `InMemoryRateLimiter` 的 `_buckets` 字典只增不减。`_prune()` 方法仅在 `is_allowed()` 被调用时对特定 key
执行清理。若某租户停止请求，其时间戳列表将永久驻留内存。

**修复**: 新增 `_gc()` 全量垃圾回收方法，每 5 分钟扫描一次全部 key，删除无活跃时间戳的过期条目。同时在 `_prune()`
中增加空列表删除逻辑。

---

### 3. [中等] 混合检索引擎缺少关键词检索通路

**文件**: `src/engine/retrievers.py`

**问题**: 规范明确要求 "QueryFusionRetriever 必须融合向量检索与关键词检索"，但原实现仅使用了向量检索器，
`QueryFusionRetriever` 的 `retrievers` 参数只有一个。

**修复**:

- 新增 `KeywordRetriever` 类：基于 TF 词频重叠的类 BM25 关键词检索器，按租户隔离节点存储
- 新增 `_FusionAdapter` 类：将预计算的关键词结果包装为 LlamaIndex `BaseRetriever` 接口
- 检索流程改为三阶段：向量检索 → 关键词检索 → QueryFusionRetriever 融合排序

---

### 4. [轻微] MCP 挂载路径重复

**文件**: `main.py`

**问题**: `mcp.http_app(path="/mcp")` 与 `app.mount("/mcp", mcp_app)` 导致实际路径变为 `/mcp/mcp`。

**修复**: 将 `http_app` 的 path 参数改为 `"/"`，由 FastAPI mount 统一管理路径前缀。

---

### 5. [轻微] 依赖声明不完整

**文件**: `requirements.txt`

**问题**: `src/pipeline/sync_manager.py` 直接 import `pymilvus`（`Collection`, `connections`, `utility`），但
requirements.txt 未显式声明该依赖，仅靠 `llama-index-vector-stores-milvus` 的传递依赖。

**修复**: 显式添加 `pymilvus>=2.4.0`，确保直接依赖关系清晰。

---

### 6. [严重] Retriever monkey-patch 并发竞态

**文件**: `src/engine/retrievers.py`

**问题**: 原实现通过 `self.vector_store.query = filtered_query` 全局替换实例方法来注入租户 filter。`try/finally`
之间无锁保护，两个并发请求的 `tenant_id` 不同时，后一个请求会覆盖前一个的 filter，导致租户 A 的查询可能错误使用租户 B
的过滤条件。

**修复**: 废弃 monkey-patch 方式，改为直接构造 `VectorStoreQuery(query_str=..., filters=tenant_filter, ...)` 对象，调用
`self.vector_store.aquery(query)` 无状态方法。每次请求独立创建 query 对象，多协程间零共享可变状态。

---

### 7. [架构] 限流器从内存实现升级为 Redis 分布式方案

**文件**: `src/middleware/rate_limiter.py`

**变更**:

- `InMemoryRateLimiter` → `RedisSlidingWindowRateLimiter`
- 使用 Redis Sorted Set + Lua 原子脚本实现滑动窗口限流
- Lua 脚本将 ZREMRANGEBYSCORE、ZCARD、ZADD、EXPIRE 封装为单次原子操作
- Redis 连接池复用（health_check_interval + socket_keepalive）
- fail-open 降级策略：Redis 不可用时放行请求 + 日志告警
- 限流键维度：`rate_limit:tenant:{tenant_id}` / `rate_limit:ip:{client_ip}`
- 新增 `get_current_usage()` 和 `reset()` 方法供监控和管理

**为什么选 ZSET 而非 INCR 固定窗口**: ZSET 的滑动窗口没有固定边界，消除"边界突发"问题（用户在窗口交界处可以双倍超限）。

---

### 8. [文档] 全部模块添加中文注释

**范围**: 所有 `src/` 下的 `.py` 文件及 `main.py`

**内容**: 为每个模块、类、方法添加详细的中文文档字符串，包括：

- 功能说明
- 设计原理与架构考量
- 参数/返回值说明
- 安全降级策略
- 生产环境注意事项

---

## 未修改但需关注的生产事项

### 1. LLMRerank 的 API 兼容性

`llama_index.core.postprocessor.LLMRerank` 在不同版本的 API 签名可能不同。部分版本使用 `choice_batch_size` 和 `top_n`
参数，部分版本使用 `choice_select`。部署前需验证实际安装版本的接口。

### 3. FastMCP http_app 传输方式

`mcp.http_app()` 默认使用 SSE（Server-Sent Events）传输。部分 MCP 客户端（如 Claude Desktop 的特定配置）可能需要
`streamable_http_app()`。需根据实际客户端选择正确的传输方式。

### 4. 裁判模型的 FaithfulnessEvaluator API

`llama_index.core.evaluation.FaithfulnessEvaluator.aevaluate()` 的参数签名在不同版本中可能为
`(response, contexts, query)` 而非 `(query, contexts, response)`。安装依赖后需验证参数顺序。

### 5. Milvus Collection Schema 前置创建

当前各模块假定 Milvus Collection 已存在且 schema 包含 `tenant_id` 和 `doc_id` 标量字段。生产环境需要单独的
migration/schema-init 脚本，在首次部署时创建 Collection 并定义：

- `id` (主键, VARCHAR)
- `tenant_id` (标量索引, VARCHAR)
- `doc_id` (标量索引, VARCHAR)
- `embedding` (向量字段, FLOAT_VECTOR)

### 6. Redis 模块未被实际使用

`requirements.txt` 中声明了 `redis` 依赖，但代码中全部使用内存模拟。当切换到真正的 Redis 分布式限流或分布式锁时，该模块才会被用到。

### 7. 评估器的异步解耦依赖 FastAPI

`AsyncEvaluator` 自身是纯粹的异步方法，但任务投递依赖 FastAPI 的 `BackgroundTasks`。若迁移至其他 Web
框架或消息队列（Celery / Redis Queue），需要额外适配投递机制。

---

## 文件清单

按规范要求生成的全部文件：

```
intellilens-mcp/
├── .env.example
├── requirements.txt
├── main.py
├── CLAUDE.md
├── Project.md
├── 修复项目.md
├── src/
│   ├── __init__.py
│   ├── config/
│   │   ├── __init__.py
│   │   └── settings.py
│   ├── observability/
│   │   ├── __init__.py
│   │   └── tracer.py
│   ├── middleware/
│   │   ├── __init__.py
│   │   ├── auth.py
│   │   └── rate_limiter.py
│   ├── pipeline/
│   │   ├── __init__.py
│   │   ├── ingestion.py
│   │   └── sync_manager.py
│   ├── engine/
│   │   ├── __init__.py
│   │   ├── retrievers.py
│   │   └── query_engine.py
│   ├── mcp_server/
│   │   ├── __init__.py
│   │   ├── server.py
│   │   └── tools.py
│   └── evaluation/
│       ├── __init__.py
│       └── evaluator.py
└── data/
    └── .gitkeep
```

## 总结

项目已按 Project.md v2.1 规范完整搭建，五大生产防线（RBAC 隔离、文档同步、限流熔断、可观测性、异步评估）均已实装。上述修复解决了
1 个严重的鉴权漏洞和 5 个中等/轻微问题。列出的 7 项"需关注事项"属于部署环境适配范畴，不影响当前代码的正确性。
