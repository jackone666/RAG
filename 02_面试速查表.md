# IntelliLens-MCP 面试速查表

## 一句话介绍

> IntelliLens-MCP 是一个企业级 RAG + MCP 系统，通过多租户隔离、混合检索、查询理解、质量评估和 Agent 工具化，把 RAG 从 demo 做成可运营的系统。

## 核心竞争力（5 个方面）

### 1. 安全（多租户隔离）
- **关键词**：纵深防御、存储层过滤、MetadataFilters
- **一句话**：权限不在应用层设防，而是在 Milvus 存储引擎层设防
- **面试要点**：即使上层代码有 bug，Milvus 也不会返回其他租户的数据

### 2. 准确（检索增强）
- **关键词**：混合检索、查询理解、上下文压缩、rerank
- **一句话**：向量负责"意思像"，关键词负责"字面准"，rerank 负责"排得准"
- **面试要点**：企业知识库需要同时处理语义查询和精确查询

### 3. 稳定（高可用）
- **关键词**：主备模型降级、Redis 限流、fail-open、缓存失效
- **一句话**：主力不在线，替补上场；替补也不行，给用户体面提示
- **面试要点**：限流故障不应导致服务中断，应该 fail-open

### 4. 可治理（质量闭环）
- **关键词**：异步评估、坏例沉淀、golden set、Langfuse 追踪
- **一句话**：机器先筛错题，人再整理成题库
- **面试要点**：LLM judge 自动发现问题，但不能完全替代人工标注

### 5. 可接入 Agent（MCP 工具化）
- **关键词**：MCP、工具暴露、Claude Desktop
- **一句话**：通过 MCP 把企业知识搜索暴露给外部 Agent 调用
- **面试要点**：标准化工具接口，Claude Desktop 原生支持

## 技术栈速记

| 组件 | 选型 | 为什么 |
|------|------|-------|
| Web 框架 | FastAPI | 异步原生，自动 OpenAPI |
| RAG 框架 | LlamaIndex | MetadataFilters 原生支持，Langfuse 官方集成 |
| 向量库 | Milvus | 支持标量过滤，多租户友好 |
| 关键词索引 | Elasticsearch | 企业级搜索，支持复杂查询 |
| 缓存 | Redis | 分布式限流、ByteCache、热点加速 |
| 文件存储 | MinIO | S3 兼容，支持原文保存 |
| 嵌入模型 | bge-m3 | 多语言，1024 维，中文友好 |
| 重排模型 | bge-reranker-v2-m3 | 专门做 query-document 相关性 |
| 生成模型 | gpt-4o（主）+ gpt-4o-mini（备） | 效果稳定 + 成本平衡 |
| 评估模型 | gpt-4o | 强推理能力，异步执行 |
| 可观测性 | Langfuse | 全链路 trace，支持自定义 span |

## 高频问题速答

### Q: 为什么选 LlamaIndex 而不是 LangChain？
**答**：三个原因：
1. MetadataFilters 原生支持，可以直接下推到 Milvus
2. CallbackManager + Langfuse 官方集成，一行代码全链路追踪
3. 内置 FaithfulnessEvaluator，配合 aevaluate() 异步调用

### Q: 多租户隔离怎么做的？
**答**：四层纵深防御：
1. JWT 认证（Token 中的 tenant_id 不可篡改）
2. RBACGuard（路由级角色校验）
3. MetadataFilters（Milvus 存储层硬过滤）
4. Langfuse 审计追踪（事后发现异常）

### Q: 为什么要混合检索？
**答**：企业知识库有两类查询：
- 语义类（"如何提升用户留存率"）→ 向量检索擅长
- 精确类（"合同编号 CN-2024-0883"）→ 关键词检索擅长
- 两路融合用 RRF 归一化后合并

### Q: Redis 限流为什么用 Lua 脚本？
**答**：消除 TOCTOU 竞态。不用 Lua 的话，"计数" 和 "新增" 之间有并发窗口，两个请求都可能通过判断。Lua 脚本在 Redis 服务端单线程执行，三条命令不可分割。

### Q: Redis 挂了怎么办？
**答**：fail-open 策略。限流是保护性功能，不是核心业务路径。Redis 故障导致所有请求被拒，这是把保护机制变成了单点故障。应该放行请求，同时告警。

### Q: 为什么要上下文压缩？
**答**：rerank 后的 chunk 仍然很长，直接塞给大模型会增加 token 成本和噪声。关键句压缩只保留和 query 最相关的句子，减少幻觉风险。

### Q: 为什么选 bge-m3 做 embedding？
**答**：三个原因：
1. 多语言能力强，适合中文和中英混合企业文档
2. 1024 维表达能力较好
3. 接口调用部署成本低

### Q: LLM judge 为什么还要人工评估集？
**答**：LLM judge 可以自动发现坏例，但也会有偏差。把 bad cases 沉淀成 golden set candidate 后，人工补 expected answer 和 label，就能做稳定的离线回归。

### Q: 缓存为什么要失效？
**答**：ByteCache 和 TTLCache 能降低热点查询延迟，但文档删除或更新后，如果不清缓存，可能查到旧知识。缓存失效是 RAG 数据治理的一部分。

## 已知问题与改进方向

### 已知问题

| 问题 | 位置 | 影响 | 改进方案 |
|------|------|------|--------|
| monkey-patch 并发不安全 | `retrievers.py` | 并发请求可能用错 tenant_id | 用 VectorStoreQuery 的 filters 参数 |
| 评估丢失 | `evaluator.py` | 3 次重试失败后评估丢了 | 加 DLQ + 定时重试 |
| 缓存失效被动 | `byte_cache.py` | 文档更新后需要等 TTL 过期 | 文档删除时主动清理缓存 |
| 检索串行 | `retrievers.py` | 向量和关键词检索不并行 | 用 asyncio.gather 并行化 |

### 可继续演进的方向

1. **动态融合权重**：根据 query 类型动态调整向量/关键词权重
2. **多粒度索引**：文档级粗筛、段落级中筛、句子级精筛
3. **近重复检测**：embedding similarity >= 0.95 检测转载版文档
4. **更细缓存失效**：根据 doc_id/tenant_id 精确清理缓存
5. **表格/OCR 增强**：PDF 表格、图片 OCR、页码定位
6. **人工评估闭环**：把 pending golden cases 做成标注后台

## 面试开场白（30 秒版）

> 我做的是一个企业级 RAG 系统。核心目标是把 RAG 从 demo 做成可运营的系统。
>
> 在安全方面，我通过多租户隔离下推到存储层（Milvus MetadataFilters）实现纵深防御。
>
> 在准确性方面，我用混合检索（向量 + 关键词）、查询理解（改写、分解、HyDE）、上下文压缩和 rerank 来提升效果。
>
> 在稳定性方面，我用 Redis 限流、主备模型降级、异步评估和缓存失效机制来保证可用性。
>
> 在可治理方面，我把坏例沉淀成人工评估集，形成从线上问题到离线改进的闭环。
>
> 同时通过 MCP 把企业知识搜索暴露给外部 Agent 调用。

## 面试开场白（1 分钟版）

> IntelliLens-MCP 是一个企业级 RAG + MCP 系统。我主要解决三类问题：
>
> **第一是安全**。通过 JWT、RBAC 和 tenant_id 下推到 Milvus/ES 实现多租户隔离。关键是不在应用层设防，而是在存储引擎层设防——即使上层代码有 bug，Milvus 也不会返回其他租户的数据。
>
> **第二是效果**。通过术语归一化、查询改写、HyDE、向量 + 关键词混合检索、RRF 融合、reranker 和上下文压缩提升召回与生成质量。核心思想是"先理解问题，再多路召回，再精排压缩，再生成评估"。
>
> **第三是生产可用性**。通过 Redis 限流（Lua 脚本 + 滑动窗口）、ByteCache、主备模型降级、Langfuse 追踪、异步 LLM judge 和人工评估集候选沉淀，把 RAG 从 demo 做成可运营的系统。
>
> 同时通过 MCP 把企业知识搜索暴露给外部 Agent 调用。

## 面试中可能被问到的深度问题

### 架构设计类
- 为什么选 LlamaIndex？缺点是什么？
- 五道防线中哪一道最难实现？为什么？
- 如果 Provider 整体宕机怎么办？
- 如何从单机改成 Kubernetes 集群？

### 分布式与高可用类
- Redis 限流为什么用 Lua 脚本？
- Redis 挂了怎么办？
- 向量检索和关键词检索是串行还是并行？
- 大租户的文档特别多，检索变慢怎么办？

### 安全与权限类
- JWT Token 被窃取了怎么办？
- 租户 A 的管理员恶意访问租户 B 的数据，有哪些防护层？
- RBAC 三个角色怎么和企业权限系统对接？

### 性能与优化类
- 系统中有哪些性能瓶颈？怎么排查？
- 大模型调用慢怎么办？
- Milvus 集合有多大？索引怎么选的？
- 如果要做 A/B 测试，怎么设计？

### 质量与运维类
- 裁判模型评估结果谁来看？发现坏例然后呢？
- FaithfulnessEvaluator 本身也可能产生幻觉，怎么处理？
- 这个系统上生产还缺什么？
- 如果用户上传 10GB 的 PDF 怎么处理？

### 独立 coding 能力类
- 加一个功能——用户对回答不满意可以点"踩"，怎么设计？
- AsyncEvaluator 的 tenacity 重试，3 次都失败了怎么办？
- Retriever 中的 monkey-patch 有什么问题？并发请求怎样？

## 面试中的表达技巧

### 好的表达方式
- ✅ "我没有只在 API 层判断权限，而是把租户条件下推到存储层"
- ✅ "这是一个有意识的权衡——评估是离线质量监控，不是在线事务"
- ✅ "当前代码中最大的已知技术债是 monkey-patch 的并发安全问题"
- ✅ "生产改进方案是用 VectorStoreQuery 的 filters 参数，而不是全局修改"

### 不好的表达方式
- ❌ "我用了 LlamaIndex 因为它很好用"
- ❌ "我的系统很安全"（没有说明具体机制）
- ❌ "这个问题我没想过"（应该说"这是一个很好的发现，生产改进方案是..."）
- ❌ "我的代码没有问题"（应该主动提及已知问题）

## 快速查阅：关键数字

- **代码规模**：36 个 Python 模块，~5000 行核心代码
- **文件大小限制**：50MB
- **限流阈值**：60 次/分钟（可配置）
- **缓存 TTL**：5 分钟（可配置）
- **Rerank Top-N**：5（可配置）
- **评估重试次数**：3 次（可配置）
- **embedding 维度**：1024（bge-m3）
- **主模型**：gpt-4o
- **备用模型**：gpt-4o-mini
- **评估模型**：gpt-4o

## 快速查阅：关键文件

| 功能 | 文件 | 关键函数 |
|------|------|--------|
| 多租户隔离 | `middleware/auth.py` | `extract_tenant_context()` |
| 限流 | `middleware/rate_limiter.py` | `check_rate_limit()` |
| 查询改写 | `engine/query_rewriter.py` | `rewrite_query()` |
| 混合检索 | `engine/retrievers.py` | `TenantAwareQueryFusionRetriever` |
| 生成 | `engine/query_engine.py` | `RAGQueryEngine.query()` |
| 文档入库 | `pipeline/ingestion.py` | `ingest_document()` |
| 文档删除 | `pipeline/sync_manager.py` | `delete_document()` |
| 质量评估 | `evaluation/evaluator.py` | `AsyncEvaluator.evaluate()` |
| 缓存 | `utils/byte_cache.py` | `ByteCache` |
| MCP 工具 | `mcp_server/tools.py` | `search_enterprise_knowledge()` |
