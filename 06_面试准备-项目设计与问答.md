# IntelliLens-MCP 面试准备文档

## 1. 项目一句话介绍

IntelliLens-MCP 是一个面向企业知识库的数据治理与 Agentic RAG 系统，核心目标是让企业内部文档在多租户隔离、权限控制、可观测、可评估、可降级的前提下，被
FastAPI 接口和 MCP 客户端安全调用。

面试时可以这样开场：

> 我做的是一个企业级 RAG 系统，不只是把文档切块后丢进向量库，而是围绕真实生产场景补齐了多租户
> RBAC、文档生命周期同步、混合检索、重排、主备模型降级、流式输出、LLMOps 追踪和异步质量评估。它既可以作为 HTTP 服务给业务系统调用，也可以通过
> MCP 暴露给 Agent 客户端。

## 2. 总体架构

### 2.1 请求主链路

```text
HTTP / MCP Request
  -> tenant_context_middleware
     从 JWT 或 X-Tenant-ID / X-Role 提取租户上下文
  -> rate_limit_dependency
     Redis ZSET + Lua 滑动窗口限流
  -> QueryRewriter
     查询改写，生成多路等价查询
  -> TenantAwareQueryFusionRetriever
     Milvus 向量检索 + Elasticsearch 关键词检索
     两路都强制 tenant_id 过滤
  -> Fusion
     weighted RRF 或 LlamaIndex fusion 融合排序
  -> RAGQueryEngine.rerank
     BAAI/bge-reranker-v2-m3 重排 Top-N
  -> RAGQueryEngine.query
     主模型生成，失败切备用模型，再失败返回友好降级文案
  -> AsyncEvaluator
     异步裁判模型评估检索质量和生成质量，坏例入库
  -> Langfuse
     记录 retrieval / rerank / generation span
```

### 2.2 离线入库链路

```text
Upload Document
  -> 文件类型和大小校验
  -> 文本抽取：txt / md / pdf / docx
  -> 文档清洗和标题结构解析
  -> content_hash 重复检测
  -> MinIO 保存原始文件
  -> SemanticSplitter 语义分块
  -> 注入 tenant_id / doc_id / content_hash
  -> BAAI/bge-m3 远程嵌入
  -> Milvus 写入向量
  -> Elasticsearch 后台写入关键词索引
```

### 2.3 核心组件分层

| 层级       | 模块                                                                                       | 职责                               |
|----------|------------------------------------------------------------------------------------------|----------------------------------|
| API 层    | `main.py`, `src/api/documents.py`, `src/api/auth.py`                                     | 查询、流式查询、文档上传、登录、租户信息             |
| 安全层      | `src/middleware/auth.py`, `src/middleware/rate_limiter.py`                               | JWT / Header 认证、RBAC、Redis 分布式限流 |
| 检索层      | `src/engine/retrievers.py`, `src/engine/query_rewriter.py`                               | 查询改写、Milvus 向量检索、ES 关键词检索、融合排序   |
| 生成层      | `src/engine/query_engine.py`                                                             | 重排、Prompt 构建、主备模型降级、SSE 流式生成     |
| 数据治理     | `src/pipeline/ingestion.py`, `src/pipeline/sync_manager.py`, `src/pipeline/doc_store.py` | 文档入库、原文存储、去重、删除同步、租户清理           |
| 质量评估     | `src/evaluation/evaluator.py`, `src/storage/pg_store.py`                                 | LLM judge、坏例沉淀、评估统计              |
| 可观测性     | `src/observability/tracer.py`                                                            | Langfuse trace/span，记录检索、重排、生成指标 |
| Agent 接入 | `src/mcp_server/tools.py`                                                                | MCP tool 暴露企业知识搜索能力              |

## 3. 关键设计

### 3.1 数据预处理决定 RAG 下限

这个项目的数据预处理不是简单读取文本，而是把“原始文件”转成“适合检索、适合生成、适合治理”的 chunk。

完整流程：

```text
原始文件
  -> 文件类型校验：txt / md / pdf / docx
  -> 文件大小校验：最大 50MB
  -> 文本抽取：PyMuPDF 解析 PDF，python-docx 解析 DOCX
  -> 文本清洗：去控制字符、规范空白、统一部分中文引号
  -> 结构解析：识别 Markdown 标题、中文编号标题、数字标题、Chapter / Section
  -> 内容哈希：SHA256(content) 做重复文档检测
  -> 大文档预拆分：超过 pre_split_chars 先按字符段切开
  -> 语义分块：SemanticSplitterNodeParser 按语义断点切 chunk
  -> 超长 chunk 保护：递归二分，避免超过 Milvus VARCHAR 限制
  -> 元数据注入：tenant_id / doc_id / content_hash
  -> 元数据瘦身：清除冗余内部字段，避免向量库存储溢出
```

预处理中的几个关键取舍：

1. **先清洗再 hash**：避免同一文档因为多余空白、控制字符导致 hash 不一致。
2. **保留结构信息但不盲目塞进 chunk metadata**：`preprocess_document` 会提取标题和 section，但入 Milvus 前只保留必要治理字段，减少
   metadata 膨胀。
3. **语义分块优先**：相比固定长度切分，SemanticSplitter 更容易保持段落语义完整，减少一个答案所需信息被切散的问题。
4. **大文档双重保护**：先对超大文档预拆分，再对超长 chunk 递归拆分，避免 Milvus 字段长度限制和 LLM 上下文污染。
5. **租户和文档元数据强制注入**：预处理结束后，每个 chunk 都带 `tenant_id`、`doc_id`、`content_hash`，后续检索、删除、去重、审计都依赖这些字段。

面试可强调：

> RAG 效果很大程度取决于数据预处理。我的处理思路是先把不同格式文档统一抽成干净文本，再做结构识别和语义分块，同时保证每个
> chunk 都带治理元数据。这样既提升检索命中率，也保证后续可以做租户隔离、去重和生命周期删除。

### 3.2 多租户隔离不是只在接口层做

项目的多租户隔离贯穿入库、检索、删除和 MCP 调用：

1. 入库时每个 chunk 强制注入 `tenant_id`、`doc_id`、`content_hash`。
2. Milvus 向量检索使用 `MetadataFilters(tenant_id == 当前租户)`，在存储层前置过滤。
3. Elasticsearch 关键词检索使用 `term tenant_id` filter，避免关键词结果串租户。
4. 文档删除和租户删除都会带 tenant 条件清理 Milvus 和 ES。
5. MCP 工具 `search_enterprise_knowledge(query, tenant_id)` 强制要求传入租户 ID，不提供全局搜索入口。

面试可强调：

> 我没有只依赖应用层判断权限，而是把 tenant_id 下推到向量库和关键词索引查询里。这样即使上层逻辑复用、MCP
> 客户端接入或并发请求增加，也不容易出现跨租户数据泄漏。

### 3.3 混合检索解决“语义召回”和“精确匹配”的冲突

系统同时走两条召回链路：

1. 向量检索：适合语义相近、表达不一致的问题。
2. Elasticsearch 关键词检索：适合产品编号、合同条款、专有名词、精确字段。

融合策略支持：

1. `weighted_rrf`：向量权重默认 0.6，关键词权重默认 0.4。
2. LlamaIndex 的 `RELATIVE_SCORE` / `RECIPROCAL_RANK` / `DIST_BASED_SCORE`。

面试可强调：

> 纯向量检索在企业场景里会漏掉编号、术语、版本号这类精确信息；纯关键词又无法处理同义表达。所以我用了向量 + ES 的双路召回，再用加权
> RRF 融合，最后交给 reranker 做 Top-N 精排。

### 3.4 查询改写提高复杂问题召回率

`QueryRewriter` 会用备用模型生成 2-3 个语义等价问题，并保留原始 query。主查询和改写查询分别检索，结果按 node_id 去重后按最高分合并。

适合讲的点：

1. 复杂问题可以被拆成更容易命中的子问题。
2. 同义表达扩展可以缓解用户提问和文档表述不一致的问题。
3. 改写失败时自动回退原 query，不影响主流程可用性。

### 3.5 重排把“召回多”变成“上下文准”

检索阶段召回 Top-K 候选，生成前调用 `BAAI/bge-reranker-v2-m3` 重排，默认保留 Top-5 进入 Prompt。

这解决两个问题：

1. 检索阶段宁可多召回，避免漏召。
2. 生成阶段只放最相关的少量上下文，减少噪声和 token 成本。

### 3.6 生成前上下文压缩降低噪声和成本

结合 `字节rag.pdf` 中“检索结果处理”的实践，检索结果进入生成层前不应该无脑拼接。字节实践里会对检索结果做排序、过滤和摘要，减少输入生成模型的信息量，提高生成效率。

本项目新增了一个轻量版落地：

1. 对每个 rerank 后的文档片段按中英文标点切句。
2. 从 query 中抽取英文/数字关键词和中文 bigram。
3. 对句子按关键词命中数打分。
4. 选择最相关的关键句，并限制每个文档进入 Prompt 的最大字符数。
5. 给上下文加上 `[文档1]`、`[文档2]` 编号，和系统提示词中的“回答末尾引用文档编号”对齐。

优化前：

```text
context = chunk1全文 + chunk2全文 + chunk3全文 ...
```

优化后：

```text
[文档1] source=xxx
与用户问题最相关的关键句...

[文档2] source=yyy
与用户问题最相关的关键句...
```

收益：

1. **降低 token 成本**：长 chunk 只保留和问题相关的关键句。
2. **降低幻觉风险**：减少无关上下文干扰生成模型。
3. **提升引用可解释性**：模型能按 `[文档1]` 这类编号输出来源。
4. **不引入额外模型依赖**：用规则打分实现，延迟很低，失败风险小。

面试可强调：

> 字节 RAG 实践里提到检索结果需要排序、过滤和摘要，不能直接把所有片段塞给生成模型。我在项目里做了一个轻量版关键句抽取：按
> query 命中的关键词选择句子，并给上下文加文档编号。这样既能减少 token，又能让引用格式更稳定。

### 3.7 大模型调用有降级链路

`RAGQueryEngine` 的生成策略：

1. 先调用主模型 `primary_model`。
2. 主模型失败后切换 `fallback_model`。
3. 主备都失败时返回标准化友好降级提示。

同时流式接口也有类似降级逻辑：主模型流失败后尝试备用模型，最终兜底输出降级文本。

面试可强调：

> 线上 RAG 服务最大的风险之一是 LLM API 抖动。如果模型超时、限流或网络异常，系统不能直接 500，所以我把生成层设计成主备模型链路，并把
> fallback 信息写进 pipeline trace，方便后续观测。

这次优化还修正了一个细节：fallback 阶段虽然拿的是备用客户端，但模型名必须显式传 `fallback_model`
，否则实际请求仍可能使用主模型名。这个问题已通过单测保护。

### 3.8 文档生命周期同步减少“幽灵知识”

`SyncManager` 支持：

1. `delete_document(doc_id, tenant_id)`：删除某租户某文档的所有 chunk。
2. `delete_tenant(tenant_id)`：租户注销或 GDPR 场景下清理全部数据。
3. 删除会同步清理 Milvus 和 Elasticsearch。

面试可强调：

> RAG 项目经常只关注入库，不关注删除。但企业知识会过期、合同会失效，如果向量库里残留旧
> chunk，模型可能基于已删除内容回答，形成幽灵幻觉。所以我专门实现了文档生命周期同步。

### 3.9 质量评估形成闭环

`AsyncEvaluator` 并行评估两类指标：

1. 检索质量：precision、recall、MRR、hit rate。
2. 生成质量：faithfulness、relevance。

最终用检索 0.4 + 生成 0.6 计算 overall，低于阈值的坏例写入 PostgreSQL，供后续分析和优化。

面试可强调：

> 我把 RAG 当成一个需要持续运营的系统，而不是一次性工程。每次查询后都会触发异步评估，把低质量样本沉淀成 bad
> cases，这样可以反向优化分块、检索权重、Prompt 和知识库内容。

### 3.10 可观测性覆盖 RAG 关键阶段

Langfuse trace 记录：

1. retrieval：向量召回数、关键词召回数、融合结果、文档预览。
2. rerank：候选数、Top-N、耗时。
3. generation：模型、是否 fallback、耗时、token、回答预览。

HTTP 响应也返回 `pipeline_trace`，前端可以直接展示每一步的耗时和数量。

### 3.11 缓存和限流兼顾性能与稳定性

缓存设计：

1. 本地 `TTLCache`：按 `tenant_id + query` 缓存热点检索结果，短 TTL。
2. Redis ByteCache：按 query embedding hash 缓存向量检索结果，命中后跳过 Milvus。
3. 评估结果缓存：供前端通过 query_id 轮询。

限流设计：

1. Redis Sorted Set 实现滑动窗口。
2. Lua 脚本保证清理、计数、写入原子性。
3. Redis 不可用时 fail-open，优先保证业务可用。

## 4. 项目优秀点

### 4.1 工程闭环完整

项目覆盖了文档上传、解析、分块、向量化、索引、查询、生成、评估、删除、监控、MCP 接入。这比普通 demo 更接近真实企业知识库系统。

### 4.2 安全意识强

多租户隔离不是单点实现，而是在入库 metadata、Milvus filter、ES filter、RBACGuard、MCP 参数、删除同步多处共同保证。

### 4.3 RAG 效果链路比较专业

系统包含查询改写、双路召回、融合排序、rerank、严格上下文 Prompt、评估回流，这些都是提升 RAG 效果的关键工程手段。

### 4.4 可用性设计较完整

包括：

1. 主备模型降级。
2. Redis 限流不可用时 fail-open。
3. ES 检索失败返回空列表，不阻塞向量检索。
4. 查询改写失败回退原始查询。
5. rerank 失败回退原始相似度排序。
6. 评估超时后转后台任务。

### 4.5 可观测与可解释

每次查询不仅返回答案，还返回 pipeline trace，包含每个阶段的 count、latency、model、fallback、token 等信息。面试里可以说这是为了让
RAG 从“黑盒问答”变成“可诊断系统”。

### 4.6 支持 Agent 生态

通过 MCP 将企业知识搜索封装成 tool，外部 Agent 可以直接调用。这个点能体现对 Agentic RAG 趋势的理解。

### 4.7 配置集中化

`settings.py` 管环境变量，`rag_params.py` 管 RAG 调参项。检索 Top-K、融合模式、重排 Top-N、生成温度、评估阈值、缓存 TTL 都能集中调整。

### 4.8 有测试意识

测试覆盖了鉴权、限流、文档 API、入库 metadata、删除同步、检索融合、生成 fallback、评估、tracer、MCP tool
等模块。面试时可以说重点保障的是租户隔离、降级逻辑和核心链路正确性。

### 4.9 动态融合权重：根据 query 类型自适应调整向量/关键词比例

不是固定 0.6/0.4 的融合比例，而是先由 `QueryRewriter.classify()` 做意图分类：

- **semantic 型**（解释类：为什么、如何、原理）→ 向量权重 0.8、关键词 0.2
- **keyword 型**（精确匹配类：编号、金额、日期、条款）→ 向量权重 0.3、关键词 0.7
- **mixed 型** → 默认平衡 0.6/0.4

这样"什么是动态市盈率"会偏向语义检索，而"查询订单编号 ORD-20240001"会偏向关键词精确匹配。

> 🧠 便于记忆：**先问 query 是什么类型，再决定怎么搜。** 解释类多看语义，查编号多看文本。

### 4.10 多粒度索引：文档级粗筛 → 段落级中筛 → 句子级精筛

三级检索漏斗，逐层减少噪声：

1. **L1 文档级**：按 doc_id metadata 快速排除不相关文档
2. **L2 段落级**：现有 Milvus chunk 级语义匹配（标准检索）
3. **L3 句子级**：对 Top-K chunk 拆句，按 query term 命中 + 位置加权精排，只留最相关句子进入生成层

有点像搜索引擎的"全站→页面→段落→句子"这种渐进式缩小范围。

> 🧠 便于记忆：**先粗后细，三级漏斗。** L1 砍文档、L2 找段落、L3 挑句子。进入 LLM 的不是整个文档，而是最相关的几句话。

### 4.11 近重复检测：从 SHA256 完全去重升级到语义去重

原方案只做精确匹配（SHA256 完全相同才算重复），无法发现以下情况：

- 同一篇文档被改了标题和少量措辞（改写版）
- 同一篇文档被翻译后重新上传（翻译版）
- 同一篇文档被摘录/转载到另一份报告中

升级后新增 embedding 余弦相似度检测：先 SHA256 精确命中（快速路径），未命中则计算文档级 embedding 并与已入库文档比对。超过阈值（默认
0.95）即标记为疑似重复，并返回原文档 ID。

> 🧠 便于记忆：**先看 hash 是不是一模一样，再看语义是不是差不多。** 精确匹配是第一道，语义相似是第二道，两道都防。

### 4.12 文档结构解析增强：表格、图片、标题树、引用定位

预处理阶段新增四项结构感知能力：

1. **表格检测**：识别 Markdown pipe table 和 ASCII grid table，记录表头和数据行
2. **图片引用提取**：检测 `![alt](path)`、`<img>`、`{图: xxx}` 等引用，预留 OCR 多模态扩展点
3. **标题层级树**：栈式算法将扁平的标题列表转为层次树，便于 chunk 上下文增强（知道每个 chunk 属于哪个 chapter/section）
4. **引用定位**：提取 `[1]`、`(Author, 2020)`、`第X条`、`Article X` 等引用标记

这些都写入文档 metadata，下游检索和生成时可以按需消费。

> 🧠 便于记忆：**RAG 不只是看文字，还要看懂结构。** 表格是结构化数据，图片是多模态入口，标题树是导航地图，引用是可追溯的锚点。

### 4.13 领域术语归一化：让"口语问法"匹配"书面写法"

企业知识库里常见缩写、产品代号、指标别名，用户口语查询和文档书面表达之间存在 gap。例如用户搜"动态市盈率"，但文档只写"PE-TTM"。

解决方案：双向映射表

- **正向**：别名 → 标准术语（"动态市盈率" → "PE-TTM"），在 query 末尾追加
- **反向**：标准术语 → 别名（"ROE" → "净资产收益率"），在文档入库时扩展

热加载 `data/term_aliases.json`，无需重启服务即可更新术语表。内置金融、技术、通用三类基础术语。

> 🧠 便于记忆：**用户说的和文档写的不一样，中间加个同义词翻译层。** query 侧追加别名，文档侧扩展术语，双向桥接。

### 4.14 缓存细粒度失效：删除文档后不残留缓存

缓存不是设个 TTL 就完事。文档删除/更新后，相关的 query cache 和 ByteCache 必须主动清理：

- **TTLCache**：按 tenant 维度全量清空（简单可靠）
- **ByteCache**：通过 doc_id → cache_key 反向索引，精确清理受影响条目
- **近重复索引**：删除文档时同步清理语义指纹，避免死引用

`SyncManager.delete_document()` 统一负责 Milvus / ES / TTLCache / ByteCache / near_dedup 五路清理。

> 🧠 便于记忆：**缓存和数据库要一起失效，不能删了文档缓存还命中的旧数据。** 精确失效靠反向索引，兜底失效靠 TTL。

### 4.15 人工评估集沉淀：不只靠 LLM judge

LLM judge 适合自动发现问题，但不能完全替代人工判断。项目做了两层补充：

1. **坏例候选池**：低分 case 自动追加到 `data/bad_cases.jsonl`，带 query、answer、context_nodes
2. **金标集管理** (`golden_set.py`)：人工补充 `expected_answer` 和 `label`，转为离线回归评测集

后续可以基于金标集做：

- 检索效果离线评测（precision / recall / MRR / hit rate）
- 生成质量离线评测（faithfulness / relevance）
- 回归测试（改分块策略/融合权重/Prompt 后跑一遍金标集）

> 🧠 便于记忆：**LLM judge 是自动巡逻兵，人工标注是金标裁判。** 两者互补，形成评测闭环。

### 4.16 查询改写增强：HyDE + decomposition + 意图分类三合一

不只是简单的同义词替换，而是三层增强：

1. **HyDE**（假设文档嵌入）：先生成一段假设性答案，以该文本做向量检索，缩小 query 和文档的语义 gap
2. **Query Decomposition**：规则级拆分复合问题（"A 和 B 的区别以及 C 的影响" → "A 是什么"、"B 是什么"、"C 的影响"）
3. **意图分类**：semantic / keyword / mixed 三类，驱动动态融合权重和检索策略

所有改写都保留原始 query，改写失败自动回退，不影响主流程。

> 🧠 便于记忆：**先假设答案长什么样、再把大问题拆小、最后判断怎么搜。** HyDE 是用答案找问题，decomposition 是化整为零，分类是因地制宜。

## 5. 面试讲述版本

### 5.1 1 分钟版本

这个项目是一个企业级 RAG + MCP 系统。我主要解决三个问题：第一是企业知识的安全访问，通过 JWT、RBAC 和 tenant_id 下推到
Milvus / Elasticsearch 实现多租户隔离；第二是 RAG 效果，通过查询改写、向量 + 关键词混合检索、加权 RRF 融合和 reranker
提升召回和精度；第三是生产可用性，通过 Redis 滑动窗口限流、主备模型降级、Langfuse 全链路观测、异步 LLM judge 评估和坏例沉淀，把系统从
demo 做成可运营的服务。除了 HTTP API，我还把查询能力封装成 MCP tool，方便外部 Agent 调用。

### 5.2 3 分钟版本

用户上传文档后，系统先做文件校验、文本抽取、清洗和结构解析，然后计算内容 hash 做重复检测。原始文件存 MinIO，文本通过
SemanticSplitter 做语义分块，每个 chunk 注入 tenant_id、doc_id、content_hash，再用 BAAI/bge-m3 做 embedding，写入
Milvus，同时后台写入 Elasticsearch 做关键词索引。

查询时，中间件先解析 JWT 或请求头得到租户上下文，然后 Redis Lua 滑动窗口限流。进入 RAG 主链路后，系统先用 LLM
做查询改写，多路检索后去重合并。检索阶段同时走 Milvus 向量召回和 ES 关键词召回，两边都带 tenant_id 过滤，之后通过 weighted
RRF 融合。融合后的候选再用 reranker 精排 Top-5，最后进入生成模型。

生成层有主备模型降级：主模型失败切备用模型，备用也失败返回统一降级文案。响应中会返回
pipeline_trace，记录向量检索、关键词检索、融合、重排、生成的耗时和数量。后台还会启动异步评估，裁判模型从
precision、recall、MRR、hit rate、faithfulness、relevance 等维度打分，低质量样本沉淀进 PostgreSQL，后续可以用来优化知识库、分块、融合权重和
Prompt。

## 6. 可能被问到的问题与参考回答

### Q1：为什么不用纯向量检索？

企业知识库里有很多精确匹配场景，比如合同编号、产品型号、政策条款、错误码。纯向量检索对语义相似问题效果好，但对精确 token
可能不稳定。所以项目里同时使用 Milvus 向量检索和 Elasticsearch 关键词检索，再通过加权 RRF 融合，兼顾语义召回和精确匹配。

### Q2：多租户隔离怎么保证？

入库时每个 chunk 都写入 tenant_id。查询时，Milvus 使用 MetadataFilters 做存储层过滤，ES 使用 term filter 做索引层过滤。API
层通过 JWT / Header 注入 tenant_context，文档上传和删除用 RBACGuard 控制角色。MCP tool 也强制 tenant_id 参数，不提供无租户的全局搜索接口。

### Q3：如果 LLM API 超时或挂了怎么办？

生成层有三段降级：先调主模型，失败后调 fallback_model，主备都失败时返回统一友好文案。rerank、query rewrite、ES
检索和评估也都有各自降级，避免一个非核心环节失败导致整个查询不可用。

### Q4：为什么需要 rerank？

检索阶段目标是尽量多召回，召回结果可能有噪声。rerank 的目标是从候选里选出最适合回答当前问题的 Top-N，减少无关上下文进入
Prompt，从而降低幻觉、降低 token 成本，并提升答案相关性。

### Q5：查询改写会不会引入错误？

会有风险，所以我做了三点控制：始终保留原始 query；改写只要求语义等价和拆分，不允许改变原意；改写失败直接回退原 query。多路结果合并时按
node_id 去重，避免重复文档挤占上下文。

### Q6：为什么要做文档删除同步？

企业知识会过期或被删除，如果只新增不删除，向量库里会残留旧 chunk，模型可能基于旧信息回答，造成幽灵知识或合规风险。项目里
delete_document 和 delete_tenant 会同步清理 Milvus 和 Elasticsearch，支持文档更新和租户注销。

### Q7：如何评估 RAG 效果？

系统用异步 LLM judge 做两类评估：检索侧看 precision、recall、MRR、hit rate；生成侧看 faithfulness 和 relevance。overall 低于阈值的
case 会写入 PostgreSQL，作为后续调参和回归测试的数据来源。

### Q8：为什么评估要异步？

裁判模型调用会增加延迟。如果同步等待，会影响用户体验。所以接口先尽量在短超时内拿评估结果，超时就转成 BackgroundTasks，用户可以通过
query_id 后续查询评估状态。

### Q9：限流为什么用 Redis ZSET + Lua？

ZSET 用时间戳作为 score，可以自然实现滑动窗口。Lua 把清理过期请求、统计窗口数量、写入本次请求这三个动作放在 Redis
内原子执行，避免并发场景下计数漂移。相比固定窗口，滑动窗口对突发流量控制更平滑。

### Q10：Redis 不可用时为什么 fail-open？

这个项目是知识查询服务，限流属于保护系统的附加能力。Redis 短暂不可用时，如果 fail-close，会导致正常用户完全不可用；所以这里选择
fail-open 并记录错误日志。生产上可以进一步增加本地内存限流作为二级兜底。

### Q11：如何降低 RAG 延迟？

项目里做了几类优化：热点 query 的 TTLCache、向量检索 ByteCache、流式 SSE 降低首 token 感知延迟、ES
后台索引不阻塞上传主流程、模型和连接延迟初始化、评估后台化。还可以继续做批量 embedding、并行检索、rerank 模型本地化和 Prompt
压缩。

### Q12：为什么要存原始文件到 MinIO？

向量库只保存 chunk 和 embedding，不适合作为原始文档存储。MinIO 保存原文后，后续可以重新分块、换 embedding
模型、做审计回放，也能支撑文档生命周期管理。

### Q13：这个项目怎么支持 Agentic RAG？

除了 HTTP API，项目通过 FastMCP 暴露 `search_enterprise_knowledge` 工具。外部 Agent 可以把企业知识检索作为 tool
调用，而且工具层仍要求 tenant_id，保证 Agent 接入不绕过多租户隔离。

### Q14：如何定位一次回答质量差的原因？

先看 pipeline_trace：向量和关键词各召回多少、融合后多少、rerank Top-N、生成是否 fallback、token 用量和耗时。再看 Langfuse
trace 里的文档预览和 bad_cases 里的评估指标。如果 precision/recall 低，优化分块、查询改写和融合权重；如果 faithfulness 低，优化
Prompt、上下文裁剪和 rerank。

### Q15：项目中已实现哪些 RAG 效果优化？

项目实现了 10 大优化，覆盖检索、生成、治理和评估四个维度：

**检索维度**

1. **动态融合权重**：根据 query 类型（semantic/keyword/mixed）自适应调整向量和关键词比例
2. **多粒度索引**：L1 文档粗筛 → L2 段落中筛 → L3 句子精筛的三级检索漏斗
3. **领域术语归一化**：双向映射表，支持别名→标准术语和标准术语→别名，热加载无需重启

**生成维度**

4. **查询改写增强**：HyDE + query decomposition + 意图分类三合一，保留原始 query，失败自动回退
5. **生成前上下文压缩**：关键句抽取 + [文档N] 引用编号，降低 token 和幻觉

**治理维度**

6. **近重复检测**：SHA256 精确匹配 + embedding 余弦相似度，发现改写版/翻译版/转载版
7. **缓存细粒度失效**：doc_id→cache_key 反向索引，删除文档时精确清理 ByteCache 受影响条目
8. **文档结构解析增强**：表格检测、图片引用、标题层级树、引文定位，预处理阶段统一提取

**评估维度**

9. **人工评估集**：低分样本自动写入候选池，人工标注 expected_answer 后转为金标回归集
10. **评估闭环**：LLM judge（自动） + 金标集（人工）互补，不单方面依赖 LLM judge

> 🧠 便于记忆：这 10 项可以记成一句话——**搜得准**（1/2/3）、**生成好**（4/5）、**治理严**（6/7/8）、**评得稳**（9/10）。

额外可继续优化的方向：

- 多租户权限从角色扩展到文档级 ACL
- 表格 OCR / 多模态图片理解
- 入库任务队列化（Celery / Kafka）支持大规模批处理

### Q16：数据预处理具体做了什么？为什么重要？

数据预处理分为文件解析、文本清洗、结构识别、去重、语义分块和元数据治理几步。上传时先限制文件类型和大小，再用 PyMuPDF /
python-docx 等工具抽取文本；然后去掉控制字符、规范空白、识别标题结构；接着对清洗后的文本做 SHA256 hash，用于重复检测。分块上使用
SemanticSplitter，尽量按语义断点切分，同时对超大文档和超长 chunk 做保护，避免 Milvus 字段溢出。最后每个 chunk 强制写入
tenant_id、doc_id、content_hash，保证后续检索隔离、文档删除和坏例追踪都有依据。

可以补一句：

> 我认为预处理是 RAG 的地基。清洗不好会影响 embedding，切分不好会影响召回和答案完整性，metadata 不规范会影响权限隔离和生命周期管理。

### Q17：结合字节 RAG 实践，你在项目中落地了哪些优化？

我按”能落地、收益明确、复杂度可控”的顺序做了 10 大优化，全面参考字节 RAG 实践中的查询理解、混合检索、检索结果处理、ByteCache
和评估闭环。目前已全部实现：

以下 10 项优化均已完成，模块可直接使用：

| #  | 优化项                         | 核心实现                                                                              | 记忆口诀                  |
|----|-----------------------------|-----------------------------------------------------------------------------------|-----------------------|
| 1  | 动态融合权重                      | `_resolve_fusion_weights()` 根据 classify() 结果 (semantic/keyword/mixed) 自适应调整 vw/kw | 先问类型，再定比例             |
| 2  | 多粒度索引                       | `MultiGranularityRetriever` — L1 doc 粗筛、L2 chunk 中筛、L3 sentence 精筛                | 三级漏斗，越筛越精             |
| 3  | 近重复检测                       | `near_dedup.py` — SHA256 精确 + embedding 余弦相似度，阈值 0.95                             | hash 一模一样 + 语义差不多     |
| 4  | 领域术语归一化                     | `terminology.py` — 双向映射、文件热加载、query/doc 双侧扩展                                      | 用户说口语，系统懂术语           |
| 5  | 缓存失效机制                      | `ByteCache.invalidate_by_doc/tenant` + `sync_manager` 五路清理                        | 删文档 = 清缓存，不留旧数据       |
| 6  | 人工评估集                       | `golden_set.py` — bad case → candidate → 人工标注 → 金标回归集                             | LLM judge 巡逻 + 人工金标裁判 |
| 7  | ByteCache                   | `byte_cache.py` — embedding hash → Redis GET/SET + Sorted Set 热点追踪 + 文档/租户反向索引    | 高频查询缓存，P99 50ms→15ms  |
| 8  | HyDE + decomposition + 意图分类 | `query_rewriter.py` — `_hyde()`, `_rule_decompose()`, `classify()`                | 假设答案、拆小问题、分类型         |
| 9  | 文档结构解析增强                    | `preprocessing.py` — 表格/图片/标题树/引用四项新能力                                            | 不只读文字，还懂结构            |
| 10 | 上下文压缩                       | `_compress_text_for_query()` — 关键句打分 + 位置加权 + `[文档N]` 编号引用                        | 找最相关句子，不喂全文           |

> 🧠 字节实践可记成一句话：**先懂问题（4/8/9）、再多路找（1/2）、找完压缩（10）、热点缓存（5/7）、坏例回流（3/6）。**

## 7. 模型选型说明

### 7.1 模型角色对照表

| 模型角色  | 当前选择                              | 为什么这样选                                  | 记忆点               |
|-------|-----------------------------------|-----------------------------------------|-------------------|
| 主生成模型 | `primary_model`，默认 `gpt-4o`       | 面向最终用户回答，需要强推理、中文表达和长上下文综合能力            | 主模型负责”答得好”        |
| 备用模型  | `fallback_model`，默认 `gpt-4o-mini` | 成本低、延迟低，主模型异常时兜底，也适合查询改写/HyDE           | 备用模型负责”别挂掉”       |
| 嵌入模型  | `BAAI/bge-m3`                     | 多语言能力强，适合中文和中英混合企业文档，1024 维表达更细         | embedding 负责”找得到” |
| 重排模型  | `BAAI/bge-reranker-v2-m3`         | 专门判断 query-document 相关性，把多召回候选精排成 Top-N | reranker 负责”排得准”  |
| 裁判模型  | `judge_model`，默认 `gpt-4o`         | 评估忠实度和相关性需要更强判断能力，且异步执行不影响首响            | 裁判模型负责”判得准”       |

### 7.2 怎么选择各个模型（决策指南）

#### 主生成模型 → 选什么？

```
核心判断：你的用户是谁？他们对答案质量有什么要求？

┌─ 面向 C 端用户，答案需要强推理和中文表达
│  → gpt-4o / claude-sonnet-4-6
│    - 推理强、中文流畅、指令跟随好
│    - 成本较高，但用户体验优先
│
├─ 面向内部员工，查询量大、成本敏感
│  → gpt-4o-mini / deepseek-v3 / qwen-plus
│    - 性价比高，多数 RAG 问题不需要最强模型
│    - 复杂推理场景偶尔翻车，但 fallback 模型会兜底
│
├─ 特殊合规要求（数据不出境/私有化部署）
│  → deepseek-r1 / qwen2.5-72b / 私有化模型
│    - 走本地 GPU 或国内 API
│    - 效果略逊但合规优先
│
└─ 需要 function calling / structured output
   → gpt-4o / claude-sonnet-4-6（原生支持最好）
```

> 🧠 记忆：**C 端用户用最强模型保体验，内部用轻量模型控成本，合规场景用本地模型。**

#### 备用模型 → 选什么？

```
核心判断：备用模型是”救火队员”，应该便宜、稳定、够用就行

┌─ 主模型是 gpt-4o
│  → gpt-4o-mini（同一生态，兼容性好）
│
├─ 主模型是 deepseek
│  → qwen-plus / gpt-4o-mini（异构降级，避免同厂商挂掉）
│
└─ 降级成本敏感的
   → gpt-4o-mini / deepseek-chat（成本极低，够用）
```

> 🧠 记忆：**备用 = 便宜 + 稳定 + 和主模型异构，避免同一厂商挂。主备不要都是同一个供应商。**

#### 嵌入模型 → 选什么？

```
核心判断：你的文档是什么语言？多长？要不要私有化？

┌─ 中英混合企业文档（最通用）
│  → BAAI/bge-m3（1024维，多语言，8192 token 上下文）
│    备选：text-embedding-3-large（OpenAI，维度可调）
│
├─ 纯英文文档
│  → text-embedding-3-large / bge-large-en-v1.5
│
├─ 需要本地/私有化部署
│  → bge-m3 本地模型 / m3e-large（中文优先）
│
├─ 文档很长，需要长上下文 embedding
│  → bge-m3 (8192 token) / jina-embeddings-v3 (8192 token)
│
└─ 做 code search
   → voyage-code-2 / code-bert
```

> 🧠 记忆：**中英文混 → bge-m3，英文为主 → OpenAI，私有化 → 本地 bge/m3e。embedding 是 RAG 的基础，选错了后面全白做。**

#### 重排模型 → 选什么？

```
核心判断：embedding 召回的多，reranker 要从中挑对的

┌─ 通用场景（中英文）
│  → BAAI/bge-reranker-v2-m3
│    备选：Cohere Rerank v3（SaaS，效果更好但贵）
│
├─ 超长文档（> 4096 token 候选）
│  → bge-reranker-v2-m3（8K 上下文）
│  → 也可用 LLM 做 pointwise rerank（更准但更慢）
│
├─ 高精度要求（金融/法律）
│  → Cohere Rerank v3（效果好，按 token 计费）
│  → 或用裁判模型 gpt-4o 做 pairwise 比较（最准但慢）
│
└─ 免费/本地需求
   → bge-reranker-v2-m3 本地部署（HuggingFace 开源）
```

> 🧠 记忆：**reranker 是”召回多 + 选出准”的关键节点。默认 bge-reranker-v2-m3 够用，高要求上 Cohere，极致要求用 LLM 做
pairwise。**

#### 裁判模型 → 选什么？

```
核心判断：异步执行，不影响用户延迟，但判断质量要好

┌─ 默认推荐
│  → gpt-4o（和主模型同款，判断标准一致）
│
├─ 成本敏感
│  → gpt-4o-mini（大部分评估够用，faithfulness 判断偶尔略弱）
│
└─ 特定领域需要强判断（法律/医疗/金融合规）
   → gpt-4o / claude-sonnet-4-6
```

> 🧠 记忆：**裁判比选手贵是正常的。评估是异步的，不影响用户延迟，所以可以上最强模型做判断。**

### 7.3 面试可以这样说

> 我没有用一个模型解决所有问题，而是按职责拆分：生成模型负责最终回答，embedding 负责召回，reranker
> 负责精排，轻量模型负责改写和兜底，裁判模型负责质量判断。这样效果、成本和稳定性更平衡。
>
> 选型上我的原则是：**C 端用户用最强模型保体验，内部用轻量模型控成本；embedding 和 reranker 优先开源模型（bge
系列），数据不出境；主备模型尽量异构，避免同一厂商同时挂掉。**

## 8. STAR 项目表达

### Situation

企业内部知识分散在 PDF、Word、Markdown 等文档中，普通关键词搜索无法理解语义，直接接入大模型又存在幻觉、权限泄露、知识过期、调用不稳定等问题。

### Task

设计一个企业级 RAG 系统，使不同租户能安全查询自己的知识库，并且系统具备可观测、可评估、可降级和可扩展能力。

### Action

我实现了文档入库、语义分块、MinIO 原文存储、Milvus 向量检索、Elasticsearch 关键词检索、加权 RRF 融合、reranker
精排、生成前关键句压缩、主备模型降级、Redis 限流、Langfuse trace、异步 LLM judge 评估和 MCP tool 暴露。安全上把 tenant_id 从入库
metadata 一直下推到检索过滤和删除同步。

### Result

系统形成了完整闭环：文档能安全入库和删除，查询能通过混合检索提升召回，回答能通过重排和上下文约束降低幻觉，质量能通过评估和坏例沉淀持续优化，也能被外部
Agent 通过 MCP 调用。

## 9. 面试官深挖时的技术关键词

可以主动提这些关键词，展示深度：

1. 多租户隔离：RBAC、tenant_context、metadata filter、term filter。
2. 检索优化：query rewriting、hybrid retrieval、weighted RRF、rerank、context compression。
3. 生产可用性：fallback model、fail-open、BackgroundTasks、SSE。
4. 数据治理：content_hash 去重、MinIO 原文、delete_document、delete_tenant。
5. LLMOps：Langfuse trace、token、latency、bad cases、faithfulness。
6. 性能：TTLCache、ByteCache、Redis ZSET、Lua atomicity。
7. Agent：MCP tool、tenant_id 必填、安全边界。

## 10. 建议演示顺序

1. 先登录拿 JWT，说明 token 内含 tenant_id 和 role。
2. 上传一份文档，展示 MinIO、分块、embedding、Milvus、ES 的入库步骤。
3. 发起 `/v1/query`，展示 answer 和 pipeline_trace。
4. 发起 `/v1/query/stream`，展示 SSE 流式输出。
5. 查询 `/v1/evaluation/{query_id}` 或 `/v1/evaluation/stats`，展示评估闭环。
6. 删除文档，说明 Milvus 和 ES 同步清理，避免幽灵知识。
7. 最后展示 MCP tool，说明可被 Agent 调用。

## 11. 需要诚实说明的边界

面试里如果被问到生产级差距，可以坦诚说：

1. 当前项目偏原型到准生产之间，真实上线还需要更完善的密钥管理（Vault/KMS）、监控告警（Prometheus/Grafana）和部署策略（K8s/灰度）。
2. 文档级 ACL 尚未完全展开，目前主要是租户级隔离 + 角色级 API 权限。
3. 大规模场景下需要把入库任务队列化，例如 Celery / Kafka，并增加批处理和重试补偿。
4. 流式评估和批量重建索引等需要额外基础设施支持。

不过以下原本是边界的项已经实装：

- ✅ 缓存细粒度失效（doc_id → cache_key 反向索引）
- ✅ 人工评估集（bad case → candidate → golden set）
- ✅ 近重复检测（SHA256 + embedding 语义去重）
- ✅ 查询改写（HyDE + decomposition + 意图分类）
- ✅ 文档结构解析（表格/图片/标题树/引用）

---

## 12. 补充问答与实例（带具体例子便于理解）

### Q18：什么是混合检索？为什么要同时用向量和关键词？

**回答：**
混合检索是同时使用向量检索和关键词检索，然后融合结果的一种策略。

**向量检索**适合语义理解场景：
- 用户问"公司请假政策是什么" → 向量检索能找到"员工请假管理制度"
- 用户问"如何报销差旅费" → 向量检索能找到"差旅费用报销流程"

**关键词检索**适合精确匹配场景：
- 用户问"合同编号 CON-2024-0056" → 关键词检索能精确匹配
- 用户问"第十二条条款内容" → 关键词检索能匹配"第十二条"开头的内容
- 用户问"产品型号 PRO-X500" → 关键词检索能匹配型号

**为什么混合？**
纯向量检索对编号、代码、条款这些精确信息可能不稳定；纯关键词检索对同义表达（如"请假"vs"休假"）不友好。

**项目实现：**
```
用户问题 → QueryRewriter分类意图
  ├─ semantic型 → 向量权重0.8 + 关键词权重0.2
  ├─ keyword型  → 向量权重0.3 + 关键词权重0.7
  └─ mixed型     → 向量权重0.6 + 关键词权重0.4
```

### Q19：什么是加权RRF融合？怎么计算的？

**回答：**
RRF（Reciprocal Rank Fusion）是一种融合多个检索结果的方法。加权RRF在此基础上为不同召回源设置不同权重。

**公式：**
```
score(d) = vw / (k + rank_vector(d)) + kw / (k + rank_keyword(d))
```

**具体例子：**
假设查询"动态市盈率计算"：
- 向量检索结果：文档A(排名1)、文档B(排名3)、文档C(排名5)
- 关键词检索结果：文档B(排名1)、文档D(排名2)、文档A(排名4)
- 设定 vw=0.6（向量权重）、kw=0.4（关键词权重）、k=60

计算各文档得分：
```
文档A: 0.6/(60+1) + 0.4/(60+4) = 0.00984 + 0.00625 = 0.01609
文档B: 0.6/(60+3) + 0.4/(60+1) = 0.00952 + 0.00656 = 0.01608
文档C: 0.6/(60+5) + 0.4/(60+∞) = 0.00909 + 0 = 0.00909
文档D: 0.6/(60+∞) + 0.4/(60+2) = 0 + 0.00667 = 0.00667
```

最终排序：A > B > C > D

**权重动态调整：**
```python
# src/engine/retrievers.py
def _resolve_fusion_weights(query: str) -> tuple[float, float]:
    qtype = shared_rewriter.classify(query)
    if qtype == "semantic":      # "为什么PE-TTM高" → 向量为主
        return (0.8, 0.2)
    if qtype == "keyword":       # "查询订单ORD-123" → 关键词为主
        return (0.3, 0.7)
    return (0.6, 0.4)             # 默认平衡
```

### Q20：什么是HyDE？怎么工作的？

**回答：**
HyDE（Hypothetical Document Embeddings）是一种查询增强技术，先让LLM生成假设性答案，再用该答案的向量去检索。

**传统方式 vs HyDE：**
```
传统方式：
  Query: "什么是动态市盈率？"
  → 直接用 Query 的 embedding 去检索
  → 可能检索到不相关片段

HyDE方式：
  Query: "什么是动态市盈率？"
  → LLM生成假设答案："动态市盈率（PE-TTM）是根据最近12个月的净利润计算的..."
  → 用假设答案的 embedding 去检索
  → 语义对齐度更高，召回更准
```

**项目实现：**
```python
# src/engine/query_rewriter.py
async def _hyde(self, query: str) -> str:
    prompt = (
        "请基于问题写一段可能出现在企业知识库中的假设性答案，"
        "只写事实风格段落，不要编造具体数字；用于向量检索，不是最终回答。\n\n"
        f"问题：{query}\n\n假设性文档："
    )
    response = await client.chat.completions.create(
        model=settings.fallback_model,
        messages=[{"role": "user", "content": prompt}],
        temperature=0.0,  # 低温度保证输出稳定
    )
    return response.choices[0].message.content.strip()
```

**适用场景：**
- 用户的query和文档表述方式不一致
- query较短，信息不足
- 需要更高的语义召回

### Q21：什么是查询改写？项目中做了哪些改写？

**回答：**
查询改写是对用户的原始问题进行变换，生成多个等价或分解的问题，分别检索后合并结果，提高召回率。

**项目中的三种改写方式：**

1. **术语归一化**（术语表）
```
输入: "什么是动态市盈率"
输出: "什么是动态市盈率 PE-TTM"

输入: "ROE高的公司有哪些"
输出: "ROE高的公司有哪些 净资产收益率"
```

2. **HyDE假设文档**
```
输入: "公司的报销流程是什么"
生成假设答案: "员工报销流程：提交申请→部门审批→财务审核→打款到员工银行卡..."
然后用这个假设答案做向量检索
```

3. **查询分解**（规则级）
```
输入: "差旅费和招待费的区别以及报销流程"
分解为:
  - "差旅费和招待费的区别"
  - "差旅费报销流程"
  - "招待费报销流程"
```

**代码流程：**
```python
async def rewrite(self, query: str) -> list[str]:
    queries = [query]  # 始终保留原始查询
    
    # 1. 术语归一化
    normalized = normalize_query_terms(query)
    if normalized != query:
        queries.append(normalized)
    
    # 2. LLM扩展（语义等价变体）
    expanded = await self._expand(query)
    queries.extend(expanded)
    
    # 3. 规则级分解
    if rag_params.query_decomposition_enabled:
        queries.extend(self._rule_decompose(query))
    
    # 4. HyDE
    if rag_params.hyde_enabled:
        hyde = await self._hyde(query)
        if hyde:
            queries.append(hyde)
    
    # 去重后返回
    return unique[:rag_params.query_rewrite_max_variants]
```

### Q22：什么是多租户隔离？项目中如何实现的？

**回答：**
多租户隔离是指不同租户（客户/部门）的数据完全隔离，一个租户只能访问自己的知识库，不能看到其他租户的数据。

**项目中的五层隔离：**

1. **认证层**（JWT解析）
```python
# src/middleware/auth.py
async def extract_tenant_context(request: Request) -> dict:
    token = auth_header.removeprefix("Bearer ")
    payload = jwt.decode(token, ...)
    return {
        "tenant_id": payload.get("tenant_id", "default"),
        "role": payload.get("role", "viewer"),
        "user_id": payload.get("sub", "anonymous"),
    }
```

2. **入库层**（元数据注入）
```python
# 每个chunk都带tenant_id
node.metadata = {
    "tenant_id": tenant_id,
    "doc_id": doc_id,
    "content_hash": content_hash,
}
```

3. **检索层**（Milvus过滤）
```python
# src/engine/retrievers.py
def _build_tenant_filter(tenant_id: str) -> MetadataFilters:
    return MetadataFilters(
        filters=[MetadataFilter(key="tenant_id", value=tenant_id, operator="==")]
    )

# 查询时强制过滤
result = await vector_store.aquery(
    VectorStoreQuery(
        query_str=query,
        similarity_top_k=top_k,
        filters=tenant_filter,  # 强制注入
    )
)
```

4. **关键词检索层**（ES过滤）
```python
# Elasticsearch查询带tenant过滤
{
    "query": {
        "bool": {
            "must": [{"match": {"text": query}}],
            "filter": [{"term": {"tenant_id": tenant_id}}]  # 强制过滤
        }
    }
}
```

5. **删除层**（同步清理）
```python
# src/pipeline/sync_manager.py
def delete_document(self, doc_id: str, tenant_id: str) -> int:
    expr = f'doc_id == "{doc_id}" && tenant_id == "{tenant_id}"'
    # 同时清理Milvus和ES
```

**安全边界：**
```
用户A(租户tenant_abc)  ─┐
                       ├─→ 只能检索/删除 tenant_abc 的数据
用户B(租户tenant_xyz)  ─┘
```

### Q23：什么是重排序（Rerank）？为什么需要它？

**回答：**
重排序是对检索结果进行二次打分和排序，选出最相关的Top-N进入生成阶段。

**为什么需要重排序？**

1. **检索阶段目标：尽量多召回，避免漏召**
   - 检索时设置较大的K（如20）
   - 宁可召回一些不相关的，也不要漏掉相关的

2. **生成阶段目标：只给最相关的上下文**
   - 上下文太多会：增加token成本、引入噪声、增加幻觉风险
   - 只需要最相关的5-10个chunk

**对比例子：**
```
Query: "公司股权结构"
检索结果（按向量相似度）：
  1. 公司简介（相似度0.85）→ 不太相关
  2. 股权结构说明（相似度0.82）→ 相关
  3. 组织架构图（相似度0.81）→ 不太相关
  4. 股东持股情况（相似度0.79）→ 相关
  ...

Rerank后（按query-document相关性）：
  1. 股权结构说明（rerank score 0.95）
  2. 股东持股情况（rerank score 0.92）
  3. 公司简介（rerank score 0.70）
  ...
```

**项目实现：**
```python
async def rerank(self, query: str, nodes: list[NodeWithScore], top_n: int = 5):
    # 1. 准备候选文档
    documents = [n.node.get_content()[:8000] for n in nodes]
    
    # 2. 调用reranker API
    resp = await self._call_rerank_api(query, documents, top_n)
    
    # 3. 更新分数并返回Top-N
    for r in resp.get("results", []):
        idx = r["index"]
        nodes[idx].score = r["relevance_score"]
    
    return sorted(nodes, key=lambda n: n.score, reverse=True)[:top_n]
```

### Q24：什么是上下文压缩？项目中如何实现？

**回答：**
上下文压缩是在将检索结果送入生成模型前，只保留与问题最相关的关键句子，减少token消耗和噪声。

**传统方式 vs 压缩方式：**
```
传统方式（可能5000 tokens）：
  [文档1] 公司股权结构概述：公司成立于2020年，注册资本5000万元，总部位于北京...
  [文档2] 人力资源管理制度第一章：员工入职流程，员工入职需提供身份证、学历证明...
  
压缩方式（可能800 tokens）：
  [文档1] source=股权结构说明书
  公司股权结构：创始人持股30%，天使投资人持股20%，A轮投资人持股25%。
  [文档2] source=员工持股方案
  员工通过ESOP可持股15%，分4年归属。
```

**项目实现：**
```python
def _compress_text_for_query(self, text: str, query: str) -> str:
    # 1. 从query提取关键词
    terms = self._extract_query_terms(query)
    
    # 2. 按标点切句
    sentences = self._split_sentences(text)
    
    # 3. 计算每个句子的关键词命中分
    scored = []
    for idx, sentence in enumerate(sentences):
        score = sum(sentence.lower().count(term) for term in terms)
        # 位置加分：靠前的句子优先
        score += 1 / (idx + 10)
        if score > 0:
            scored.append((score, idx, sentence))
    
    # 4. 选取得分最高的句子
    selected = []
    total = 0
    for score, idx, sentence in sorted(scored, reverse=True):
        if total + len(sentence) > max_chars:
            break
        selected.append(sentence)
        total += len(sentence)
    
    return "\n".join(selected)
```

**收益：**
- Token成本降低：5000 → 800 tokens
- 幻觉风险降低：无关上下文被过滤
- 答案相关性提升：只给最相关的内容

### Q25：什么是主备模型降级？项目中如何实现？

**回答：**
主备模型降级是指当主模型调用失败时，自动切换到备用模型，确保服务可用性。

**失败场景：**
- API超时
- 429限流
- 401凭证失效
- 网络异常
- 模型服务不可用

**项目中的三段降级：**
```python
async def query_with_trace(self, nodes, query) -> tuple[str, GenerationTrace]:
    trace = GenerationTrace()
    context = self._build_context(nodes, query)
    
    # 第1段：尝试主模型
    try:
        answer, usage = await self._generate(
            self._get_primary_llm(), context, query, settings.primary_model
        )
        trace.model_used = settings.primary_model
        return answer, trace
    except Exception as e:
        logger.warning(f"主模型失败: {e}，切换备用模型...")
    
    # 第2段：降级至备用模型
    try:
        answer, usage = await self._generate(
            self._get_fallback_llm(), context, query, settings.fallback_model
        )
        trace.model_used = settings.fallback_model
        trace.fallback_used = True
        return answer, trace
    except Exception as e:
        logger.error(f"备用模型也失败: {e}，返回降级提示")
    
    # 第3段：返回标准化降级提示
    trace.model_used = "fallback_text"
    trace.fallback_used = True
    return FALLBACK_RESPONSE, trace
```

**降级提示：**
```python
FALLBACK_RESPONSE = (
    "很抱歉，知识服务暂时不可用，请稍后重试或联系系统管理员。"
    "I'm sorry, but the knowledge service is temporarily unavailable. "
    "Please try again in a moment or contact your administrator."
)
```

**配置示例：**
```python
primary_model = "gpt-4o"          # 主模型，效果最好
fallback_model = "gpt-4o-mini"   # 备用模型，便宜稳定
```

### Q26：什么是异步评估？为什么需要异步？

**回答：**
异步评估是在用户得到回答后，后台启动LLM裁判对回答质量进行评估，不影响用户响应时间。

**为什么异步？**
- 评估需要调用裁判模型，增加2-5秒延迟
- 评估不是回答的必要条件，用户可以立即得到答案
- 用户可以通过query_id轮询评估结果

**评估指标：**
```python
{
    "precision": 0.9,      # 检索到的文档中相关的比例
    "recall": 0.7,         # 相关文档中被检索到的比例
    "mrr": 0.85,           # 第一个相关文档的倒数排名
    "hit_rate": 1.0,       # Top-K中是否有相关文档
    "faithfulness": 0.95,  # 回答是否基于上下文
    "relevance": 0.9,      # 回答与问题的相关性
    "overall": 0.88,       # 综合得分
    "passing": True        # 是否通过质量阈值
}
```

**项目实现：**
```python
@app.post("/v1/query")
async def query_endpoint(...):
    # 1. 正常流程：检索→重排→生成
    answer, gen_trace = await query_engine.query_with_trace(reranked, body.query)
    
    # 2. 短超时内尝试获取评估结果
    try:
        await asyncio.wait_for(
            _evaluate_and_cache(query_id, query, context, answer, tenant_id),
            timeout=rag_params.eval_timeout_seconds  # 15秒
        )
        if query_id in eval_cache:
            eval_result = eval_cache.get(query_id)
            # 返回评估指标
            return QueryResponse(answer=answer, faithfulness=eval_result.get("faithfulness"), ...)
    except asyncio.TimeoutError:
        # 超时则转为后台任务
        background_tasks.add_task(_evaluate_and_cache, ...)
    
    # 即使评估未完成，也先返回答案
    return QueryResponse(answer=answer, faithfulness=None, ...)
```

### Q27：Redis滑动窗口限流是如何工作的？

**回答：**
Redis滑动窗口限流使用Sorted Set（ZSET）记录每次请求的时间戳，通过Lua脚本保证原子性。

**工作原理：**
```
ZSET key: rate_limit:tenant_abc
member:  timestamp:uuid
score:    timestamp_ms (毫秒时间戳)

操作流程：
1. ZREMRANGEBYSCORE 删除窗口外的过期请求
2. ZCARD 统计窗口内的请求数
3. 若未超限，ZADD 记录本次请求
4. EXPIRE 设置TTL
```

**Lua脚本（原子操作）：**
```lua
local key       = KEYS[1]
local now_ms    = tonumber(ARGV[1])
local window_ms = tonumber(ARGV[2])
local max_req   = tonumber(ARGV[3])

-- 1. 清理窗口外的过期请求
redis.call('ZREMRANGEBYSCORE', key, 0, now_ms - window_ms)

-- 2. 统计窗口内的请求数
local count = redis.call('ZCARD', key)

-- 3. 是否超限
if count >= max_req then
    return {0, count}  -- 拒绝
end

-- 4. 记录本次请求
redis.call('ZADD', key, now_ms, ARGV[4])
redis.call('EXPIRE', key, math.ceil(window_ms / 1000) * 2)

return {1, count + 1}  -- 放行
```

**举例：**
```
假设：60秒窗口，最大60次请求

时间轴：
0s   → 请求1，ZADD score=0
10s  → 请求2，ZADD score=10000
...
50s  → 请求6，ZADD score=50000
60s  → 查询，先删除 score < 0 的记录，统计5个，放行
61s  → 查询，先删除 score < 1000 的记录（删除请求1），统计5个，放行
```

**相比固定窗口的优势：**
```
固定窗口（每分钟60次）：
  00:59 → 60次请求（突发流量）
  01:00 → 又可以60次请求（重置计数器）
  → 实际2分钟内可接受120次请求，可能压垮系统

滑动窗口：
  00:59 → 60次请求
  01:00 → 检查过去60秒（00:01-01:00），已有60次请求，拒绝
  → 平滑控制，防止突发流量
```

### Q28：什么是ByteCache？如何实现热点缓存？

**回答：**
ByteCache是基于Redis的向量检索热点缓存，命中时直接返回缓存结果，跳过Milvus，降低延迟。

**缓存键设计：**
```python
key = f"bytecache:v:{tenant_id}:{top_k}:{embedding_hash[:20]}"
```

**工作流程：**
```
Query → embedding
  → 计算hash
  → Redis GET key
  → 命中？直接返回（~15ms）
    ↓ 未命中
  → Milvus检索（~50ms）
  → Redis SET key value（TTL 7天）
  → 返回结果
```

**热点追踪：**
```python
# 使用另一个ZSET记录每个key的访问次数
hot_key = f"bytecache:hot:{date}"
self.client.zincrby(hot_key, 1, key)  # 每次访问+1
self.client.zrevrange(hot_key, 0, 19)  # 获取Top-20热点key
```

**细粒度失效：**
```python
# 删除文档时，清理相关缓存
def invalidate_by_doc(self, tenant_id: str, doc_id: str):
    # 反向索引：doc_id → cache_keys
    idx_key = f"bytecache:doc:{tenant_id}:{doc_id}"
    cache_keys = self.client.smembers(idx_key)
    
    # 批量删除受影响的缓存条目
    if cache_keys:
        self.client.delete(*cache_keys)
        self.client.delete(idx_key)
```

**性能对比：**
| 场景         | 不使用ByteCache | 使用ByteCache |
|-------------|---------------|-------------|
| 冷查询        | ~50ms         | ~50ms       |
| 热点查询（命中） | ~50ms         | ~15ms       |
| P99延迟      | ~50ms         | ~30ms       |

### Q29：什么是近重复检测？项目中如何实现？

**回答：**
近重复检测是发现内容相似但不完全相同的文档，避免重复入库。

**传统精确匹配 vs 近重复检测：**
```
精确匹配（SHA256）：
  文档A: "公司股权结构概述：创始人持股30%..."
  文档B: "公司股权结构概述：创始人持股30%..."（完全相同）
  → 检测为重复

近重复检测（embedding相似度）：
  文档A: "公司股权结构概述：创始人持股30%，天使投资人持股20%..."
  文档B: "公司股权说明：创始人持有30%股权，天使投资人占20%..."（措辞略有不同）
  → 相似度0.98，检测为近重复
```

**项目实现：**
```python
def detect_near_duplicate(tenant_id: str, text: str, embed_model) -> dict | None:
    threshold = 0.95  # 相似度阈值
    
    # 1. SHA256精确匹配（快速路径）
    content_hash = hashlib.sha256(text.encode()).hexdigest()
    if content_hash in index:
        return {"existing_doc_id": index[content_hash]["doc_id"], "similarity": 1.0}
    
    # 2. 计算文档embedding
    query_emb = embed_model._get_text_embedding(text[:8000])
    
    # 3. 与已入库文档计算余弦相似度
    best_match = None
    best_similarity = 0.0
    for content_hash, entry in index.items():
        if entry["tenant_id"] != tenant_id:
            continue
        sim = cosine_similarity(query_emb, entry["embedding"])
        if sim > best_similarity:
            best_similarity = sim
            best_match = entry
    
    # 4. 阈值判断
    if best_match and best_similarity >= threshold:
        return {
            "existing_doc_id": best_match["doc_id"],
            "similarity": best_similarity,
            "match_type": "near"
        }
    
    # 未检测到重复，注册指纹
    register_doc_embedding(tenant_id, "pending", content_hash, query_emb)
    return None
```

**适用场景：**
- 同一文档被改了标题和少量措辞
- 同一文档被翻译后重新上传
- 同一文档被摘录/转载到另一份报告

### Q30：什么是MCP？项目中如何集成？

**回答：**
MCP（Model Context Protocol）是Anthropic提出的协议，允许大模型客户端（如Claude Desktop）调用外部工具。

**项目中MCP集成：**
```python
# src/mcp_server/tools.py
from fastmcp import FastMCP

mcp = FastMCP("IntelliLens-MCP")

@mcp.tool()
async def search_enterprise_knowledge(query: str, tenant_id: str) -> str:
    """搜索企业知识库。
    
    Args:
        query: 搜索问题
        tenant_id: 租户ID（必须提供，保证数据隔离）
    
    Returns:
        搜索结果摘要
    """
    nodes = await shared_retriever.retrieve(query, tenant_id)
    answer, _ = await shared_query_engine.query(nodes, query)
    return answer

# 挂载到FastAPI
app.mount("/mcp", mcp.streamable_http_app())
```

**MCP客户端连接：**
```
Claude Desktop 配置:
{
  "mcpServers": {
    "intellilens": {
      "command": "python",
      "args": ["-m", "src.mcp_server.server"],
      "env": {
        "OPENAI_API_KEY": "xxx",
        "TENANT_ID": "tenant_abc"
      }
    }
  }
}
```

**使用场景：**
```
用户（在Claude Desktop）:
  "帮我在公司知识库里搜索一下股权结构相关的信息"

Claude:
  1. 调用 search_enterprise_knowledge("股权结构", "tenant_abc")
  2. 收到MCP返回的搜索结果
  3. 基于结果生成回答
```

**安全边界：**
- tenant_id是必填参数，不提供无租户的全局搜索
- 仍通过tenant_context_middleware进行认证
- 每次MCP调用都带tenant_id，确保租户隔离

### Q31：如何解决"幻觉"问题？

**回答：**
幻觉是指模型生成了不在上下文中的信息。项目中通过以下方式减少幻觉：

**1. 系统提示词约束**
```python
system_prompt = """
你是企业智能知识助手，擅长基于上下文回答问题。
要求：
1. 严格基于下方给出的【参考资料】生成答案，不得臆造信息
2. 如果参考资料不足以回答问题，请如实说明
3. 回答末尾标注引用的文档编号（如 [文档1]、[文档3]）
"""
```

**2. 上下文压缩**
只给模型最相关的关键句，减少无关信息干扰。

**3. 忠实度评估**
```python
async def _evaluate_generation(query, context, answer):
    prompt = f"""
评估以下 RAG 回答的质量：

## 上下文
{context}

## 问题
{query}

## 回答
{answer}

## 评估
faithfulness（忠实度）：回答是否严格基于上下文？无编造=1.0，完全编造=0.0

严格输出 JSON：
{{"faithfulness": 0.0, "faithfulness_reason": "理由"}}
"""
    result = await self._judge(prompt)
    if result["faithfulness"] < 0.8:
        # 标记为坏例
        insert_bad_case(...)
```

**4. Rerank**
确保给模型的上下文是相关的。

### Q32：如何优化检索召回率？

**回答：**
召回率（Recall）是指所有相关文档中被检索到的比例。项目中通过以下方式优化召回：

**1. 查询改写**
```python
输入: "什么是动态市盈率"
改写为:
  - "什么是动态市盈率"
  - "PE-TTM是什么"
  - "如何计算市盈率TTM"
```

**2. 混合检索**
向量 + 关键词双路召回，兼顾语义和精确匹配。

**3. 动态融合权重**
根据查询类型自适应调整向量/关键词比例。

**4. 查询分解**
复杂问题拆分成多个子问题，分别检索后合并。

**5. 术语归一化**
将用户口语转换为文档中的标准术语。

**6. 多粒度检索**
L1文档级粗筛、L2段落级中筛、L3句子级精筛。

**7. 近重复检测**
发现改写版、翻译版文档，避免漏掉相关内容。

### Q33：如何保证数据一致性？

**回答：**
数据一致性是指在文档删除/更新后，向量库、索引、缓存等各存储系统保持同步。

**项目中的五路清理机制：**
```python
def delete_document(self, doc_id: str, tenant_id: str) -> int:
    # 1. 清理Milvus向量库
    milvus_deleted = delete_from_milvus(doc_id, tenant_id)
    
    # 2. 清理Elasticsearch索引
    es_deleted = delete_from_es(doc_id, tenant_id)
    
    # 3. 清理TTLCache（全量清理）
    retrieval_cache.clear()
    
    # 4. 清理ByteCache（精确清理）
    byte_cache.invalidate_by_doc(tenant_id, doc_id)
    
    # 5. 清理近重复索引
    remove_doc_from_index(content_hash)
    
    return max(milvus_deleted, es_deleted)
```

**ByteCache精确失效：**
```python
# 入库时建立反向索引
def set_with_tracking(self, tenant_id, embedding, top_k, results, doc_ids):
    key = self.build_key(tenant_id, embedding, top_k)
    self.client.setex(key, ttl, json.dumps(results))
    
    # 记录 doc_id → cache_key 映射
    for doc_id in doc_ids:
        idx_key = f"bytecache:doc:{tenant_id}:{doc_id}"
        self.client.sadd(idx_key, key)

# 删除时反向查找并清理
def invalidate_by_doc(self, tenant_id, doc_id):
    idx_key = f"bytecache:doc:{tenant_id}:{doc_id}"
    cache_keys = self.client.smembers(idx_key)
    if cache_keys:
        self.client.delete(*cache_keys)
        self.client.delete(idx_key)
```

### Q34：如何支持文档更新？

**回答：**
文档更新采用"先删后写"策略。

**流程：**
```python
@router.put("/v1/documents/{doc_id}")
async def update_document(doc_id: str, file: UploadFile):
    tenant_ctx = request.state.tenant_context
    tenant_id = tenant_ctx["tenant_id"]
    
    # 1. 读取新内容
    content = await file.read()
    text = _extract_text(file.filename, content)
    
    # 2. 先删除旧数据
    deleted = sync_manager.delete_document(doc_id, tenant_id)
    logger.info(f"删除旧数据: {deleted} chunks")
    
    # 3. 计算新hash
    content_hash = _compute_content_hash(text)
    
    # 4. 重新入库
    nodes = await ingestion_pipeline.ingest_text(text, tenant_id, doc_id)
    
    return UploadResponse(doc_id=doc_id, chunk_count=len(nodes))
```

**更新问题处理：**
- 新内容hash与旧内容相同：无需处理
- 新内容hash不同：先删后写

### Q35：如何监控和诊断RAG系统？

**回答：**
项目通过Langfuse和API响应提供全链路监控。

**Langfuse追踪：**
```python
# 创建trace
langfuse_trace = lf.trace(
    name="RAG Query",
    input={"query": query, "tenant_id": tenant_id}
)

# 记录retrieval span
with trace_span(langfuse_trace, "retrieval",
                input={"query": query},
                output={"vector_count": 10, "keyword_count": 8}):
    pass

# 记录generation span
with trace_span(langfuse_trace, "generation",
                input={"context_chunks": 5},
                output={"model": "gpt-4o", "tokens": 500}):
    pass
```

**API响应中的pipeline_trace：**
```json
{
  "pipeline_trace": {
    "vector_search": {
      "name": "向量检索 (Milvus)",
      "count": 10,
      "latency_ms": 45.2
    },
    "keyword_search": {
      "name": "关键词检索 (Elasticsearch)",
      "count": 8,
      "latency_ms": 23.1
    },
    "fusion": {
      "name": "融合排序",
      "count": 10,
      "details": {"mode": "weighted_rrf"}
    },
    "rerank": {
      "name": "LLM 重排序",
      "count": 5,
      "latency_ms": 120.5
    },
    "generation": {
      "name": "大模型生成",
      "count": 5,
      "latency_ms": 1234.5,
      "details": {
        "model": "gpt-4o",
        "fallback": false,
        "total_tokens": 850
      }
    }
  }
}
```

### Q36：如何支持流式输出？

**回答：**
流式输出通过Server-Sent Events (SSE)实现，逐token推送回答。

**实现：**
```python
@app.post("/v1/query/stream")
async def query_stream_endpoint(body: QueryRequest):
    async def event_stream():
        collected_tokens = []
        
        # 1. 发送元数据
        yield f"data: {json.dumps({'type': 'meta', 'query_id': query_id})}\n\n"
        
        # 2. 流式生成
        async for event in query_engine.query_stream(nodes, query):
            if "token" in event:
                collected_tokens.append(event["token"])
                yield f"data: {json.dumps({'type': 'token', 'token': event['token']})}\n\n"
            elif event.get("done"):
                yield f"data: {json.dumps({'type': 'done', 'model_used': event['model_used']})}\n\n"
        
        # 3. 发送管线追踪
        yield f"data: {json.dumps({'type': 'trace', 'pipeline_trace': trace})}\n\n"
        
        yield "data: [DONE]\n\n"
    
    return StreamingResponse(event_stream(), media_type="text/event-stream")
```

### Q37：如何处理长文档？

**回答：**
项目通过预拆分、语义分块和超长chunk保护来处理长文档。

**完整流程：**
```
长文档（100KB）
  ↓
1. 文件大小检查：>50MB → 拒绝
  ↓
2. 文本抽取：PDF/DOCX → 纯文本
  ↓
3. 文本清洗：去控制字符、规范空白
  ↓
4. 预拆分：>20KB → 按20KB分段
  ↓
5. 语义分块：按语义断点切分
  ↓
6. 超长chunk保护：>60KB → 递归拆分
  ↓
7. 向量化：生成embedding
  ↓
8. 入库：写入Milvus + ES
```

**超长chunk保护代码：**
```python
def _force_split_oversized(nodes: list[BaseNode], max_chars: int) -> list[BaseNode]:
    result = []
    for node in nodes:
        text = node.get_content()
        if len(text) <= max_chars:
            result.append(node)
            continue
        
        # 递归二分，尽量在句号处断开
        mid = len(text) // 2
        for sep in ["。", ". ", "；", "\n\n"]:
            pos = text.rfind(sep, mid - 100, mid + 100)
            if pos > 0:
                mid = pos + 1
                break
        
        left = TextNode(text=text[:mid])
        right = TextNode(text=text[mid:])
        result.extend(_force_split_oversized([left, right], max_chars))
    return result
```

### Q38：如何支持跨文档推理？

**回答：**
跨文档推理是指答案需要综合多个文档的信息。

**项目中的支持方式：**
1. 混合检索召回多文档
2. 上下文构建带文档编号
3. 提示词要求引用来源

**示例：**
```
问题: "公司的股权结构和员工持股方案是什么？"

上下文:
[文档1] source=股权结构说明书
公司股权结构：创始人持股30%，天使投资人持股20%...

[文档2] source=员工持股方案
员工通过ESOP可持股15%，分4年归属...

回答:
公司股权结构为创始人持股30%，天使投资人持股20%...[文档1]
员工可通过ESOP持股15%，分4年归属...[文档2]
```

### Q39：什么是术语归一化？如何工作的？

**回答：**
术语归一化是将用户口语和文档书面表达之间建立映射，提升召回率。

**双向映射：**
```python
_DEFAULT_TERMS = {
    # 正向：别名 → 标准术语
    "动态市盈率": "PE-TTM",
    "市盈率ttm": "PE-TTM",
    
    # 反向：标准术语 → 别名
    "ROE": "净资产收益率",
    "ROI": "投入产出比",
}
```

**工作流程：**

1. **Query侧**：追加标准术语
```python
输入: "什么是动态市盈率"
输出: "什么是动态市盈率 PE-TTM"
```

2. **文档侧**：扩展标准术语
```python
输入: "公司ROE为15%"
输出: "公司ROE(净资产收益率)为15%"
```

**热加载支持：**
```python
# data/term_aliases.json 可以热更新，无需重启服务
{
    "新术语": "标准写法",
    ...
}
```

### Q40：如何处理多语言文档？

**回答：**
项目使用多语言embedding模型BAAI/bge-m3，支持中英混合文档。

**模型特性：**
- 1024维
- 多语言支持（中文、英文等）
- 8192 token上下文

**多语言示例：**
```python
中英混合文档:
  "公司使用LLM（Large Language Model）来优化客服..."

检索支持:
  - "什么是LLM" → 匹配
  - "什么是大语言模型" → 也匹配（通过术语归一化）
  - "What is LLM" → 也匹配（多语言embedding）
```

---

## 13. 快速记忆口诀

### 核心概念记忆

**RAG五大环节：**
```
入库 → 检索 → 重排 → 生成 → 评估
```

**混合检索：**
```
向量看语义 + 关键词看精确
```

**多租户隔离五层：**
```
认证 → 入库 → 检索 → 删除 → MCP
```

**降级三段：**
```
主模型 → 备用模型 → 降级提示
```

**缓存失效两招：**
```
反向索引精确失效 + TTL兜底
```

### 面试常用短语

- **介绍项目**：企业级RAG + 多租户隔离 + MCP集成
- **讲架构**：JWT认证 → 限流 → 混合检索 → 融合 → 重排 → 生成 → 评估
- **讲安全**：tenant_id贯穿入库/检索/删除
- **讲效果**：查询改写 + 混合检索 + 动态权重 + 重排
- **讲可用**：主备降级 + 限流fail-open + 异步评估
- **讲优化**：ByteCache + TTLCache + 上下文压缩 + 细粒度失效
