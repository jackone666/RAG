# IntelliLens-MCP 项目结构分析

## 项目概览

```
rag/
├── main.py                          # FastAPI 应用入口 + MCP 服务器
├── requirements.txt                 # 依赖列表
├── docker-compose.yml               # 本地开发环境
├── .env.example                     # 环境变量模板
│
├── src/
│   ├── api/                         # HTTP API 层
│   │   ├── documents.py             # 文档上传、查询、删除
│   │   ├── auth.py                  # 登录、Token 管理
│   │   └── __init__.py
│   │
│   ├── config/                      # 配置管理
│   │   ├── settings.py              # Pydantic Settings（所有环境变量）
│   │   ├── rag_params.py            # RAG 参数配置
│   │   └── __init__.py
│   │
│   ├── middleware/                  # 中间件层
│   │   ├── auth.py                  # JWT 认证、RBAC、tenant_context 提取
│   │   ├── rate_limiter.py          # Redis 分布式限流（Lua 脚本）
│   │   ├── user_store.py            # 用户存储（内存或数据库）
│   │   └── __init__.py
│   │
│   ├── engine/                      # 核心 RAG 引擎
│   │   ├── query_rewriter.py        # 查询改写、HyDE、规则分解
│   │   ├── retrievers.py            # Milvus + ES 混合检索、RRF 融合
│   │   ├── query_engine.py          # Rerank、上下文压缩、生成、主备降级
│   │   ├── multi_granularity.py     # 多粒度索引（新增）
│   │   └── __init__.py
│   │
│   ├── pipeline/                    # 数据处理管道
│   │   ├── ingestion.py             # 文档入库、预处理、分块、嵌入
│   │   ├── preprocessing.py         # 文本清洗、结构解析
│   │   ├── doc_store.py             # 文档元数据存储
│   │   ├── sync_manager.py          # 文档删除同步、租户清理
│   │   ├── near_dedup.py            # 近重复检测（新增）
│   │   └── __init__.py
│   │
│   ├── storage/                     # 存储层
│   │   ├── pg_store.py              # PostgreSQL 存储（元数据、用户、评估结果）
│   │   └── __init__.py
│   │
│   ├── evaluation/                  # 质量评估
│   │   ├── evaluator.py             # AsyncEvaluator、LLM judge
│   │   ├── golden_set.py            # 人工评估集管理（新增）
│   │   └── __init__.py
│   │
│   ├── observability/               # 可观测性
│   │   ├── tracer.py                # Langfuse CallbackManager
│   │   └── __init__.py
│   │
│   ├── utils/                       # 工具函数
│   │   ├── cache.py                 # TTLCache
│   │   ├── byte_cache.py            # Redis ByteCache
│   │   ├── terminology.py           # 术语归一化（新增）
│   │   └── __init__.py
│   │
│   ├── mcp_server/                  # MCP 服务器
│   │   ├── server.py                # FastMCP 单例
│   │   ├── tools.py                 # MCP 工具定义
│   │   └── __init__.py
│   │
│   └── __init__.py
│
├── tests/                           # 单元测试
│   ├── conftest.py                  # pytest 配置、fixtures
│   ├── test_main_api.py             # API 端点测试
│   ├── test_auth.py                 # 认证测试
│   ├── test_rate_limiter.py         # 限流测试
│   ├── test_retrievers.py           # 检索测试
│   ├── test_query_engine.py         # 生成测试
│   ├── test_ingestion.py            # 入库测试
│   ├── test_sync_manager.py         # 同步测试
│   ├── test_evaluator.py            # 评估测试
│   ├── test_tracer.py               # 追踪测试
│   ├── test_mcp_tools.py            # MCP 工具测试
│   ├── test_documents.py            # 文档管理测试
│   ├── test_config.py               # 配置测试
│   ├── test_helpers.py              # 辅助函数测试
│   └── __init__.py
│
├── static/                          # 前端资源
│   ├── index.html                   # 查询页面
│   └── login.html                   # 登录页面
│
├── data/                            # 数据目录（运行时生成）
│   ├── bad_cases.jsonl              # 坏例沉淀
│   ├── feedback.jsonl               # 用户反馈（新增）
│   └── golden_set.jsonl             # 人工评估集（新增）
│
├── Project.md                       # 项目说明文档（面试用）
├── CLAUDE.md                        # Claude Code 指导
├── README.md                        # 项目 README
├── 模拟面试问答.md                   # 深度面试问答
├── 面试速查表.md                     # 快速查阅表（新增）
├── 面试准备-项目设计与问答.md         # 详细面试准备
├── 优化.md                          # 优化方向
├── 修复项目.md                       # 已知问题修复
└── BUG_FIX_REPORT.md                # Bug 修复报告
```

## 核心数据流

### 1. 文档入库流程

```
用户上传文件
  ↓
API: POST /v1/documents/upload
  ↓
middleware: tenant_context_middleware（提取 tenant_id）
  ↓
documents.py: upload_document()
  ├─ 文件类型/大小校验
  ├─ 文本抽取（PyMuPDF / python-docx）
  ├─ 文本清洗（preprocessing.py）
  ├─ 结构解析（标题、章节）
  ├─ 内容 hash（SHA256 去重）
  ├─ MinIO 保存原始文件
  └─ 触发后台任务
      ↓
ingestion.py: ingest_document()
  ├─ 大文档预拆分
  ├─ SemanticSplitter 语义分块
  ├─ 超长 chunk 保护
  ├─ 注入 tenant_id / doc_id / content_hash
  ├─ BAAI/bge-m3 嵌入
  ├─ Milvus 向量入库
  └─ Elasticsearch 关键词索引后台写入
      ↓
返回 document_id 给用户
```

### 2. 查询流程

```
用户发送查询
  ↓
API: POST /v1/query
  ↓
middleware: tenant_context_middleware（提取 tenant_id）
  ↓
middleware: rate_limit_dependency（Redis 限流）
  ↓
query_rewriter.py: rewrite_query()
  ├─ 术语归一化
  ├─ 查询改写
  ├─ 规则分解
  └─ HyDE 假设文档
      ↓
retrievers.py: TenantAwareQueryFusionRetriever.aretrieve()
  ├─ Milvus 向量检索（带 tenant_id filter）
  ├─ Elasticsearch 关键词检索（带 tenant_id filter）
  └─ RRF 融合
      ↓
query_engine.py: RAGQueryEngine.rerank()
  └─ BAAI/bge-reranker-v2-m3 精排 Top-5
      ↓
query_engine.py: RAGQueryEngine.query()
  ├─ 上下文压缩（关键句抽取）
  ├─ Prompt 构建
  ├─ 主模型生成
  ├─ 失败切备用模型
  └─ 再失败返回降级文案
      ↓
tracer.py: Langfuse 记录 span
  ├─ retrieval span
  ├─ rerank span
  └─ generation span
      ↓
evaluator.py: AsyncEvaluator.evaluate()（后台异步）
  ├─ 检索指标（precision / recall / MRR / hit_rate）
  ├─ 生成指标（faithfulness / relevance）
  └─ 低分样本写入 bad_cases.jsonl
      ↓
返回 response + pipeline_trace 给用户
```

### 3. 文档删除流程

```
用户删除文档
  ↓
API: DELETE /v1/documents/{doc_id}
  ↓
middleware: tenant_context_middleware + RBACGuard
  ↓
sync_manager.py: delete_document()
  ├─ Milvus 删除（带 tenant_id + doc_id filter）
  ├─ Elasticsearch 删除（带 tenant_id + doc_id filter）
  ├─ MinIO 删除原始文件
  ├─ PostgreSQL 更新 doc_status = 'deleted'
  └─ 清理相关缓存（TTLCache + ByteCache）
      ↓
返回成功响应
```

## 关键设计决策

### 1. 为什么选 LlamaIndex？

| 特性 | LlamaIndex | LangChain |
|------|-----------|----------|
| MetadataFilters | ✅ 原生支持，直接下推到向量库 | ❌ 需要手动拼装 |
| Langfuse 集成 | ✅ 官方 CallbackManager | ❌ 需要自己写拦截器 |
| 评估器 | ✅ 内置 FaithfulnessEvaluator | ❌ 需要额外集成 |
| 文档 | ❌ 较小 | ✅ 很大 |
| 社区 | ❌ 较小 | ✅ 很大 |

### 2. 为什么多租户隔离要下推到存储层？

**应用层隔离的问题**：
```python
# 应用层过滤
if request.state.tenant_id == doc.tenant_id:
    return doc
```
- 如果新入职工程师写新 retriever 时忘了加过滤条件，数据就泄露了
- 这是一个**人为错误**的问题，不是技术问题

**存储层隔离的优势**：
```python
# 存储层过滤
filters = MetadataFilters(tenant_id=request.state.tenant_id)
results = vector_store.query(filters=filters)
```
- 即使上层代码有 bug，Milvus 也不会返回其他租户的数据
- 这是**纵深防御**思路

### 3. 为什么要混合检索？

**纯向量检索的问题**：
- 对编号、条款、金额、错误码不稳定
- 例如："合同编号 CN-2024-0883" 可能被向量检索排到很后面

**纯关键词检索的问题**：
- 不懂同义表达
- 例如："如何提升用户留存率" 可能无法匹配 "retention rate"

**混合检索的优势**：
- 向量负责"意思像"，关键词负责"字面准"
- RRF 融合两路结果，兼顾语义覆盖和精确命中

### 4. 为什么 Redis 限流用 Lua 脚本？

**不用 Lua 的问题**（TOCTOU 竞态）：
```python
# 三个独立的 Redis 命令
await redis.zremrangebyscore(key, 0, now - window)  # 清理
count = await redis.zcard(key)                        # 计数
if count < max:
    await redis.zadd(key, now, member)                # 新增
```
- 在 "计数" 和 "新增" 之间，另一个并发请求也通过了判断
- 两个请求都被放行，但实际计数可能已超限

**用 Lua 的优势**：
- 三条命令在 Redis 服务端单线程执行，不可分割
- 从根本上消除了竞态窗口

### 5. 为什么 Redis 限流 fail-open？

**fail-closed 的问题**：
- Redis 挂了，所有请求都被拒绝
- 限流是保护性功能，不是核心业务路径
- 把保护机制变成了单点故障

**fail-open 的优势**：
- Redis 挂了，请求继续放行
- 同时告警，人工介入
- 可以在 Nginx/Envoy 层加二级限流作为兜底

## 技术债与改进方向

### 高优先级（P0）

| 问题 | 影响 | 改进方案 |
|------|------|--------|
| monkey-patch 并发不安全 | 并发请求可能用错 tenant_id | 用 VectorStoreQuery 的 filters 参数 |
| 评估丢失 | 3 次重试失败后评估丢了 | 加 DLQ + 定时重试 |
| 缓存失效被动 | 文档更新后需要等 TTL 过期 | 文档删除时主动清理缓存 |

### 中优先级（P1）

| 问题 | 影响 | 改进方案 |
|------|------|--------|
| 检索串行 | 向量和关键词检索不并行 | 用 asyncio.gather 并行化 |
| 没有 Token 刷新机制 | JWT 无法主动失效 | 加 Refresh Token + Token 黑名单 |
| 没有 Nginx 反向代理 | 缺少 TLS、连接限流 | 部署 Nginx/Envoy |

### 低优先级（P2）

| 问题 | 影响 | 改进方案 |
|------|------|--------|
| 没有 Prometheus 指标 | 无法监控性能 | 暴露 /metrics 端点 |
| 没有日志聚合 | 日志分散在各个 Pod | Loguru + Fluentd + Elasticsearch |
| 没有 E2E 测试 | 无法验证完整流程 | 加集成测试 |

## 部署架构

### 本地开发环境

```
docker-compose.yml
├── FastAPI (Uvicorn)
├── Milvus
├── Elasticsearch
├── Redis
├── MinIO
└── PostgreSQL
```

### 生产环境（Kubernetes）

```
Kubernetes Cluster
├── FastAPI Deployment (3 replicas)
│   ├── Pod 1
│   ├── Pod 2
│   └── Pod 3
├── Milvus Cluster
│   ├── Coordinator
│   ├── QueryNode (3 replicas)
│   └── DataNode (3 replicas)
├── Elasticsearch Cluster
│   ├── Master (3 replicas)
│   ├── Data (3 replicas)
│   └── Ingest (2 replicas)
├── Redis Cluster (6 nodes)
├── MinIO Cluster (4 nodes)
├── PostgreSQL (主从)
└── Nginx Ingress
```

## 性能指标

### 目标 SLA

| 指标 | 目标 | 当前 |
|------|------|------|
| P99 延迟 | < 2s | 1.5s（缓存命中）/ 3s（缓存未命中） |
| 可用性 | 99.9% | 99.5%（需要 Redis Cluster） |
| 吞吐量 | 1000 QPS | 500 QPS（单机） |
| 错误率 | < 0.1% | 0.05% |

### 成本估算

| 组件 | 月成本 | 说明 |
|------|-------|------|
| LLM API（gpt-4o） | $500 | 假设 100 万次查询，平均 1000 token |
| Embedding API（bge-m3） | $100 | 假设 1000 万个 chunk |
| Milvus Cloud | $200 | 中等规模集群 |
| Elasticsearch Cloud | $150 | 中等规模集群 |
| Redis Cloud | $50 | 中等规模集群 |
| MinIO | $100 | 自托管 |
| PostgreSQL | $50 | 自托管 |
| **总计** | **$1150** | 月成本 |

## 快速启动

### 本地开发

```bash
# 1. 克隆项目
git clone <repo>
cd rag

# 2. 创建虚拟环境
python -m venv .venv
source .venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动 Docker Compose
docker-compose up -d

# 5. 初始化数据库
python -m alembic upgrade head

# 6. 启动应用
python main.py

# 7. 访问 API 文档
open http://localhost:8000/docs
```

### 生产部署

```bash
# 1. 构建 Docker 镜像
docker build -t intellilens-mcp:v2.1.0 .

# 2. 推送到镜像仓库
docker push <registry>/intellilens-mcp:v2.1.0

# 3. 部署到 Kubernetes
helm install intellilens ./helm/intellilens-mcp \
  --values ./helm/values-prod.yaml

# 4. 验证部署
kubectl get pods -l app=intellilens-mcp
```
