# IntelliLens-MCP — 模拟面试官深度追问

---

## 第一轮：架构设计

### Q1: "你为什么要选 LlamaIndex 而不是 LangChain？三句话说清楚。"

**答**：

1. **MetadataFilters 原生支持**：LlamaIndex 的 `MetadataFilters` 可以直接下推到向量存储层（Milvus 标量过滤），LangChain
   需要手动拼装 filter 表达式，多租户场景下 LlamaIndex 的抽象更干净。
2. **官方可观测性集成**：`CallbackManager` + Langfuse 官方 handler，一行
   `Settings.callback_manager = CallbackManager([handler])` 就完成了全链路追踪，不需要自己写拦截器。
3. **内置评估器**：`FaithfulnessEvaluator` 直接可用，配合 `aevaluate()` 异步调用，LangChain 的评估体系需要额外集成。

**追问**: "那 LlamaIndex 的缺点是什么？"

**答**：文档和社区比 LangChain 小，遇到边缘问题排查更费劲。另外 `IngestionPipeline` 的 `transformations` 抽象在复杂多步变换时不如
LangChain 的 LCEL 灵活。如果后续要做复杂的多步 Agent 编排，可能会混用两个框架。

---

### Q2: "你的五道防线，哪一道最难实现？为什么？"

**答**：最难的是**第一道——多租户 RBAC 在存储引擎层的硬隔离**。

不是因为代码复杂，而是因为**安全假设必须零容忍**。应用层的 `if tenant_id == ...` 过滤有个致命问题：如果某个新入职的工程师在写新
retriever 时忘了加过滤条件，数据就泄露了。

所以我把过滤推到 Milvus 的 `MetadataFilters`，在存储引擎查询阶段就完成隔离——即使上层代码有 bug，Milvus 也不会返回其他租户的数据。这是一种
**纵深防御**思路：不在最后一层设防，而是在最底层设防。

**追问**: "那如果攻击者直接连 Milvus 呢？"

**答**：生产环境 Milvus 应该开启认证（token），并且放在 VPC 内网，不暴露公网端口。应用服务器通过内网 IP + Milvus token
连接。另外可以在 Milvus 前面加一层 Proxy 做审计日志。

---

### Q3: "检索流程中为什么需要向量+关键词两路融合？只用向量不够吗？"

**答**：这是为了应对**不同类型查询的召回需求**：

- **语义类查询**（"如何提升用户留存率"）：向量检索擅长，能抓到语义相关的概念性文档
- **精确类查询**（"合同编号 CN-2024-0883 的终止日期"）：向量检索可能漏掉或排到很后面，关键词匹配（BM25）对数字、编号、专业术语的召回更准确

两路融合用 `RELATIVE_SCORE` 模式归一化得分后合并排序，兼顾了语义覆盖和精确命中。

**追问**: "关键词检索引擎为什么自己写而不用 Elasticsearch？"

**答**：当前实现是内存 TF 词频匹配，适合 POC 阶段。文档里已标注生产应替换为 ES。之所以没直接集成 ES
是考虑到部署复杂度——这个项目定位是展示架构设计能力，每个模块都可以独立替换。关键词检索的 `KeywordRetriever` 实现了与
`BaseRetriever` 兼容的接口，换成 ES 只需换实现类，调用方代码零改动。

---

## 第二轮：分布式与高可用

### Q4: "Redis 限流用了 Lua 脚本，为什么不能只用 Redis 基础命令？"

**答**：因为 **TOCTOU 竞态**（Time-of-Check Time-of-Use）。

假设不用 Lua，分为三步：

```python
await redis.zremrangebyscore(key, 0, now - window)  # 清理
count = await redis.zcard(key)                        # 计数
if count < max:
    await redis.zadd(key, now, member)                # 新增
```

在 "计数" 和 "新增" 之间，另一个并发请求也通过了 `count < max` 的判断——两个请求都被放行，但实际计数可能已超限。在 4
worker × 1000 QPS 场景下，这种竞态会导致实际限流阈值失效。

Lua 脚本在 Redis 服务端单线程执行，三条命令不可分割，从根本上消除了竞态窗口。

**追问**: "为什么选择了 ZSET 而不是 String + INCR 的固定窗口？"

**答**：固定窗口（如每分钟计数器 + EXPIRE 60s）有个**边界突发**问题：

- 假设限制 60次/分钟
- 用户在 12:00:59 发了 60 次
- 在 12:01:00 计数器归零，又能发 60 次
- 实际 2 秒内发出了 120 次请求

滑动窗口用 ZSET 记录每个请求的精确时间戳，没有固定边界，任何 60 秒窗口内都不会超过阈值。

---

### Q5: "Redis 挂了你的限流怎么办？你会让服务中断吗？"

**答**：不会。我设计的策略是 **fail-open + 告警**：

```python
try:
    result = await redis.evalsha(...)
except (ConnectionError, TimeoutError):
    logger.error("Redis 不可用，fail-open 放行")
    return True  # 放行
```

选 fail-open 而不是 fail-closed 的原因是：限流是**保护性功能**，不是**核心业务路径**。因为限流故障导致所有合法请求被拒，这是把保护机制变成了单点故障。

**追问**: "那恶意用户趁着 Redis 挂了刷爆你的 API 怎么办？"

**答**：两个补充手段：

1. 在 Uvicorn 前面加一层 Nginx/Envoy，配置 `limit_req_zone` 作为 IP 级的兜底限流——这是进程级别的，不依赖 Redis
2. 可以在应用层加一个本地内存计数器作为二级限流（代价是进程间不一致，但至少能挡住单进程的极端情况）

---

### Q6: "如果你的主模型和备用模型都来自同一个 API Provider（比如都是 OpenAI），Provider 整体宕机了怎么办？"

**答**：当前设计的主备模型假设是不同的模型型号（gpt-4o → gpt-4o-mini），如果 Provider 整体宕机确实无法切换。

生产级的改进方案：

1. **多 Provider 配置**：`PRIMARY_MODEL` 走 OpenAI，`FALLBACK_MODEL` 走 Azure OpenAI 或 Anthropic Claude，API Base URL 分别配置
2. **本地模型兜底**：部署 vLLM 或 Ollama 托管一个开源模型（如 Qwen-2.5），作为第三层降级
3. **缓存热点问答**：高频问题走 Redis 缓存直接返回，减少实时 LLM 调用

当前代码的 `_get_fallback_llm()` 可以配置不同的 `api_base`，只需在 Settings 中增加 `FALLBACK_BASE_URL` 字段即可支持多
Provider。

---

## 第三轮：安全模型

### Q7: "JWT Token 被窃取了怎么办？你做了哪些防护？"

**答**：坦诚说，当前实现是基础 JWT 验证，没有做 Token 撤销和刷新机制。生产需要加：

1. **短期 Token + Refresh Token**：Access Token 有效期 15 分钟，Refresh Token 7 天，泄露窗口有限
2. **Token 黑名单**（Redis）：用户登出或异常行为时，将 `jti` 加入黑名单，中间件每次校验时查 Redis
3. **IP 绑定**（可选）：JWT Payload 中包含签发时的客户端 IP，验证时比对，防止 Token 跨网络使用
4. **限流本身就提供了一定保护**：同一个 tenant_id 一分钟内最多 60 次请求，即使 Token 泄露，攻击者也无法大规模滥用

---

### Q8: "你的 RBAC 三个角色（admin/editor/viewer），怎么和实际的企业权限系统对接？"

**答**：当前 `RBACGuard` 是依赖注入方式：

```python
@app.get("/admin-only")
async def admin_route(ctx: dict = Depends(RBACGuard("admin"))):
    ...
```

对接企业权限系统（如 LDAP/AD/Okta）有两种方式：

1. **JWT Payload 映射**：企业 SSO 签发的 JWT 中通常包含 `groups` 或 `realm_access.roles` 字段，在 `extract_tenant_context`
   中映射到 admin/editor/viewer
2. **外部权限服务调用**：中间件拿到 `user_id` 后调用企业权限 API 查询角色，缓存结果到 Redis（TTL 5 分钟）

当前设计的关键是 `ROLE_HIERARCHY` 字典可以直接扩展——增加 `super_admin: 4` 或更细粒度的 `auditor: 0.5` 都不会破坏现有逻辑。

---

### Q9: "如果租户 A 的管理员恶意构造请求，尝试访问租户 B 的数据，你有哪些防护层？"

**答**：四层纵深防御：

| 层级                 | 机制                                | 如果被绕过                          |
|--------------------|-----------------------------------|--------------------------------|
| 1. JWT 认证          | Token 中的 tenant_id 不可篡改（签名校验）     | 需要窃取租户 B 的有效 Token             |
| 2. RBACGuard       | 路由级角色校验                           | 同租户内不同角色的权限隔离                  |
| 3. MetadataFilters | Milvus 存储引擎层过滤                    | 即使应用层代码有 bug，Milvus 也不会返回跨租户数据 |
| 4. 审计追踪（Langfuse）  | 每次查询记录 tenant_id + user_id + 检索结果 | 事后审计能发现异常查询模式                  |

核心思想：不要把安全寄托在任何单一层上。最关键的第三层——即便有人跳过了前两层，存储引擎的硬过滤是最后一道无法绕过的防线。

---

## 第四轮：性能与扩展性

### Q10: "你的混合检索中，向量检索和关键词检索是串行还是并行？"

**答**：当前实现是串行：先向量检索拿到候选集，再在候选集上做关键词匹配和融合。

这在候选集不大（top_k=10）时没问题，但如果要做大规模召回（top_k=1000），优化方案是：

```python
# 并行执行 + asyncio.gather
vector_task = vector_retriever.aretrieve(query_bundle)
keyword_task = keyword_retriever.aretrieve(query_bundle)
vector_nodes, keyword_nodes = await asyncio.gather(vector_task, keyword_task)
```

然后融合两路独立的结果集，而不是关键词仅在向量结果上重排。这样两路可以独立扩展。

---

### Q11: "大模型调用慢怎么办？用户要等 3-5 秒。"

**答**：几个方向：

1. **流式输出（SSE）**：当前 `acomplete()` 是等待完整响应。改用 `astream_chat()` + StreamingResponse，用户 200ms 就能看到第一个
   token
2. **缓存常见问题**：对高频查询（如"公司年假政策"），将最终回答缓存到 Redis，相同 query → 直接返回，跳过 LLM
3. **缩短上下文**：Rerank 只保留 Top-3（当前 Top-5），减少 Prompt Token 数
4. **异步预测**：对于可以预判的后续问题（如列表中的下一个），提前预热检索结果

当前`query_engine.py` 的 Prompt 是中英双语，可以去掉英文部分减少 Token。

---

### Q12: "Milvus 集合有多大？索引怎么选的？"

**答**：当前代码未涉及索引选型（Collection 假定已创建）。对于一个典型的企业知识库场景（百万级文档，千万级 Chunk）：

- **索引类型**：IVF_SQ8（内存敏感）或 HNSW（速度优先）
- **标量索引**：tenant_id 和 doc_id 建字典索引（Dict Index），过滤查询走索引不扫全表
- **分区策略**：按 tenant_id 做 Partition Key，大租户物理隔离，小租户共享分区

这些配置应该在 Migration 脚本中定义，不在应用代码中。

---

## 第五轮：质量与运维

### Q13: "你的裁判模型评估结果谁来看？发现了坏例然后呢？"

**答**：当前实现是**自动沉淀**——坏例写入 `bad_cases.jsonl`，但还没有自动处理链路。完整的闭环应该是：

```
bad_cases.jsonl
    → 定时任务（Celery/Cron）聚合分析
    → 按租户 + 错误类型分类
    → 高优 Bad Case 推送 Slack/钉钉通知 → 人工审核
    → 审核后的修正答案 → 写回知识库 / 微调模型 / 更新 Prompt
```

`bad_cases.jsonl` 的 JSONL 格式设计就是为了方便后续流式处理——每行一个独立 JSON，可以 tail -f 实时消费，也可以批量导入
Pandas 做分析。

---

### Q14: "FaithfulnessEvaluator 本身也可能产生幻觉（裁判误判），你怎么处理？"

**答**：确实存在这个问题。裁判模型的评估结果不能 100% 相信：

1. **阈值不是硬边界**：score=0.79 和 score=0.81 在统计上可能没有显著差异，所以 `bad_cases.jsonl` 中同时记录了 `eval_score`
   和 `eval_feedback`，人工审核时可以二次判断
2. **多裁判交叉验证**（生产优化）：对同一回答用多个裁判模型打分（GPT-4o + Claude），取最低分或投票
3. **定期人工抽检**：每周从 `bad_cases.jsonl` 中随机抽 20 条人工复审，计算裁判模型的准确率和召回率

当前设计中 temperature=0.0 可以减少裁判模型自身的随机性，但不能消除系统性偏见。

---

### Q15: "这个系统如果要上生产，还缺什么？"

**答**：

| 缺失项                         | 优先级 | 说明                                              |
|-----------------------------|-----|-------------------------------------------------|
| Collection Schema Migration | P0  | 首次部署需要创建 Milvus Collection + 索引                 |
| Token 刷新/撤销机制               | P0  | JWT 目前无状态，无法主动失效                                |
| Nginx/Envoy 反向代理            | P1  | TLS 终止、静态文件、连接限流                                |
| 健康检查就绪探针                    | P1  | K8s readiness probe 需要检查 Milvus + Redis 连接      |
| 配置热更新                       | P1  | 当前修改配置需要重启服务                                    |
| 日志聚合                        | P1  | Loguru 写文件 → Fluentd/Fluent Bit → Elasticsearch |
| 指标暴露                        | P2  | Prometheus metrics（请求延迟、检索延迟、限流触发次数）            |
| 集成测试                        | P2  | 当前只有单元架构，缺少 E2E 测试                              |
| 多 Provider 降级               | P2  | 支持 OpenAI + Azure + Anthropic 互备                |
| Redis Cluster/Sentinel      | P2  | 限流层高可用                                          |

---

## 第六轮：独立 coding 能力

### Q16: "如果让你加一个功能——用户对回答不满意可以点'踩'，然后系统用这个反馈来优化检索策略。你打算怎么设计？"

**答**：三步设计：

**第一步：反馈收集（轻量）**

```python
# POST /v1/feedback
class FeedbackRequest(BaseModel):
    query_id: str      # 哪次查询
    rating: int        # 1-5 打分，或 thumbs_up / thumbs_down
    comment: str       # 用户自由反馈（可选）
```

数据写入 `data/feedback.jsonl`，同时上报 Langfuse 作为 `score` trace。

**第二步：检索策略自适应（离线）**

- 定期分析低分 query 的共性（query 长度？领域术语？缺少的关键词？）
- 如果特定领域的低分率高 → 调整 `SemanticSplitterNodeParser` 的分块大小
- 如果关键词类查询低分率高 → 增加 `KeywordRetriever` 的融合权重（`FUSION_MODES.RECIPROCAL_RANK`）

**第三步：Prompt 优化（半自动）**

- 坏例聚类 → 发现重复的错误模式（如"回答引用了过期的合同模板"）
- 在 Prompt 中加入反例：`"如果上下文中包含日期早于 2023 年的合同，请明确标注'该信息可能已过期'"`
- A/B 测试新旧 Prompt

关键设计原则：用户反馈用于**优化检索策略和 Prompt**，而不是直接喂给模型微调——避免某条错误反馈污染模型。

---

### Q17: "AsyncEvaluator 的 tenacity 重试，如果 3 次都失败了，评估就丢了。有没有更好的方案？"

**答**：当前设计是"尽力而为"（best-effort），评估丢失意味着坏例没被记录。更可靠的方案：

**方案 A：死信队列（DLQ）**

```python
try:
    result = await evaluate_with_retry(...)
except MaxRetryError:
    await redis.lpush("eval_dlq", json.dumps(eval_request))
    # 独立 worker 定期重试 DLQ 中的任务
```

**方案 B：数据库持久化 + 定时任务**

```python
# 评估任务先写入 DB（状态=pending）
await db.insert("eval_tasks", {status: "pending", ...})
# 定时 Celery 任务扫描 pending 任务并执行
# 失败标记为 failed，超过 N 次标记为 abandoned
```

**方案 C（当前最佳平衡）**：BackgroundTasks + Redis DLQ

- 主流程：BackgroundTasks → evaluate → 失败 → 写入 DLQ
- 后台 Cron 每分钟扫描 DLQ 重试（最多 3 次）
- 3 次后告警 + 人工介入

当前代码选"尽力而为"是为了保持模块简洁，面试中我会说明：这是一个有意识的权衡——评估是离线质量监控，不是在线事务，丢失少量评估样本不影响业务正确性。

---

### Q18: "Retriever 中你用 monkey-patch 替换了 vector_store.query 方法。这个做法有什么问题？如果两个并发请求同时进来会怎样？"

**答**：这是一个很好的发现。当前代码：

```python
self.vector_store.query = filtered_query  # 全局修改！
try:
    nodes = await vector_retriever.aretrieve(...)
finally:
    self.vector_store.query = original_query
```

问题确实存在：**两个并发请求的 tenant_id 不同，后一个请求会覆盖前一个的 filter**。在 `finally` 恢复前，另一个请求可能用到了错误的
tenant filter。

**正确的修复方案**（我标注为待优化）：

```python
async def retrieve(self, query: str, tenant_id: str, top_k: int = 10):
    # 方案1: 如果 MilvusVectorStore 支持 filter 参数
    retriever = self.vector_store.as_retriever(
        similarity_top_k=top_k,
        vector_store_kwargs={"filters": tenant_filter}
    )
    
    # 方案2: 直接调用 vector_store.aquery() 传入 filter
    query = VectorStoreQuery(
        query_str=query, 
        similarity_top_k=top_k,
        filters=tenant_filter
    )
    result = await self.vector_store.aquery(query)
```

这是当前代码中最大的已知技术债，在"修复项目.md"和面试中应当主动提及——展示对并发安全的理解。

---

### Q19: "如果要把这个系统从单机部署改成 Kubernetes 集群，最大的挑战是什么？"

**答**：三个关键挑战：

1. **有状态组件的分布式一致性**
   - 当前 `InMemoryRateLimiter` 是进程内存，多个 Pod 间无法共享
   - 必须改用 Redis 分布式限流（已有 Lua 脚本，改动不大）
   - ByteCache 也需要从本地 Redis 改成集群 Redis

2. **Milvus 和 Elasticsearch 的高可用**
   - 单机 Milvus 不适合生产，需要 Milvus Cluster 或 Milvus Cloud
   - ES 需要配置副本和分片策略
   - 应用层需要健康检查和自动重连

3. **配置和密钥管理**
   - 当前 `.env` 文件不适合 K8s，需要改用 ConfigMap + Secret
   - 敏感信息（API Key、数据库密码）应该用 Secret 而不是 ConfigMap
   - 支持配置热更新，不需要重启 Pod

**生产改进方案**：
- 使用 Helm Chart 定义 K8s 资源
- 配置 Readiness Probe 检查 Milvus + Redis 连接
- 使用 HPA（Horizontal Pod Autoscaler）根据 CPU/内存自动扩容
- 配置 PDB（Pod Disruption Budget）保证最小可用副本数

---

### Q20: "你的项目中有哪些地方可能成为性能瓶颈？怎么排查？"

**答**：几个可能的瓶颈：

1. **向量检索延迟**（最常见）
   - Milvus 查询慢可能是索引选型不对（IVF_SQ8 vs HNSW）或参数调优不足
   - 排查方法：Langfuse trace 中看 retrieval span 的 latency，对比 Milvus 日志
   - 优化方向：增加 nprobe、调整 ef_search、或改用 GPU 索引

2. **LLM 调用延迟**（最直观）
   - 主要取决于 API Provider 的响应时间和模型大小
   - 排查方法：Langfuse 中看 generation span 的 latency
   - 优化方向：用流式输出让用户更快看到第一个 token，或缓存常见问题

3. **Rerank 延迟**（容易忽视）
   - 当前 rerank 是串行的，Top-100 候选逐个打分
   - 排查方法：Langfuse 中看 rerank span 的 latency
   - 优化方向：批量 rerank（bge-reranker 支持 batch），或只 rerank Top-20

4. **网络往返延迟**（分布式系统常见）
   - 多个 Redis、Milvus、ES 调用累积
   - 排查方法：Langfuse trace 中看各个 span 的时间分布
   - 优化方向：并行化（asyncio.gather）、缓存、或减少调用次数

**排查工具链**：
- Langfuse Dashboard：可视化每个 span 的 latency
- Prometheus + Grafana：应用层指标（请求延迟、QPS、错误率）
- 数据库慢查询日志：Milvus slow log、ES slow log
- 火焰图（Flamegraph）：CPU 热点分析

---

### Q21: "如果一个租户的文档特别多（百万级），检索变得很慢，怎么办？"

**答**：这是一个典型的"大租户问题"。几个解决方案：

1. **Milvus 分区策略**
   - 按 tenant_id 创建 Partition Key，大租户物理隔离
   - 查询时指定 partition，避免扫全表
   - 代价：需要提前规划分区，不能动态调整

2. **多 Collection 策略**
   - 为大租户单独创建 Collection（如 `knowledge_tenant_123`）
   - 小租户共享一个 Collection（`knowledge_shared`）
   - 代价：管理复杂度高，需要路由逻辑

3. **缓存优化**
   - 大租户的热点查询更容易命中缓存
   - 可以提高 TTLCache 的 TTL 或增加 ByteCache 的容量
   - 代价：缓存失效时需要更谨慎

4. **检索策略优化**
   - 减少 top_k（从 100 改成 50）
   - 增加 rerank 的阈值，只 rerank Top-20
   - 使用更快的索引（HNSW 比 IVF_SQ8 快）

**我的建议**：
- 短期：用缓存 + 减少 top_k
- 中期：按 tenant_id 做 Milvus 分区
- 长期：考虑多 Collection 或分库分表

---

### Q22: "你的 golden_set.py 是怎么设计的？人工标注的流程是什么？"

**答**：`golden_set.py` 的设计思路是**从坏例到标注集的自动化流程**：

**第一步：坏例聚类**
```python
# 从 bad_cases.jsonl 中读取低分样本
# 按 query 长度、领域、错误类型分类
# 生成 candidates.jsonl
```

**第二步：人工标注**
- 标注者打开 candidates.jsonl，逐条审核
- 对每条记录补充：
  - `expected_answer`：正确答案
  - `label`：错误类型（检索漏召、生成幻觉、排序错误等）
  - `notes`：备注
- 标注后的记录写入 `golden_set.jsonl`

**第三步：离线回归**
```python
# 用 golden_set.jsonl 做离线评估
# 对比新旧 Prompt、新旧检索策略
# 计算 precision、recall、F1
```

**当前缺失的部分**：
- 没有标注后台（Web UI）
- 没有定时任务自动聚类
- 没有离线回归的自动化

**生产改进**：
- 用 Streamlit 或 FastAPI 做简单的标注 UI
- Celery 定时任务每天聚类一次
- 集成 MLflow 或 Weights & Biases 做实验追踪

---

### Q23: "如果用户上传了一个 10GB 的 PDF，你的系统怎么处理？"

**答**：当前代码有 50MB 的文件大小限制，10GB 会被拒绝。但如果要支持超大文件，需要几个改进：

1. **分块上传**
   - 前端分块上传（如 5MB 一块）
   - 后端合并后再处理
   - 需要 Redis 记录上传进度

2. **流式处理**
   - 不等待整个文件上传完成，边上传边处理
   - 对于 PDF，可以逐页处理
   - 减少内存占用

3. **异步处理**
   - 上传后立即返回 task_id
   - 后台 Celery 任务处理文件
   - 用户可以查询处理进度

4. **存储优化**
   - MinIO 支持分片上传（Multipart Upload）
   - 原始文件不需要全部加载到内存

**当前代码改进方案**：
```python
# 在 documents.py 中
@app.post("/v1/documents/upload-large")
async def upload_large_document(
    file: UploadFile,
    tenant_id: str = Header(...),
):
    # 1. 保存上传进度到 Redis
    # 2. 触发后台 Celery 任务
    # 3. 返回 task_id
    task_id = await celery_app.send_task(
        'process_document',
        args=(file.filename, tenant_id)
    )
    return {"task_id": task_id}
```

---

### Q24: "你的系统支持多语言吗？中文、英文、日文混合的文档怎么处理？"

**答**：当前设计对多语言有基础支持，但还有优化空间：

**已支持的部分**：
- bge-m3 embedding 模型本身支持多语言（中、英、日、韩等）
- 文本清洗时保留了中文标点和英文标点
- 术语归一化可以扩展为多语言术语表

**需要改进的部分**：
1. **分块策略**
   - 当前 SemanticSplitter 是基于英文的，对中文可能不够优化
   - 中文应该按句号、问号、感叹号切分
   - 日文应该按句号、顿号切分
   - 可以用 jieba（中文）、janome（日文）做语言特定的分词

2. **查询改写**
   - 当前 query rewrite 用的是英文 prompt
   - 应该根据 query 的语言动态选择 prompt 语言

3. **Rerank**
   - bge-reranker-v2-m3 支持多语言，但没有特别优化
   - 可以考虑用语言特定的 reranker

**生产改进方案**：
```python
# 在 preprocessing.py 中
def detect_language(text: str) -> str:
    # 用 langdetect 或 textblob 检测语言
    pass

def split_by_language(text: str) -> List[str]:
    # 根据语言选择分割策略
    lang = detect_language(text)
    if lang == 'zh':
        return split_chinese(text)
    elif lang == 'ja':
        return split_japanese(text)
    else:
        return split_english(text)
```

---

### Q25: "如果要做 A/B 测试，对比两个不同的 Prompt，怎么设计？"

**答**：A/B 测试的关键是**随机分组 + 指标对比**：

**第一步：实验分组**
```python
# 在 query_engine.py 中
def get_prompt_version(tenant_id: str, user_id: str) -> str:
    # 用 hash 做确定性分组
    hash_val = hash(f"{tenant_id}:{user_id}") % 100
    if hash_val < 50:
        return "prompt_v1"  # 对照组
    else:
        return "prompt_v2"  # 实验组
```

**第二步：记录实验标签**
```python
# 在 Langfuse trace 中记录
trace = {
    "experiment": "prompt_ab_test",
    "variant": prompt_version,
    "query": query,
    "response": response,
    "score": eval_score,
}
```

**第三步：离线分析**
```python
# 用 Pandas 分析
df = load_traces_from_langfuse()
v1 = df[df['variant'] == 'prompt_v1']
v2 = df[df['variant'] == 'prompt_v2']

# 计算平均分
print(f"v1 avg score: {v1['score'].mean()}")
print(f"v2 avg score: {v2['score'].mean()}")

# 统计显著性检验（t-test）
from scipy import stats
t_stat, p_value = stats.ttest_ind(v1['score'], v2['score'])
print(f"p-value: {p_value}")
```

**关键设计原则**：
- 用 hash 做确定性分组，同一用户始终在同一组
- 样本量要足够（通常需要 1000+ 样本）
- 运行时间要足够（通常需要 1-2 周）
- 用统计显著性检验，不要只看平均值

---

## 总结：面试官可能的评价维度

| 维度 | 表现 | 面试中的表现 |
|------|------|-----------|
| **架构能力** | 五道防线设计有层次，知道每一层的不可替代性 | 能解释为什么选 LlamaIndex、为什么多租户隔离要下推到存储层 |
| **安全意识** | 理解纵深防御，存储层过滤 > 应用层过滤 | 能主动提及 monkey-patch 问题，知道如何修复 |
| **分布式理解** | Redis Lua 原子操作，ZSET vs 固定窗口的区别 | 能解释 TOCTOU 竞态、fail-open 策略 |
| **工程务实** | 承认当前方案的不足，知道生产改进方向 | 能列举 P0/P1/P2 的缺失项，知道优先级 |
| **性能优化** | 知道瓶颈在哪，怎么排查，怎么优化 | 能用 Langfuse trace 定位问题，提出并行化、缓存等方案 |
| **面试表达** | 能一句话说清选型理由，不堆砌术语 | 用"纵深防御""fail-open""TOCTOU 竞态"等精准术语 |
| **主动承认不足** | 知道当前代码的已知问题，有改进计划 | 主动提及 monkey-patch、缓存失效、评估丢失等问题 |
