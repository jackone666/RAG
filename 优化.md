# IntelliLens-MCP 优化记录

> 参考文档：字节跳动 RAG 实践手册（字节rag.pdf）
> 更新时间：2026-05-20
> 说明：本文档记录每条优化的具体实现方式，并标注字节内部方案与本项目的开源平替对照。

---

## 字节内部基础设施 vs 本项目平替总览

| 字节内部 | 本项目平替 | 说明 |
|---------|-----------|------|
| ByteEmbedding（自研嵌入模型） | HuggingFace Embedding（BGE-large-zh） | 本地部署，1024 维 |
| ByteVectorDB（自研向量库） | Milvus（开源） | 已内置 minio + etcd |
| 云雀模型（自研 LLM） | OpenAI GPT-4o / GPT-4o-mini | 兼容 base_url 切换 |
| ByteCache（分布式缓存） | Python OrderedDict + Redis | 单机 LRU → 可升级 |
| ByteLB（负载均衡） | FastAPI 单机 | 暂缺，未来可加 nginx |
| 字节云 OSS（对象存储） | MinIO（开源） | docker-compose 已集成 |
| 自研数据标注平台 | 无 | 依赖 LlamaIndex 自动评估 |
| 自研模型蒸馏框架 | 无 | 未来可探索 |
| 自研分词工具（结巴改进版） | LlamaIndex SemanticSplitter | 语义分块替代固定长度 |
| 自研全链路监控 | Langfuse + 前端管线图 | 开源可观测 + 自建可视化 |

---

## 1. 流式输出 (SSE Streaming)

**字节怎么做**（§6.4）：
- 抖音客服实时问答场景要求首 token 延迟 < 200ms
- 字节内部使用云雀模型的 streaming API，配合自研网关做 token 级转发
- 结合 ByteLB 的请求级流控，防止单个慢请求阻塞其他用户

**我们怎么做**：

```
用户浏览器 ──POST /v1/query/stream──▶ FastAPI ──SSE──▶ 浏览器逐 token 渲染
                                           │
                                           └── OpenAI astream_complete()
```

具体实现：
1. `query_engine.py` 新增 `_generate_stream()` 方法，调用 LlamaIndex OpenAI 的 `astream_complete()`，`async for chunk in response: yield chunk.delta`
2. `query_engine.py` 新增 `query_stream()` 方法，三层降级均支持流式：主模型流 → 备用模型流 → 降级文本逐字发送
3. `main.py` 新增 `/v1/query/stream` 端点，返回 `StreamingResponse(event_stream(), media_type="text/event-stream")`
4. SSE 事件格式：`data: {"type":"meta"}` → `data: {"type":"token"}` × N → `data: {"type":"trace"}` → `data: [DONE]`
5. 前端 `submitQueryStream()`：`fetch` + `ReadableStream` + `TextDecoder`，实时拼接 token 并渲染，带闪烁光标

**平替要点**：
- 字节云雀模型 → OpenAI 兼容 API（`astream_complete`），可通过 `OPENAI_BASE_URL` 切换到任何兼容服务
- 字节 ByteLB → FastAPI StreamingResponse 原生支持，单机无需额外组件
- 字节网关流控 → 依赖现有的 `rate_limit_dependency`（滑动窗口限流）

---

## 2. 查询改写 (Query Rewriting)

**字节怎么做**（§5.1）：
- 复杂多跳问题先经过「查询理解」模块，使用轻量模型做意图识别、实体抽取、指代消解
- 查询分解为原子子查询后，并行检索再合并去重
- 使用缓存避免相同改写请求重复调用 LLM

**我们怎么做**：

```
原始查询 ──▶ QueryRewriter.rewrite()
                │
                ├── 原始查询（始终保留）
                ├── LLM 生成 2-3 个语义等价变体
                └── 去重、限制最多 5 个
            ──▶ 多路并行检索
                │
                ├── 变体1 → retriever.aretrieve_with_trace()
                ├── 变体2 → retriever.aretrieve_with_trace()
                └── 变体3 → retriever.aretrieve_with_trace()
            ──▶ 按 node_id 去重合并，取 score 最高的节点
```

具体实现：
1. 新建 `src/engine/query_rewriter.py`：`QueryRewriter` 类
2. `_expand()` 方法：用 gpt-4o-mini（temperature=0.0）生成改写，prompt 要求「保持原意、分解复合问题、每行一个改写」
3. `rewrite()` 方法：合并原始查询 + LLM 改写，去重后最多 5 个变体，异常时回退只返回原始查询
4. `main.py` 的 `_retrieve_with_rewrite_and_cache()`：遍历改写列表并行检索，用 `dict[node_id] = (node, max_score)` 去重，按分数降序取 top-10

**平替要点**：
- 字节自研查询理解模块 → 直接用 LLM（gpt-4o-mini）做 prompt-based 改写，零额外训练成本
- 字节改写缓存 → 本项目的热点检索缓存已覆盖（命中缓存直接跳过改写+检索）
- 字节意图识别/实体抽取 → 当前未实现，复杂查询依赖 LLM 的通用理解能力

---

## 3. 热点检索缓存 (Hot Query Cache)

**字节怎么做**（§4.3.1）：
- ByteCache 是字节自研的分布式缓存系统，7 天内检索过的向量自动缓存
- 配合 ByteLB 的请求缓存，相同请求 10 分钟内直接返回缓存结果
- 双 11 期间 QPS 承载能力提升 2 倍，平均响应时间稳定在 20ms 以内

**我们怎么做**：

```
查询请求 ──▶ MD5(tenant_id::query.lower()) 
                │
                ├── 命中缓存且未过期 ──▶ 直接返回节点列表
                │
                └── 未命中 ──▶ 查询改写 → 检索 → 写入缓存 ──▶ 返回
```

具体实现：
1. `main.py` 中定义 `_retrieval_cache: OrderedDict[str, tuple[float, list]]`，key 为 MD5 哈希，value 为 `(写入时间戳, 节点列表)`
2. `_MAX_CACHE_SIZE = 256`：写满后自动淘汰最旧条目（OrderedDict.popitem(last=False)）
3. `_CACHE_TTL_SECONDS = 600`：10 分钟过期，参考字节的「相同请求 10 分钟缓存」策略
4. `_retrieve_with_rewrite_and_cache()` 在检索前先查缓存，命中则直接返回

**平替要点**：
- 字节 ByteCache（分布式）→ Python `OrderedDict`（单机内存），适合单机部署
- 未来扩展路径：替换为 Redis（`redis://` 已在 settings 中配置），只需改存储后端
- 当前实现无分布式一致性，多副本部署时缓存不共享（可接受，因为最终数据源一致）

---

## 4. 文档对象存储 (MinIO Document Storage)

**字节怎么做**（§2.2）：
- 数据湖 + 数据仓库分层：数据湖存储原始文档（HDFS/字节云 OSS），数据仓库存储清洗后结构化数据
- 原始文档永久保留，支持后续重处理（更换 embedding 模型、改进分块策略等）
- 冷数据（3 个月前的历史数据）自动迁移至低成本归档存储

**我们怎么做**：

```
文件上传 ──▶ 提取文本 ──▶ SHA256 哈希
                │               │
                │               ├── 写入 Milvus + ES（向量 + 关键词索引）
                │               │
                │               └── 写入 MinIO（原始字节流）
                │                       │
                │          s3://intellilens-documents/{tenant_id}/{doc_id}/{filename}
                │
                └── 重复检测：查询 Milvus 中 content_hash 是否已存在
```

具体实现：
1. 新建 `src/pipeline/doc_store.py`：`DocumentObjectStore` 类，封装 MinIO 客户端
2. 存储路径：`{tenant_id}/{doc_id}/{原文件名}`，带 metadata 标记
3. `store_document()`：`put_object` + 自动创建 bucket
4. `get_document()`：按路径读取原始字节流，供重入库使用
5. `delete_document()` / `delete_tenant_objects()`：生命周期管理 + GDPR 支持
6. `documents.py` 上传端点：文本提取 + 重复检测通过后，调用 `doc_store.store_document()` 写入 MinIO
7. 配置项（`settings.py`）：`minio_endpoint`、`minio_access_key`、`minio_secret_key`、`minio_bucket`

**平替要点**：
- 字节云 OSS / HDFS → MinIO（开源，S3 兼容），已在 docker-compose 中运行
- 文档 bucket（`intellilens-documents`）与 Milvus 内部 bucket 独立，互不干扰
- 冷热分离（字节将 3 个月以上数据迁至冷存储）→ 当前未实现，所有文档保持热存储

---

## 5. 重复文档检测 (Content Hash Dedup)

**字节怎么做**（§3.1.2 / §4.3.2）：
- 数据清洗阶段使用哈希算法删除完全相同的数据记录
- 向量层面：计算新文档向量与已有向量库的相似度，≥95% 判定为重复，仅存元数据不存向量
- 在资讯业务线中，向量去重使重复向量占比从 15% 降至 3%

**我们怎么做**：

```
文件上传
  │
  ├── 1. 提取文本
  ├── 2. SHA256(文本) → content_hash
  ├── 3. check_duplicate(tenant_id, content_hash)
  │       │
  │       ├── 查询 Milvus: tenant_id == "X" && content_hash == "Y"
  │       │
  │       ├── 命中 ──▶ 返回 {duplicate: true, existing_doc_id, chunk_count}
  │       │
  │       └── 未命中 ──▶ 正常入库，content_hash 写入每个 node 的 metadata
  │
  └── 4. LlamaIndex SimpleDocumentStore（data/docstore.json）
          └── DocstoreStrategy.UPSERT：管道内相同 doc_id + content_hash 自动跳过
```

具体实现：
1. `ingestion.py` 新增 `_compute_content_hash(text)`：`hashlib.sha256(text.encode()).hexdigest()`
2. `TenantAwareIngestionPipeline.check_duplicate(tenant_id, content_hash)`：
   - 连接 Milvus，查询 `tenant_id == "X" && content_hash == "Y"`
   - 命中 → 统计该 doc_id 的 chunk 数量
   - 异常 → 降级放行（`logger.warning` + 返回 None）
3. 入库时 `_inject_metadata()` 注入 `content_hash` 字段
4. LlamaIndex `SimpleDocumentStore` 持久化到 `data/docstore.json`，管道 `DocstoreStrategy.UPSERT` 防止重复处理
5. 前端：上传响应 `duplicate=true` 时展示黄色警告卡片，告知已存在的 doc_id 和 chunk_count

**平替要点**：
- 字节「向量相似度 ≥95% 去重」→ 我们简化为「内容 SHA256 完全相等去重」，更严格但无法检测近重复文档
- 字节去重粒度在向量层面 → 我们在 Milvus metadata 层面，查询效率更高（标量过滤 vs 向量相似度计算）
- 未来可增强：对相似度 ≥90% 但 ≠100% 的文档做「疑似重复」标记，人工审核

---

## 6. Token 消耗追踪

**字节怎么做**（§13）：
- RAG 成本精细化管控：LLM 调用费（prompt + completion）、向量库存储费、带宽费分项拆解
- 每个业务线的成本独立核算，成本归因到具体 query
- 通过 Prompt Engineering 优化（精简上下文、减少示例）降低 token 消耗

**我们怎么做**：

```
查询请求
  │
  ├── 检索阶段（不计 LLM token）
  │
  ├── 生成阶段
  │     │
  │     └── OpenAI API 响应 .raw.usage
  │           ├── prompt_tokens
  │           ├── completion_tokens
  │           └── total_tokens
  │
  └── PipelineTrace → 前端展示
```

具体实现：
1. `query_engine.py` 的 `_generate()` 改为返回 `(answer_text, usage_dict)`
2. token 提取：`response.raw.usage.prompt_tokens` / `completion_tokens` / `total_tokens`
3. `GenerationTrace` dataclass 扩展字段：`prompt_tokens`、`completion_tokens`、`total_tokens`、`prompt_preview`（前 200 字符）
4. `main.py` 的 `PipelineTrace` 传递 token 数据到前端
5. 前端展示：
   - 管线图 Generation 节点下方显示 `{prompt_tokens}+{completion_tokens} tok`
   - answer-meta 栏显示 `Tokens: 1234+567 = 1801`
   - 流式模式下显示 completion_tokens 估算值

**平替要点**：
- 字节成本归因系统 → 我们仅记录 token 数，未做费用换算（不同模型单价不同）
- 字节带宽费/存储费拆解 → 未实现，MinIO/Milvus 存储成本需另外监控
- token 数据目前仅前端展示，未持久化 → 未来可写入 Langfuse trace 做历史趋势分析

---

## 7. 前端全链路可视化 (Pipeline Visualization)

**字节怎么做**（§8.1 / §4.3.1）：
- 全链路监控：检索效果（准确率/召回率）+ 性能（延迟/QPS）+ 成本（token/存储）三维指标
- 每个 RAG 环节（查询理解→粗筛→中筛→精筛→生成）独立监控
- 字节内部有专门的 RAG 运维 dashboard

**我们怎么做**：

```
┌─────────┐    ┌──────────┐    ┌──────────┐    ┌────────┐    ┌────────┐
│  向量检索 │───▶│ 融合排序  │───▶│ LLM 重排  │───▶│ 大模型  │───▶│  评估   │
│ (Milvus) │    │ (RRF)    │    │ (Top-5)  │    │  生成   │    │  结果   │
│ 10 nodes │    │ 12 nodes │    │ 5 nodes  │    │ 5 nodes │    │ 85% ✅ │
│  45ms     │    │          │    │  230ms   │    │  1200ms │    │        │
└─────────┘    └──────────┘    └──────────┘    └────────┘    └────────┘
      │              │               │               │
      └── 关键词检索 ─┘               │               │
         (ES) 7 nodes                │               │
           23ms                       │               │
```

具体实现：
1. 前端 `buildPipelineSVG(trace)` 函数：从 `PipelineTrace` JSON 构建 5 节点管线图
2. 每个节点：圆形图标 + 步骤名 + 节点数 + 耗时（ms）
3. 连接线：SVG 箭头 + 动画圆点，状态完成后显示绿色
4. 颜色编码：`success`（绿色，正常完成）/ `active`（蓝色，活跃中）/ `warning`（橙色，降级触发）
5. 图例栏：Milvus 结果数、ES 结果数、融合结果数、改写查询数、Token 消耗、模型名称
6. 向量检索节点额外显示「查询改写次数」
7. 管线图在查询完成后自动展开，数据来自后端 `PipelineTrace`（SSE 流式则为最终的 trace 事件）

**平替要点**：
- 字节内部监控 dashboard → 纯前端 CSS+JS（零依赖），可直接嵌入任何页面
- 字节 Prometheus/Grafana → Langfuse 提供部分 LLM 调用追踪，前端管线图提供请求级可视化
- 当前管线图仅展示单次请求，未做聚合统计 → 未来可加「历史平均延迟」对比

---

## 8. 异步评估轮询 (Async Eval Polling)

**字节怎么做**（§2.5.2 / §6.3）：
- 人工评估 + 自动评估（BLEU/ROUGE）结合
- 评估结果反馈至系统优化（调整检索策略、改进 prompt、更新模型）
- 字节有专门的评估平台，支持多维度打分

**我们怎么做**：

```
POST /v1/query 响应（不等待评估）
  │
  ├── 返回 answer + query_id ──▶ 前端展示答案
  │
  └── BackgroundTasks ──▶ AsyncEvaluator.evaluate()
                              │
                              ├── FaithfulnessEvaluator（LlamaIndex）
                              ├── 裁判模型：gpt-4o, temperature=0.0
                              ├── tenacity 重试（3次，指数退避 2s→4s→8s）
                              │
                              ├── passing=True & score >= 0.8 → 静默通过
                              └── 否则 → 写入 data/bad_cases.jsonl
                                          │
                                          └── 同时写入内存缓存 _recent_evals

前端：setInterval 每 1.5s 轮询 GET /v1/evaluation/{query_id}（最多 20 次）
```

具体实现：
1. `evaluator.py`：`AsyncEvaluator.evaluate()` 返回评估 dict（之前返回 None），包含 `eval_score`、`eval_passing`、`eval_feedback`
2. `main.py`：`_evaluate_and_cache()` 将评估结果存入 `_recent_evals: OrderedDict`（最多 200 条）
3. `GET /v1/evaluation/{query_id}`：前端轮询端点，返回评分/通过/反馈
4. `GET /v1/evaluation/stats`：聚合统计端点，读取 `bad_cases.jsonl` 计算总量、通过率、平均分、最近坏例
5. 前端 `pollEvaluation(queryId, container)`：1.5s 间隔轮询，拿到结果后渲染分数徽章（绿=通过/红=未通过）

**平替要点**：
- 字节自研评估平台 → LlamaIndex 的 `FaithfulnessEvaluator`（开源，底层仍是 LLM-as-Judge）
- 字节 BLEU/ROUGE 自动评估 → Faithfulness（忠实度）更聚焦 RAG 防幻觉需求
- 字节人工评估 → 未实现，坏例沉淀后需人工审查 `bad_cases.jsonl`
- 当前评估仅忠实度一维 → 未来可增加 `CorrectnessEvaluator`、`RelevancyEvaluator`

---

## 9. 多粒度上传进度展示

**字节怎么做**（§4.2.2）：
- 离线索引构建：1 亿条数据分阶段并行构建，4 小时完成
- 索引构建过程有完整的进度监控和状态上报

**我们怎么做**：

```
文件拖拽/选择
  │
  ├── 前端校验：扩展名 + 大小（50MB 上限）
  │
  ├── XHR upload.onprogress
  │     ├── 上传百分比
  │     ├── 速度（bytes/s，每 0.5s 更新一次）
  │     └── 已用时间（s）
  │
  └── 服务端响应
        ├── chunk_count（语义分块数量）
        ├── doc_id
        └── duplicate（是否重复）
```

具体实现：
1. 使用 `XMLHttpRequest`（而非 fetch）以支持 `upload.onprogress` 事件
2. 进度卡片：「文件名 + 文件大小」 / 进度条（渐变色） / 上传速度 + 已用时间 + chunk 数量
3. 上传速度：每 0.5s 计算一次 `(loaded_now - loaded_last) / time_delta`
4. 服务端响应后显示 chunk_count（从 `SemanticSplitterNodeParser` 分块结果获取）
5. 重复文档时显示黄色警告卡片

**平替要点**：
- 字节并行索引构建 → 单文件同步上传（当前阶段无需并行，文档量未到亿级）
- 字节进度监控 → 浏览器原生 XHR 进度事件，零额外依赖
- 未来可增强：批量上传 + 队列 + 每个文件的独立进度卡片

---

## 优化效果总结

| 优化项 | 参考字节章节 | 预期效果 | 字节方案 | 本项目平替 |
|--------|------------|----------|---------|-----------|
| 流式输出 | §6.4 | 首 token 延迟 < 1s | 云雀 streaming + ByteLB | OpenAI SSE + FastAPI StreamingResponse |
| 查询改写 | §5.1 | 召回率提升 15-20% | 自研查询理解模块 | LLM prompt-based 改写 (gpt-4o-mini) |
| 热点缓存 | §4.3.1 | 热点延迟降低 90%+ | ByteCache 分布式 | OrderedDict LRU + 未来 Redis |
| 对象存储 | §2.2 | embedding 模型可更换 | 字节云 OSS / HDFS | MinIO (S3 兼容) |
| 重复检测 | §3.1.2 / §4.3.2 | 存储成本降低 10-15% | 向量相似度 ≥95% | SHA256 哈希 + Milvus metadata |
| Token 追踪 | §13 | 成本可视化 | 自研成本归因系统 | OpenAI usage 字段提取 |
| 管线可视化 | §8.1 | 问题定位效率提升 | 内部监控 dashboard | 纯前端 CSS+SVG 动画 |
| 评估轮询 | §2.5.2 / §6.3 | 坏例自动发现 | 自研评估平台 | LlamaIndex FaithfulnessEvaluator |
| 上传进度 | §4.2.2 | 大文件上传体验改善 | 并行索引 + 进度上报 | XHR onprogress + 服务端 chunk 统计 |

---

## 后续可优化的方向

1. **多粒度向量生成**（字节 §4.1.2）：文档级 + 段落级 + 句子级三级向量，粗筛→中筛→精筛
2. **多级索引过滤**（字节 §4.3.1）：Bloom Filter + ANN + 精确余弦相似度三级
3. **模型蒸馏**（字节 §4.1.1）：将大模型蒸馏为轻量模型用于边缘端部署
4. **冷热数据分离**（字节 §4.2.1）：3 个月以上数据迁移至低成本冷存储
5. **查询意图分类**（字节 §5.1）：FAQ 类 / 文档检索类 / 多跳推理类分流处理
6. **检索结果摘要**（字节 §2.4.2）：对长文本结果自动提取关键句，减少生成层输入
7. **Prompt 缓存优化**（字节 §6.4）：相同 system prompt 使用 Anthropic/OpenAI 的 prompt caching 功能
8. **跨地域部署**（字节 §15）：多地域 Milvus 副本 + 就近路由
