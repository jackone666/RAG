# IntelliLens-MCP 面试深度补充 —— 面试官追问"为什么不用X"

> 现有文档覆盖了"做了什么"和"怎么做的"，本文档补充"为什么不用其他方案"以及面试官可能追问的对比知识点。

---

## 1. 限流算法深度对比

### 1.1 现有文档已覆盖
- 滑动窗口 (ZSET + Lua)
- fail-open 策略
- TOCTOU 竞态问题
- 固定窗口 vs 滑动窗口

### 1.2 面试官追问：四种限流算法全面对比

| 算法 | 原理 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|---------|
| **固定窗口** | 时间窗口内计数，超限拒绝 | 实现简单，内存极低 | 边界突发问题（00:59和01:00各发60次，实际2秒120次） | 粗粒度防护，不重要API |
| **滑动窗口日志** | ZSET记录每次请求时间戳，实时计算窗口内数量 | 精确平滑，无边界问题 | 内存较高（每次请求存一条记录） | 精确限流，本项目采用 |
| **滑动窗口计数器** | 将窗口分成N个小格，统计格子内计数 | 内存固定，近似精确 | 实现复杂（需要后台Cron维护格子） | 大流量场景（10000+ QPS） |
| **令牌桶** | 固定速率放入令牌，请求需获取令牌 | 允许突发流量（桶容量），平滑限流 | 需要后台进程放令牌，实现复杂 | API网关、Nginx限流 |
| **漏桶** | 请求进桶，固定速率流出处理 | 绝对平滑，强制匀速 | 不允许任何突发，可能丢弃合理请求 | 流量整形、消息队列 |

### 1.3 面试追问：为什么选滑动窗口日志而不是令牌桶？

**答**：三个原因：

1. **RAG查询流量特征**：用户查询天然不均匀，开会前后可能有突发查询，令牌桶允许桶容量内的突发（合理），但对于恶意爬取，滑动窗口更精确——每个请求的时间戳都精确记录，无法利用桶容量"透支"。

2. **实现复杂度与收益平衡**：令牌桶需要后台进程持续放令牌（或使用Redis定时任务），增加运维复杂度。滑动窗口ZSET实现只用一条Lua脚本，逻辑自包含。

3. **多租户公平性**：滑动窗口按tenant_id独立计数，每个租户独立60秒窗口。令牌桶的"桶容量"参数对不同租户很难统一——大租户可能合理需要30次/分钟的突发，小租户可能只需要5次。

**但令牌桶也有优势**：如果你做API网关级别的限流（如Nginx `limit_req`），令牌桶更适合，因为它允许正常的流量波动（burst参数）。这也是为什么我在fail-open策略中提到"在Nginx层加令牌桶作为二级兜底"。

### 1.4 面试追问：如果QPS很高（10000+），ZSET会有性能问题吗？

**答**：确实会有。每个请求都在ZSET中新增一个member，高QPS下Redis内存和CPU压力大。优化方案：

1. **滑动窗口计数器**：把60秒窗口分成6个10秒格子，只存6个计数。牺牲精度换性能。
2. **分层限流**：Nginx层用令牌桶做粗粒度限流（如1000 QPS/IP），应用层用滑动窗口做精确多租户限流。
3. **Redis Cluster分片**：按tenant_id做hash分片，每个分片独立限流。

---

## 2. 向量数据库选型对比

### 2.1 现有文档已覆盖
- 选了Milvus，理由：标量过滤、多租户友好

### 2.2 面试官追问：Milvus vs Qdrant vs Pinecone vs Weaviate vs pgvector

| 数据库 | 类型 | 过滤能力 | 分布式 | 运维成本 | 适合场景 |
|--------|------|---------|--------|---------|---------|
| **Milvus** | 专用向量库 | MetadataFilters原生 | 天然支持(Proxy/QueryNode/DataNode) | 高（需要etcd/MinIO/Pulsar依赖） | 大型企业，千万级以上向量 |
| **Qdrant** | 专用向量库 | payload filter（类似Milvus） | 原生支持（Raft共识） | 中（单二进制，无外部依赖） | 中型企业，部署简单 |
| **Pinecone** | SaaS云服务 | metadata filter | 自动管理 | 零（托管服务） | 不想运维的团队 |
| **Weaviate** | 向量+图混合 | GraphQL filter | 原生支持 | 中 | 需要知识图谱+向量的场景 |
| **pgvector** | PostgreSQL扩展 | SQL WHERE原生 | 依赖PG主从 | 低（已有PG的企业） | 小规模，已有PG基础设施 |
| **Chroma** | 嵌入式向量库 | metadata filter | 无 | 极低 | 本地开发/POC |

### 2.3 为什么不用pgvector？

三个现实原因：

1. **标量过滤性能**：pgvector的`WHERE tenant_id = 'xxx' ORDER BY embedding <-> query_embedding LIMIT 10`在大数据量下，需要先做向量搜索再做标量过滤（或反过来）。如果先向量搜索，可能搜到其他租户的数据再过滤；如果先标量过滤，可能漏掉向量相似的数据。Milvus的MetadataFilters是在索引阶段就结合了标量过滤。

2. **索引选型受限**：pgvector只支持IVFFlat和HNSW，没有量化索引（如IVF_SQ8、IVF_PQ）。千万级向量下，pgvector的HNSW内存占用远超Milvus。

3. **多租户隔离运维**：pgvector是PG的一个扩展，权限隔离依赖PG的RLS（Row Level Security）。Milvus原生支持多Collection和多Partition，多租户物理隔离更清晰。

**什么时候pgvector更合适**：
- 向量量 < 100万
- 已有PostgreSQL基础设施
- 不需要多租户物理隔离
- 团队不想引入新的数据库

### 2.4 Milvus的部署依赖为什么这么多？

Milvus集群需要etcd（元数据）、MinIO/S3（持久化）、Pulsar/Kafka（消息）。这是**存算分离**架构的代价：

- **Coordinator**管理元数据 → 依赖etcd
- **DataNode**持久化向量 → 依赖MinIO/S3
- **QueryNode**消费数据变更 → 依赖Pulsar/Kafka

**好处**：各组件可以独立扩缩容。查询量大扩容QueryNode，写入量大扩容DataNode，互不影响。

**劣势**：小规模下的运维复杂度高。对于POC或小规模场景，可以用Milvus Standalone模式（单机，无外部依赖），本项目docker-compose用的就是Standalone。

---

## 3. Embedding模型选型对比

### 3.1 现有文档已覆盖
- 选了bge-m3：多语言、1024维、中文友好

### 3.2 面试官追问：常见embedding模型全面对比

| 模型 | 维度 | 最大长度 | 多语言 | 开源 | 中文能力 | 适合场景 |
|------|------|---------|--------|------|---------|---------|
| **BAAI/bge-m3** | 1024 | 8192 token | 强 | ✅ | 优秀 | 中英混合企业文档 |
| **OpenAI text-embedding-3-large** | 256-3072 | 8192 token | 强（100+语言） | ❌ | 好 | 快速接入，不想运维 |
| **OpenAI text-embedding-3-small** | 512-1536 | 8192 token | 强 | ❌ | 好 | 成本敏感 |
| **Cohere Embed v3** | 1024 | 512 token/chunk | 强 | ❌ | 好 | 英文为主，需要分类 |
| **Jina embeddings v3** | 1024 | 8192 token | 强 | ✅ | 好 | 长文档、多语言 |
| **m3e-large** | 1024 | 512 token | 弱（中文为主） | ✅ | 优秀 | 纯中文场景 |
| **stella-base-zh-v3-1792d** | 1792 | 512 token | 弱 | ✅ | 优秀 | 中文高精度 |
| **gte-Qwen2-7B-instruct** | 3584 | 32K token | 中 | ✅ | 优秀 | 需要LLM级别语义理解 |

### 3.3 面试追问：为什么不用OpenAI的embedding？

**答**：三个考量：

1. **数据出境与合规**：bge-m3开源可本地部署，企业数据不出境。OpenAI embedding必须调用API，数据会离开企业网络。对于金融、政务等合规要求高的场景，这是硬约束。

2. **成本**：1000万chunk × 1024维 × $0.13/1M token（OpenAI）vs 本地GPU推理成本。大规模场景下，开源的边际成本趋近于零。

3. **可迁移性**：如果后续需要换云厂商或私有化部署，bge-m3可以直接换环境，OpenAI embedding绑定了供应商。

**但OpenAI embedding也有优势**：接入快（不需要GPU）、维度可调（text-embedding-3-large可以降到256维节省存储）、多语言覆盖更广。

### 3.4 面试追问：1024维够吗？是不是维度越高越好？

**答**：不是。维度选择需要权衡：

- **高维（1024-3072）**：语义表达能力更强，能区分更细微的语义差异。代价：存储空间大、检索慢（维度灾难）、内存占用大。
- **低维（256-512）**：检索快、存储省、内存低。代价：语义表达能力稍弱，可能丢失小众领域的细节。

**选择原则**：
- 通用知识库（政策文档、合同、产品说明）→ 1024维足够
- 高精度场景（法律判例、医疗文献、专利）→ 1536-3072维
- 大规模低成本（亿级向量）→ 256-512维

bge-m3的1024维是在"表达力"和"检索效率"之间的平衡点，对企业知识库来说非常合适。

### 3.5 面试追问：embedding模型需要微调吗？

**答**：取决于场景：

- **不需要微调**：通用企业文档（合同、制度、FAQ），bge-m3预训练已经覆盖得很好。
- **需要微调**：领域黑话特别多（如医学、法律、芯片设计），通用embedding可能不理解领域术语的语义关系。

微调方案：
1. **领域数据对**：(query, positive_doc, negative_doc) → 对比学习
2. **SimCSE / Contriever**：无监督或弱监督微调
3. **LoRA微调bge-m3**：只训练少量参数，成本低

---

## 4. Rerank方法对比

### 4.1 现有文档已覆盖
- 选了bge-reranker-v2-m3
- 召回多捞鱼，重排挑好鱼

### 4.2 面试官追问：三种Rerank方法对比

| 方法 | 原理 | 延迟 | 精度 | 成本 | 适用场景 |
|------|------|------|------|------|---------|
| **Cross-encoder (bge-reranker)** | query-document对同时输入，联合编码打分 | 中（数十ms/chunk） | 高 | 低（开源本地） | 通用Rerank |
| **LLM Pointwise Rerank** | LLM逐条判断"文档是否相关" | 高（秒级/chunk） | 很高 | 高（API调用费） | 候选<20且精度要求高 |
| **LLM Listwise Rerank** | LLM一次比较所有文档，输出排序 | 很高（秒级） | 最高 | 高 | 候选<10且需要全局排序 |
| **Bi-encoder (向量相似度)** | 检索阶段已经用了，rerank不会再用 | 极低 | 最低 | 极低 | 检索阶段 |

### 4.3 面试追问：为什么不用LLM做rerank，效果应该更好？

**答**：这是**效率与精度的权衡**：

1. **延迟成本**：检索Top-20，LLM pointwise需要20次LLM调用，即使并发也可能需要2-3秒。bge-reranker一次API调用处理20个候选，~100ms。
2. **经济成本**：如果每次查询都用LLM rerank，每月10万次查询就是200万次LLM调用。用bge-reranker是本地模型推理，成本趋近于零。
3. **两级设计**：bge-reranker是"日常精排"，LLM rerank可以作为"高价值场景"的备用。比如用户点击"不满意"后，用LLM重新rerank并评估。

**什么场景值得用LLM rerank**：法律文书搜索、金融研报查询——用户对精度要求极高，且查询量不大（日几百次）。

### 4.4 面试追问：Cross-encoder和Bi-encoder的本质区别？

```
Bi-encoder (embedding模型):
  Query → [Encoder] → vec_q ─┐
                               ├─ cosine_similarity → score
  Doc   → [Encoder] → vec_d ─┘
  
  特点：Query和Doc独立编码，向量可预计算和索引
  适用：大规模检索（百万级以上）

Cross-encoder (reranker模型):
  [Query, Doc] → [联合Encoder] → score
  
  特点：Query和Doc同时输入，交叉注意力机制能看到交互
  优势：精度更高（因为能看到query和doc之间的精确匹配）
  劣势：无法预计算，每个query-doc对都要重新推理
  适用：小规模精排（Top-20~50）
```

**面试一句话**：Bi-encoder负责"从百万中找到可能相关的一百个"，Cross-encoder负责"从一百个中挑出最相关的5个"。

---

## 5. RAG框架选型对比

### 5.1 现有文档已覆盖
- LlamaIndex vs LangChain（MetadataFilters、Langfuse集成、评估器）

### 5.2 面试官追问：为什么不用LlamaIndex之外的选择？

| 框架 | 定位 | 检索 | 生成 | 评估 | 多租户 | 适合场景 |
|------|------|------|------|------|--------|---------|
| **LlamaIndex** | RAG数据框架 | 丰富（向量/关键词/融合/递归） | LLM抽象 | 内置FaithfulnessEvaluator | MetadataFilters原生 | 企业RAG，本项目 |
| **LangChain** | LLM应用框架 | LCEL Runnable抽象 | 丰富（Chain/Agent） | 需额外集成 | 需手动拼装filter | 复杂Agent编排 |
| **Haystack** | NLP Pipeline | Pipeline+Node架构 | 丰富 | 有EvalNode | 需自定义 | 需要可视化的场景 |
| **DSPy** | 编程式Prompt优化 | 基础的RM | 模块化（Signature/Optimizer） | 自动优化 | 需自定义 | 需要自动优化Prompt |
| **直接写（纯SDK）** | 最大灵活性 | 完全自由 | 完全自由 | 完全自由 | 完全自由 | 有特殊需求，不想被框架约束 |

### 5.3 面试追问：既然LlamaIndex社区小，为什么还选它？

**答**：这是一个**当前收益 vs 长期风险**的权衡：

**当前收益明显**：
- MetadataFilters一行代码解决多租户过滤，LangChain需要自己封装Milvus的filter表达式
- Langfuse集成官方维护，兼容性有保证
- 内置评估器减少开发时间

**长期风险可控**：
- 核心检索逻辑（`TenantAwareQueryFusionRetriever`）是和框架解耦的，未来可以迁移
- `query_engine.py`中的生成逻辑也不依赖LlamaIndex的特定抽象
- 如果LlamaIndex掉队，迁移成本主要是retriever的适配，约1-2周工作量

### 5.4 面试追问：什么场景应该选LangChain？

- 需要复杂Agent编排（多步推理、工具调用、记忆管理）
- 需要和大量第三方工具集成（LangChain有更大的插件生态）
- 团队已经熟悉LangChain

**本项目如果后续要加入复杂Agent**（如自动检索→判断→追问→再检索），可能会在LlamaIndex之外引入LangChain的Agent模块，两者混用。这也是我在Q1追问中提到的。

---

## 6. 缓存策略深度对比

### 6.1 现有文档已覆盖
- TTLCache（本地缓存）
- ByteCache（Redis缓存）
- 细粒度失效（反向索引）
- 热点追踪

### 6.2 面试官追问：缓存策略全面对比

| 策略 | 实现 | 命中率 | 一致性 | 内存成本 | 适用场景 |
|------|------|--------|--------|---------|---------|
| **TTLCache（本地）** | Python `cachetools.TTLCache` | 低（单进程） | 弱（多进程不一致） | 低 | 非关键数据、配置缓存 |
| **Redis Cache（集中）** | Redis GET/SET | 中（多进程共享） | 中（集中管理） | 中 | 分布式缓存、ByteCache |
| **Cache-Aside** | 应用手动管理缓存 | 高 | 中 | 可控 | 需要精确控制缓存逻辑 |
| **Write-Through** | 写入时同步更新缓存 | 高 | 强 | 高 | 读多写少 |
| **Write-Behind** | 异步写入缓存 | 高 | 弱 | 高 | 写入量大，可以容忍短暂不一致 |
| **Read-Through** | 缓存层自动加载 | 高 | 中 | 中 | 缓存层封装数据源访问 |

### 6.3 面试追问：为什么ByteCache用Cache-Aside而不是Read-Through？

**答**：Read-Through意味着缓存层需要知道如何从Milvus加载数据——这违反了单一职责原则。Cache-Aside让应用层明确控制"什么时候查缓存、什么时候查Milvus、什么时候回写缓存"。

### 6.4 面试追问：缓存击穿（Hotspot Invalid）、缓存穿透（Penetration）、缓存雪崩（Avalanche）怎么处理？

这是缓存三大经典问题，面试官一定听过：

| 问题 | 现象 | 本项目处理 |
|------|------|----------|
| **缓存击穿** | 热点key过期，大量请求同时打到后端 | ByteCache用热点追踪ZSET延长热点key的TTL；TTLCache对高频key不会被轻易驱逐 |
| **缓存穿透** | 查询不存在的数据，每次都穿透到后端 | 当前未处理。生产方案：对空结果也缓存（短TTL），或用Bloom Filter预先判断 |
| **缓存雪崩** | 大量key同时过期，后端瞬间压力 | 当前未处理。生产方案：TTL加随机偏移（如5分钟±30秒），避免同时过期 |

**面试时主动提到这三点**，比只谈"有缓存"有深度得多。

### 6.5 为什么不在检索阶段用Redis缓存完整查询结果而用了ByteCache（按embedding hash）？

**答**：这是一个精妙的设计选择：

- **按原始query缓存**：命中率低（用户很少问一模一样的句子），且缓存粒度太粗（无法复用）。
- **按embedding hash缓存**：语义相似的不同问法（"什么是PE-TTM"和"动态市盈率是什么"）有相似的embedding，hash碰撞概率高 → 命中率高。同时粒度细，一个缓存结果可以被多个相似query复用。

---

## 7. RAG评估框架对比

### 7.1 现有文档已覆盖
- FaithfulnessEvaluator (LlamaIndex内置)
- 检索指标（precision/recall/MRR/hit_rate）
- 生成指标（faithfulness/relevance）

### 7.2 面试官追问：为什么自己写评估而不用RAGAS？

**答**：事实上应该提一下RAGAS，这是目前RAG评估的标准框架：

| 框架 | 指标 | 优点 | 缺点 |
|------|------|------|------|
| **RAGAS** | Faithfulness, AnswerRelevancy, ContextPrecision, ContextRecall, ContextRelevance 等 | 社区标准，指标全面，有学术论文支持 | 每个指标都需要LLM调用，成本较高 |
| **DeepEval** | 类似RAGAS + 自定义 | 有Web UI，支持CI/CD集成 | 商业化较强 |
| **TruLens** | RAG Triad（Answer Relevance, Context Relevance, Groundedness） | 可视化好 | 重，依赖较多 |
| **LlamaIndex Evaluator** | Faithfulness, Relevance, Correctness | 轻量，和框架集成好 | 指标较少 |
| **自建评估（本项目）** | precision/recall/MRR/hit_rate/faithfulness/relevance | 完全可控，定制化强 | 需要自己维护 |

**答**：选LlamaIndex内置评估 + 自建检索指标的组合，原因是：

1. **不想引入额外依赖**：RAGAS本身也依赖LangChain或LlamaIndex，引入RAGAS后链路变长。
2. **检索指标不需要LLM**：precision/recall/MRR/hit_rate是纯计算，不需要LLM judge。RAGAS的ContextPrecision/Recall需要LLM调用，成本高且增加延迟。
3. **生成指标够用**：LlamaIndex的FaithfulnessEvaluator和RAGAS的Faithfulness本质相同（都是LLM判断"回答是否基于上下文"），不需要重复引入。

**如果面试官追问**："那你什么时候会用RAGAS？"
- 需要更细粒度的指标（如ContextEntityRecall、SemanticSimilarity）
- 需要和社区benchmark对比
- 多个项目需要统一的评估标准

---

## 8. 多租户架构模式深度对比

### 8.1 现有文档已覆盖
- 五层纵深防御
- tenant_id下推到存储层

### 8.2 面试官追问：SaaS多租户三大数据隔离模式

| 模式 | 隔离方式 | 安全性 | 成本 | 扩展性 | 适用场景 |
|------|---------|--------|------|--------|---------|
| **Shared DB + Shared Schema** | tenant_id列过滤 | 低（依赖应用层过滤） | 最低 | 最好（共享资源） | 中小企业SaaS，租户间信任度高 |
| **Shared DB + Separate Schema** | 每个租户独立Schema | 中（数据库级隔离） | 中 | 中（Schema数量有上限） | 中型企业，需要更强隔离 |
| **DB per Tenant** | 每个租户独立数据库 | 最高（物理隔离） | 最高（资源浪费） | 差（数据库数量有限） | 金融/医疗等合规要求严格 |

### 8.3 本项目属于哪种？

**混合模式**：

- **Milvus**：Shared Collection + tenant_id Partition Key → 物理隔离（不同分区）+ 共享资源
- **Elasticsearch**：Shared Index + tenant_id filter → 逻辑隔离（同模式）+ 共享资源
- **MinIO**：Shared Bucket + tenant_id目录 → 逻辑隔离 + 共享存储

**选择理由**：
- 纯Shared Schema对本项目不够安全（企业客户对数据泄露零容忍）
- 纯DB per Tenant太贵（每个租户一个Milvus Collection管理成本极高）
- 混合方案：Milvus的Partition Key提供了接近物理隔离的安全性，同时保持运营效率

### 8.4 面试追问：PostgreSQL的RLS（Row Level Security）能替代你的多租户方案吗？

**答**：RLS只能解决PG这一层的问题，但RAG系统的数据在多个存储中（Milvus向量、ES索引、MinIO原文、Redis缓存），PG只占一小部分（用户元数据、评估结果）。

如果全用PG（pgvector + RLS），可以统一权限控制，但pgvector在大规模下的性能瓶颈（见第2节）。RAG系统需要专用向量库，而专用向量库的权限模型各不相同（Milvus没有内置RBAC），所以需要在应用层统一封装多租户逻辑。

---

## 9. LLM降级与韧性模式深度对比

### 9.1 现有文档已覆盖
- 主备模型降级（primary → fallback → 降级文案）
- 多Provider异构降级

### 9.2 面试官追问：为什么不用更成熟的韧性模式？

| 模式 | 原理 | 本项目实现 | 差距 |
|------|------|----------|------|
| **Retry with Backoff** | 失败后等待递增时间重试 | 评估用了tenacity重试，生成未用 | 生成层可以加重试（快速重试1次） |
| **Circuit Breaker** | 连续失败N次后直接熔断，定时探测恢复 | 未实现 | 如果OpenAI持续故障，每个请求都要等超时才知道失败 → 应该快速熔断 |
| **Bulkhead** | 隔离不同资源池，一个池耗尽不影响其他 | 未实现 | 查询和评估共享同一LLM连接池，评估过多可能影响查询 |
| **Timeout** | 设置超时时间 | 有（HTTP客户端timeout） | 应该对每个阶段分别设置超时 |
| **Fallback** | 主失败切备 | 已实现 | ✅ |
| **Graceful Degradation** | 部分功能失败，核心功能仍可用 | 部分实现（ES失败不影响向量检索） | ✅ |

### 9.3 什么是Circuit Breaker？怎么实现？

```
Circuit Breaker 三态模型：

   ┌─────────┐
   │ CLOSED  │ ← 正常状态，请求通过
   └────┬────┘
        │ 连续失败 > 阈值
        ▼
   ┌─────────┐
   │  OPEN   │ ← 熔断状态，直接拒绝请求
   └────┬────┘
        │ 等待 timeout 后
        ▼
   ┌─────────────┐
   │ HALF-OPEN   │ ← 探测状态，允许少量请求通过
   └────┬────────┘
        │ 成功 → CLOSED
        │ 失败 → OPEN
```

**在生产中的价值**：
- 如果OpenAI API挂了，没有Circuit Breaker → 每个RAG请求都要等5秒超时才知道失败
- 有Circuit Breaker → 连续3次失败后立即熔断，后续请求直接走fallback（0延迟）
- 每30秒探测一次（half-open），服务恢复后自动切回

**面试一句话**：我在代码里实现了fallback，生产应该再加Circuit Breaker，避免故障放大。

### 9.4 面试追问：Retry with Backoff的坑你知道吗？

**答**：三个常见坑：

1. **Thundering Herd**：如果一个热点请求失败，大量重试同时发起，可能压垮恢复后的服务。→ 加随机抖动（Jitter）。
2. **Retry Storm**：多层服务都加重试，故障层层放大（如API Gateway重试3次 → 应用层重试3次 → SDK重试3次 = 27次调用）。→ 只在最外层重试，内层让异常上浮。
3. **非幂等操作重试**：如创建资源的请求重试可能导致重复创建。→ 用幂等key（如基于content_hash去重）。

---

## 10. 查询改写增强：HyDE的局限性

### 10.1 现有文档已覆盖
- HyDE原理和实现
- 查询分解、术语归一化

### 10.2 面试官追问：HyDE有什么缺点？什么时候不该用？

**答**：

**缺点**：
1. **增加延迟**：HyDE需要一次LLM调用生成假设文档（~200-500ms），对延迟敏感的场景不友好。
2. **假设文档质量不稳定**：如果LLM对领域不熟悉，生成的假设文档可能误导检索。
3. **token成本**：每次查询多一次LLM调用。
4. **可能放大偏见**：如果LLM在某些领域有知识盲区，假设文档会系统性偏差。

**什么时候不该用HyDE**：
- 用户query已经很具体（如"合同编号CN-2024-0883的签署日期"）→ 不需要假设
- 延迟要求极高（<200ms） → 加一次LLM调用不可接受
- query本身就是文档中的原文 → 直接检索即可
- 成本极其敏感（如免费用户） → 额外的LLM调用是浪费

**什么时候HyDE效果最好**：
- query很短且抽象（"什么是股权激励"）
- 用户query和文档表达风格差异大（口语 vs 正式公文）
- 领域特殊，通用embedding可能理解不透

### 10.3 面试追问：除了HyDE，还有哪些查询增强手段？

| 手段 | 原理 | 延迟 | 适用 |
|------|------|------|------|
| **Query2Doc** | 类似HyDE，生成伪文档 | 高 | 短查询 |
| **Step-Back Prompting** | 先生成更抽象/更具体的回退问题 | 中 | 需要背景知识的复杂问题 |
| **Multi-Query** | 生成多个等价查询 | 中 | 覆盖面广的需求 |
| **Query Routing** | 根据意图分类到不同检索策略 | 低 | 异构知识库 |
| **Query Expansion（术语归一化）** | 基于词典扩展 | 极低 | 领域术语 |

本项目已实现：HyDE + Multi-Query（_expand）+ Query Expansion（术语归一化）+ Query Routing（classify意图分类）。

---

## 11. 分块策略深度对比

### 11.1 现有文档已覆盖
- SemanticSplitter语义分块
- 超长chunk递归拆分

### 11.2 面试官追问：分块策略全面对比

| 策略 | 原理 | 优点 | 缺点 | 适用 |
|------|------|------|------|------|
| **固定长度** | 每N个字符切一块 | 简单、快 | 句子被切断，语义不完整 | 快速POC |
| **固定长度+重叠** | 每N字符切，相邻块重叠M字符 | 减少边界信息丢失 | 冗余存储，重叠部分被多块保留 | 一般场景 |
| **语义分块（SemanticSplitter）** | 按embedding相似度断点 | 保持段落完整性 | 慢（需要计算embedding）、chunk大小不均 | 质量优先（本项目） |
| **递归分块** | 按分隔符层级递归切（段落→句子→词） | 灵活可控 | 逻辑复杂 | 结构化文档 |
| **句子窗口** | 每个句子为一个chunk，检索时取前后N句 | 精确 | 需要二次检索（检索句子→取窗口） | 需要精确引用 |
| **Markdown/HTML感知** | 按标题层级切分 | 保持文档结构 | 强依赖格式 | 格式化文档 |
| **Agentic Chunking** | LLM判断断点 | 最智能 | 极慢、极贵 | 高价值少量文档 |

### 11.3 面试追问：Sentence Window（句子窗口检索）是什么？为什么没用？

**原理**：
1. 把文档按句子切分，每个句子一个chunk
2. 检索时匹配到某个句子，返回该句子+前后各N句作为上下文
3. 检索精度高（句子级匹配），上下文完整（窗口展开）

**为什么没用**：
- 实现复杂：需要"检索句子→映射到原文→展开窗口"，比直接检索chunk多一步
- 配合rerank和上下文压缩后，chunk级检索的效果已经不错
- 适合精确引用场景（法律条款），对于企业FAQ类查询收益不大

### 11.4 面试追问：Agentic Chunking是什么？

Agentic Chunking是用LLM来决定"哪里应该断"。流程：
1. LLM读取文档
2. LLM标记"这里是一个语义断点"
3. 按标记切分

优点：分块质量极高（LLM理解上下文）。缺点：太慢（读一遍文档就需要一次LLM调用）、太贵（长篇文档token消耗大）。适合年报、招股书等高价值文档，不适合日常文档。

---

## 12. MCP vs 其他Agent接入方式

### 12.1 现有文档已覆盖
- MCP工具暴露
- Claude Desktop原生支持

### 12.2 面试官追问：MCP vs Function Calling vs REST API vs gRPC？

| 方式 | 标准化 | 客户端支持 | 复杂度 | 适用 |
|------|--------|----------|--------|------|
| **MCP** | Anthropic定义的标准协议 | Claude Desktop原生，其他框架逐渐支持 | 中 | 和Claude生态集成 |
| **OpenAI Function Calling** | OpenAI定义 | OpenAI SDK原生 | 低 | 和OpenAI生态集成 |
| **LangChain Tool** | LangChain抽象 | LangChain框架内 | 低 | LangChain应用内 |
| **REST API** | 通用HTTP标准 | 所有语言/框架 | 极低 | 通用集成 |
| **gRPC** | Google标准 | 强类型语言 | 高 | 内部微服务高性能通信 |

### 12.3 面试追问：MCP比Function Calling好在哪里？

**答**：MCP解决的不仅是"怎么调用工具"，而是"Agent怎么发现和管理工具"：

1. **工具发现**：MCP客户端可以列出服务端提供的所有工具（`list_tools`），Function Calling需要手动在代码中注册每个工具的定义。
2. **双向通信**：MCP支持Server→Client的资源暴露和Prompt模板，Function Calling只支持单向调用。
3. **标准化传输**：MCP定义了Streamable HTTP和stdio两种传输方式，Function Calling绑定在OpenAI的API格式上。

**但Function Calling也有优势**：生态更大，几乎所有LLM Provider都支持类似格式。MCP相对较新，支持的客户端有限。

### 12.4 为什么项目同时提供REST API和MCP？

**答**：面向不同消费者：

- **REST API**：给业务系统的后端调用（如内部OA系统、客服系统）——开发者熟悉、调试方便、有OpenAPI文档
- **MCP**：给AI Agent调用（如Claude Desktop、Cursor）——Agent自动发现、标准化tool schema、无需人工读文档

两类消费者未来可能都存在：业务系统通过API查询知识库，Agent通过MCP在对话中自动检索。

---

## 13. 异步任务处理：BackgroundTasks vs 专业队列

### 13.1 现有文档已覆盖
- BackgroundTasks异步评估

### 13.2 面试官追问：什么时候BackgroundTasks不够用？

| 方案 | 可靠性 | 持久化 | 重试 | 监控 | 适用 |
|------|--------|--------|------|------|------|
| **FastAPI BackgroundTasks** | 低（进程重启丢失） | 无 | 需自己写 | 无 | 轻量、不重要任务 |
| **Celery** | 中 | 有（Redis/RabbitMQ Broker） | 原生支持 | 有（Flower） | Python标准方案 |
| **RQ** | 中 | 有（Redis） | 有限 | 有限 | 轻量Python队列 |
| **Kafka/RabbitMQ** | 高 | 有（磁盘） | 灵活 | 丰富 | 大规模、多语言 |
| **Redis Queue** | 低 | 有（Redis） | 需自己写 | 需自己写 | 简单队列 |

### 13.3 为什么本项目选BackgroundTasks？

**答**：**有意识的权衡**：

1. **评估是best-effort**：丢几个评估样本不影响业务正确性，所以不需要消息队列的可靠性保证。
2. **项目定位**：展示架构设计能力，不是生产系统。引入Celery会增加部署复杂度（需要Broker + Worker + Result Backend）。
3. **渐进式演进**：当前代码中，`BackgroundTasks`→`asyncio.create_task`→`Redis DLQ`是逐级升级的路径。先跑通逻辑，再生产加固。

**面试一句话**：评估是离线质量监控，不是在线事务。BackgroundTasks对当前规模足够了，生产改成Celery或Kafka也就一两天的工作量。

---

## 14. 检索融合算法对比

### 14.1 现有文档已覆盖
- weighted RRF融合

### 14.2 面试追问：为什么用RRF而不是其他融合方法？

| 融合方法 | 原理 | 优点 | 缺点 |
|----------|------|------|------|
| **RRF** | `score = Σ 1/(k+rank_i)` | 不需要归一化分数，跨检索器可直接融合 | 只用了排名信息，丢失了原始分数 |
| **CombSum** | `score = Σ norm_score_i` | 保留了原始相关性分数 | 不同检索器的分数不可直接相加（需要归一化，且归一化方法影响结果） |
| **CombMNZ** | `score = Σ norm_score_i × 命中次数` | 惩罚只被一个检索器找到的文档 | 和CombSum同样需要归一化 |
| **学习融合** | 训练一个小模型预测融合分数 | 最优（数据驱动） | 需要标注数据、有冷启动问题 |

### 14.3 为什么不直接用CombSum（分数加和）？

**答**：核心问题是**向量相似度分和BM25分不在同一个量纲上**：

- 向量相似度：通常在0.7-1.0之间
- BM25分数：可能在0-50之间，取决于文档长度和词频

如果直接相加，需要归一化，而归一化方法（min-max/z-score）对异常值敏感。RRF只依赖排名，天然避免了量纲问题。

但RRF也有缺点：忽略了原始分数的差异。如向量检索中#1和#2的分数可能差距很大（0.95 vs 0.75），但RRF只看排名（1 vs 2），丢失了这种信息。这也是我在"可演进方向"中提"动态融合权重"的原因——通过query类型动态调整，补偿RRF的信息损失。

### 14.4 为什么 k=60？

RRF公式中的k是平滑参数，防止`1/(k+rank)`在rank很大时变化太小。k=60是经验值：
- 太小（k=1）：rank=1 vs rank=2差异巨大，对排名过于敏感
- 太大（k=1000）：所有rank的分数几乎一样，排名失去意义
- k=60是学术界常用值（由Cormack et al. 2009提出）

---

## 15. 检索模式对比：Dense vs Sparse vs Hybrid

### 15.1 面试官追问：你知道SPLADE和ColBERT吗？

| 方法 | 类型 | 原理 | 优点 | 缺点 |
|------|------|------|------|------|
| **Dense (bge-m3)** | 稠密向量 | embedding → cosine similarity | 语义理解强 | 对精确token弱 |
| **Sparse (BM25)** | 稀疏向量 | 词频+逆文档频率 | 精确匹配强 | 不懂语义 |
| **SPLADE** | 学习型稀疏 | 用神经网络学习稀疏向量，每个token有学习后的权重 | 兼顾语义和精确 | 模型大、推理慢 |
| **ColBERT** | 延迟交互 | Query和Doc的每个token都编码，用MaxSim计算相似度 | 精度极高 | 存储大（每个token都要存向量） |
| **Hybrid (本项目)** | 混合 | Dense + Sparse → RRF | 平衡 | 两路维护成本 |

### 15.2 面试追问：为什么不用ColBERT？据说效果很好。

**答**：
1. **存储成本巨大**：ColBERT需要存储每个文档中每个token的向量（而不是每个chunk一个向量）。一个1000 token的chunk需要存储1000个向量，存储成本是传统方法的1000倍。
2. **检索复杂度高**：MaxSim需要query的每个token和文档的每个token做相似度计算，不能直接用向量索引加速。
3. **收益不够明确**：ColBERT在BEIR基准上确实好，但在企业知识库场景（混合检索+rerank+上下文压缩）下，增量收益有限。

**一句话**：ColBERT适合学术基准刷榜，Hybrid更适合工程落地。

---

## 16. 安全补充：OAuth2/OIDC vs 自建JWT

### 16.1 现有文档已覆盖
- JWT认证
- RBAC角色

### 16.2 面试官追问：为什么自建JWT而不用OAuth2/OIDC？

**答**：

| 方案 | 复杂度 | 安全性 | SSO | 适用 |
|------|--------|--------|-----|------|
| **自建JWT** | 低 | 中（取决于实现） | 无 | POC/小规模 |
| **OAuth2 + OIDC** | 高 | 高（标准化流程） | 天然支持 | 企业级SSO |
| **Keycloak** | 中 | 高 | 开箱即用 | 中型企业 |
| **Auth0/Okta** | 低（集成） | 极高 | 丰富 | 不想自己运维 |

**自建JWT的问题**：
- 没有Token撤销机制（只能在Q7中说的加黑名单补救）
- 没有PKCE（Proof Key for Code Exchange）防CSRF
- 没有OIDC的id_token（身份信息标准化）

**生产改进**：
- 接入企业已有的SSO（OIDC Provider）
- 应用层只验证JWT签名和过期时间
- 角色的`groups`字段从ID Token中提取

---

## 17. 可观测性补充：Langfuse vs 其他方案

### 17.1 面试官追问：为什么不自己记录日志而要用Langfuse？

| 方案 | 优点 | 缺点 |
|------|------|------|
| **自己写日志** | 完全可控 | 需要自己建索引、可视化、告警 |
| **Langfuse** | RAG专用、LlamaIndex原生集成、开源可自部署 | 多一个依赖 |
| **LangSmith** | LangChain官方 | 绑定LangChain生态，商业化 |
| **MLflow** | 通用ML实验追踪 | 不是RAG专用，需要自定义 |
| **Weights & Biases** | 强大的实验对比 | 贵，学术友好 |
| **Phoenix (Arize)** | OpenTelemetry原生 | 较新，社区小 |

### 17.2 Langfuse的核心价值

Langfuse不仅记录日志，还提供：
- **Trace/Span树结构**：可以看到一次查询中每个子步骤的时间分布
- **评分系统**：可以给一次trace打faithfulness/relevance分
- **数据集**：可以把trace转为评估数据集
- **Prompt管理**：可以托管和版本化Prompt

---

## 18. 补充：面试官可能问到的其他技术点

### 18.1 为什么不用GraphRAG？

GraphRAG（微软）是通过知识图谱增强RAG的方法，适合需要跨文档推理（如"A公司和B公司有什么战略合作"需要多个文档的信息拼接）。

**本项目的基础RAG已经通过上下文压缩和多文档引用支持了一定程度的跨文档推理。GraphRAG的收益在全知全能型问答场景，但代价很高（建图成本、查询复杂度），对于企业FAQ和政策查询类场景，ROI不高。**

### 18.2 Embedding维度为什么影响检索速度？

这是一个常被追问的基础知识：

- **暴力搜索**：O(D×N)，D是维度，N是向量数。维度增加直接增加搜索时间。
- **近似搜索（ANN）**：如HNSW或IVF，维度的影响是非线性的。高维空间中"最近邻"的概念变弱（维度灾难），需要更多的候选来保证召回。
- **存储**：1024维×float32×N = 4KB/N向量。百万向量=4GB存储。如果用3072维=12GB。

### 18.3 你的系统数据库是怎么做迁移的？

当前代码假定Collection已创建，没有Migration。生产需要：
1. **Alembic**管理PG的Schema（表结构、索引）
2. **Milvus Collection Schema**通过启动脚本管理（`create_collection` if not exists）
3. **Elasticsearch Index Template**通过启动脚本管理

### 18.4 如果embedding模型升级了（bge-m3 → bge-m4），存量的向量怎么处理？

这是RAG领域的一个经典运维问题：

1. **双写**：新旧模型同时embedding，写入不同Collection
2. **双读**：检索时同时查新旧Collection，用新模型结果为主
3. **渐进迁移**：后台任务逐步给存量文档用新模型重新embedding
4. **切换**：全部存量迁移完成后，切到新Collection

成本：迁移期间存储翻倍、检索延迟稍增。周期：百万级文档约1-2天。

### 18.5 面试反问（轮到问面试官时）

当面试官问"你有什么想问我们的？"时，可以问：

1. "团队目前在RAG方面最大的技术挑战是什么？是召回率、幻觉、延迟还是成本？"
2. "目前的RAG架构是自建还是基于某个框架？评估是怎么做的？"
3. "对于我做的这种多租户企业RAG，团队面临过类似的需求场景吗？"

这些问题能展示你对RAG的系统性思考和对实际问题的关注。

---

## 快速记忆卡

### 限流算法口诀
```
固定窗口有边界，滑动窗口最精确
令牌桶能抗突发，漏桶强制要匀速
本项目选滑动窗，Lua原子防竞态
大流量改计数器，Nginx兜底令牌桶
```

### 向量库口诀
```
Milvus强在大规模，存算分离好扩容
Qdrant轻量无依赖，中小场景最省心
pgvector省事但不扛量，百万以内可以考虑
Pinecone托管最省心，就是数据要上云
```

### 缓存三兄弟
```
击穿：热点过期一起打，热点追踪延TTL
穿透：查无此人也来问，空值也存短TTL
雪崩：大家一起到期了，TTL加随机偏移
```

### RAG框架一句话
```
LlamaIndex：数据优先，检索生成评估一条龙
LangChain：Agent优先，复杂编排和工具集成
Haystack：Pipeline优先，可视化和节点编排
DSPy：优化优先，自动调Prompt和权重
```
