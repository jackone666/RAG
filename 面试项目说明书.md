# IntelliLens-MCP 企业级智能数据治理与 Agentic RAG 系统

## 面试项目说明书 v2.1

---

## 一、项目定位

**IntelliLens-MCP** 是一个面向企业级场景的智能知识检索与问答系统。核心解决三个痛点：

1. **大模型幻觉**：RAG 检索增强生成 + 独立裁判模型自动检测幻觉，将不可靠回答沉淀为坏例
2. **多租户数据安全**：从存储引擎层到 API 层的五级权限隔离，杜绝租户间数据泄露
3. **高可用与熔断**：主备模型自动切换 + Redis 分布式限流，面对大模型 API 故障优雅降级

**一句话总结**：用 RAG + MCP 协议构建的带五道防线的企业级知识问答中台。

---

## 二、技术栈与选型理由

| 组件 | 选型 | 理由 |
|---|---|---|
| RAG 框架 | LlamaIndex | 官方的 MetadataFilters、CallbackManager、FaithfulnessEvaluator 开箱即用，比 LangChain 更适合多租户检索场景 |
| 向量数据库 | Milvus | 原生支持标量过滤（tenant_id 下推），2.4+ 支持 GPU 索引加速 |
| LLM 网关 | OpenAI API（兼容协议） | 统一接口，主模型 gpt-4o / 备用 gpt-4o-mini，也可对接 vLLM 私有部署 |
| 限流 | Redis Sorted Set + Lua 原子脚本 | 分布式共享状态，多 worker 进程一致，ZSET 天然映射到滑动窗口 |
| MCP 协议 | FastMCP (官方 SDK) | 让 Claude Desktop 等外部客户端以标准化方式调用企业 RAG 工具 |
| 可观测性 | Langfuse | LlamaIndex 官方集成，记录每次检索耗时、Prompt、Token 消耗 |
| Web 框架 | FastAPI + Uvicorn | 原生 async/await，BackgroundTasks 支持异步评估解耦 |
| 重试框架 | Tenacity | 指数退避重试（2s→4s→8s），配合作者模型/裁判模型限流 |

---

## 三、系统架构

```
                          ┌─────────────────────────────┐
                          │    Claude Desktop / Agent     │
                          │    (MCP Client)               │
                          └─────────────┬───────────────┘
                                        │ MCP Protocol
                                        ▼
┌──────────────────────────────────────────────────────────────┐
│                     FastAPI (Uvicorn)                         │
│  ┌──────────────────────────────────────────────────────────┐│
│  │               HTTP Middleware Pipeline                    ││
│  │  Request → Auth(JWT) → RateLimit(Redis) → Route Handler  ││
│  └──────────────────────────────────────────────────────────┘│
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │  POST /v1/query  │  GET /health │  │  /mcp (FastMCP)  │  │
│  │  ┌───────────┐ │  └──────────────┘  └───────────────────┘  │
│  │  │ Retriever │ │                                           │
│  │  │ (vector + │ │  ┌──────────────────────────────────┐    │
│  │  │ keyword + │ │  │  BackgroundTasks                 │    │
│  │  │ RBAC)     │ │  │  └→ AsyncEvaluator               │    │
│  │  └─────┬─────┘ │  │     └→ FaithfulnessEvaluator     │    │
│  │        ▼       │  │        └→ bad_cases.jsonl        │    │
│  │  ┌───────────┐ │  └──────────────────────────────────┘    │
│  │  │ Rerank    │ │                                           │
│  │  └─────┬─────┘ │                                           │
│  │        ▼       │                                           │
│  │  ┌───────────┐ │                                           │
│  │  │ LLM Gen   │ │                                           │
│  │  │ (1st→2nd) │ │                                           │
│  │  └───────────┘ │                                           │
│  └────────────────┘                                           │
└──────────────────────────────────────────────────────────────┘
         │                 │                  │
         ▼                 ▼                  ▼
   ┌──────────┐    ┌────────────┐    ┌──────────────┐
   │  Milvus   │    │   Redis     │    │ Langfuse      │
   │  (向量库) │    │  (分布式限流)│    │ (LLMOps 追踪)│
   └──────────┘    └────────────┘    └──────────────┘
         │
         ▼
   ┌──────────┐
   │ OpenAI API│  (主模型: GPT-4o / 备用: GPT-4o-mini)
   └──────────┘
```

---

## 四、五道生产防线详解

### 防线 1：多租户 RBAC 数据隔离

**不仅仅是 API 层的 if 判断，而是存储引擎层的强制过滤。**

```
请求 → JWT 解析 tenant_id
     → 注入 request.state.tenant_context
     → Retriever 构建 MetadataFilters(tenant_id="tenant_abc")
     → Milvus 在检索阶段对标量索引做前置过滤
     → 关键词检索同样按租户隔离节点存储
```

- JWT Payload 包含 tenant_id、role、sub
- RBACGuard 支持 admin / editor / viewer 三级角色
- 即使通过 MCP 协议（Claude Desktop）调用，也必须传入 tenant_id

### 防线 2：文档增量同步与幽灵数据清理

- IngestionPipeline 入库时强制注入 `metadata={"tenant_id": ..., "doc_id": ...}`
- SyncManager 提供 `delete_document(doc_id)` 和 `delete_tenant(tenant_id)`
- 上游业务系统删除文档 → 精准清除 Milvus 中对应 Chunks
- 防止"合同已删除但知识库仍能查到"的幽灵幻觉

### 防线 3：网关限流与大模型熔断降级

**限流层（Redis 分布式）：**
- Redis Sorted Set 滑动窗口，Lua 原子脚本保证并发安全
- ZREMRANGEBYSCORE → ZCARD → ZADD 三步原子执行
- Redis 不可用时 fail-open 降级 + 告警

**大模型熔断层：**
```
try:    answer = primary_model(上下文)       # GPT-4o
except: try:    answer = fallback_model(上下文)  # GPT-4o-mini
        except: answer = FALLBACK_RESPONSE      # 友好提示
```

### 防线 4：LLMOps 全链路可观测性

- LlamaIndex CallbackManager 全局挂载 Langfuse 拦截器
- 自动追踪：检索耗时、重排耗时、LLM 调用 Prompt、Token 消耗
- 线上 Bad Case 溯源：通过 Trace ID 还原完整请求上下文

### 防线 5：异步大模型裁判评估中枢

- FaithfulnessEvaluator（LlamaIndex 内置）作为独立裁判模型
- FastAPI BackgroundTasks 异步投递，不阻塞 C 端用户
- Tenacity 指数退避重试（2s→4s→8s）应对裁判模型 429 限流
- passing=False 或 score < 0.8 → 自动写入 `data/bad_cases.jsonl`
- 形成"回答→评估→坏例沉淀→模型微调/提示优化"的质量闭环

---

## 五、Redis 分布式限流详细设计

### 为什么不用内存实现？

| 维度 | 内存方案 | Redis 方案 |
|---|---|---|
| 多进程一致性 | ❌ 每个 worker 独立计数 | ✅ 所有 worker 共享状态 |
| 持久化 | ❌ 进程重启清零 | ✅ RDB/AOF 持久化 |
| 水平扩展 | ❌ 多实例计数分散 | ✅ 所有实例读同一 Redis |
| 原子性 | ❌ 需要加锁 | ✅ Lua 脚本天然原子 |

### Lua 原子脚本

```lua
-- 三条 Redis 命令封装为一次原子操作
redis.call('ZREMRANGEBYSCORE', key, 0, now - window)  -- 清理过期
if redis.call('ZCARD', key) >= max then return 0 end   -- 判断限流
redis.call('ZADD', key, now, member)                    -- 记录请求
redis.call('EXPIRE', key, window * 2)                   -- 自动回收
return 1
```

### 降级策略

- Redis 连接超时 → fail-open（放行），避免限流组件成为单点故障
- 生产建议：Redis 限流 + 本地内存限流作为二级兜底
- Redis Sentinel / Cluster 保证限流层自身高可用

---

## 六、MCP 协议集成

Model Context Protocol 让外部大模型客户端（Claude Desktop、自定义 Agent）可以通过标准化协议调用企业知识工具。

```
Claude Desktop                    IntelliLens-MCP
     │                                  │
     │  tools/list ────────────────────→│  返回: [search_enterprise_knowledge]
     │                                  │
     │  tools/call ────────────────────→│
     │  {name: "search_enterprise_     │
     │   knowledge",                    │
     │   arguments: {query: "...",      │
     │   tenant_id: "tenant_abc"}}     │
     │                                  │  1. 混合检索(RBAC过滤)
     │                                  │  2. LLM 重排序
     │                                  │  3. 大模型生成+降级
     │  ←──── 回答文本 ──────────────── │
```

**安全关键设计**：`tenant_id` 参数为必填，不提供"全局搜索"或无租户变体。

---

## 七、代码目录结构

```
intellilens-mcp/
├── main.py                       # FastAPI 入口 + 中间件 + MCP 挂载
├── requirements.txt              # 全部依赖
├── .env.example                  # 环境变量模板
├── src/
│   ├── config/settings.py        # Pydantic Settings 配置中心
│   ├── middleware/
│   │   ├── auth.py               # JWT + RBAC 鉴权
│   │   └── rate_limiter.py       # Redis 分布式限流
│   ├── observability/tracer.py  # Langfuse 追踪
│   ├── pipeline/
│   │   ├── ingestion.py          # 文档入库 + 元数据注入
│   │   └── sync_manager.py       # 文档删除/同步
│   ├── engine/
│   │   ├── retrievers.py         # 混合检索 + RBAC 过滤
│   │   └── query_engine.py       # 重排 + 生成 + 熔断
│   ├── mcp_server/
│   │   ├── server.py             # FastMCP 实例
│   │   └── tools.py              # MCP 工具注册
│   └── evaluation/evaluator.py  # 异步裁判评估
└── data/                         # .gitkeep + bad_cases.jsonl
```

---

## 八、快速启动

```bash
# 1. 安装依赖
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env: 填写 OPENAI_API_KEY

# 3. 启动 Redis (限流依赖)
docker run -d -p 6379:6379 redis:7-alpine

# 4. 启动服务
python main.py
# → FastAPI:  http://0.0.0.0:8000
# → Swagger:  http://0.0.0.0:8000/docs
# → MCP:      http://0.0.0.0:8000/mcp
```

---

## 九、技术亮点总结（面试可用）

1. **存储层而非应用层的租户隔离**：MetadataFilters 下推到 Milvus，比"检索后过滤"更高效也更安全
2. **Redis Lua 原子限流**：ZSET 滑动窗口 + 单条 Lua 脚本，既解决并发竞态又支持水平扩展
3. **主备模型自动熔断**：两层 try-except + 标准化降级文本，不让用户看到技术错误
4. **异步评估解耦**：BackgroundTasks + Tenacity 指数退避，质量监控不影响用户体验
5. **MCP 标准化工具暴露**：让任何兼容 MCP 的 Agent 都能以受控方式访问企业知识
